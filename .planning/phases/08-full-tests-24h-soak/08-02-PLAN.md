---
phase: 08
plan: 02
plan_id: "08-02"
type: execute
wave: 1
depends_on:
  - "08-01"
files_modified:
  - tests/test_filter_compiler.py
  - tests/test_hidden_data_enforcement.py
  - tests/test_rate_limit.py
  - tests/test_error_catalog.py
  - tests/test_cursor.py
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "All 13 filter operators (exact, not, contains, startswith, endswith, gt, gte, lt, lte, in, notin, isnull, notnull) are individually verified AND covered by a parametrized completeness sweep that asserts the FilterOp Literal contains exactly 13 names — pytest tests/test_filter_compiler.py -x is GREEN."
    - "tests/test_hidden_data_enforcement.py exists as NEW and parametrizes across every (database, hidden_table) pair in config.HIDDEN_TABLES, asserting list_tables strips them; AND parametrizes across every (database, table, hidden_column) tuple sourced via core.config_lookup.hidden_columns_for(...), asserting describe_table omits them — without ever reading config.HIDDEN_COLUMNS directly (D2-10 single-source-of-truth)."
    - "tests/test_rate_limit.py keyword-selectable surfaces remain green for the three rate-limit windows: pytest -k burst, pytest -k sustained, and pytest -k daily each select at least one test that exercises that window."
    - "tests/test_error_catalog.py confirms the locked 11-code catalog (unknown_database, unknown_table, unknown_column, invalid_filter_op, invalid_cursor, invalid_query, unsupported_table_for_fetch, not_found, query_timeout, rate_limited, upstream_unavailable) — pytest tests/test_error_catalog.py -x is GREEN."
    - "tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor (or equivalent qhash-mismatch test) asserts a cursor encoded for shape A rejected with invalid_cursor when reused on shape B."
  artifacts:
    - path: tests/test_filter_compiler.py
      provides: "Extended TEST-01 filter coverage — parametrized 13-op completeness sweep + numeric-ops × column-types matrix"
      contains: "ALL_OPS"
    - path: tests/test_hidden_data_enforcement.py
      provides: "TEST-01 hidden-data sweep — list_tables + describe_table strip every entry in HIDDEN_TABLES + HIDDEN_COLUMNS via parametrize fan-out"
      contains: "test_list_tables_strips_hidden"
    - path: tests/test_rate_limit.py
      provides: "Verified keyword-selectable burst / sustained / daily windows (rename only if the keyword does not already select; no new tests added)"
      contains: "burst"
    - path: tests/test_error_catalog.py
      provides: "Verified 11-code locked catalog assertion remains green"
      contains: "test_all_11_codes_in_catalog"
    - path: tests/test_cursor.py
      provides: "Verified cursor qhash mismatch rejection test exists and is named for VALIDATION.md selectability"
      contains: "test_shape_mismatch_raises_invalid_cursor"
  key_links:
    - from: tests/test_hidden_data_enforcement.py
      to: src/mcp_zeeker/core/config_lookup.py::hidden_columns_for
      via: "Parametrize source — never reads config.HIDDEN_COLUMNS directly (D2-10)"
      pattern: "hidden_columns_for"
    - from: tests/test_filter_compiler.py
      to: src/mcp_zeeker/core/filter_compiler.py::FilterOp
      via: "typing.get_args(FilterOp) — must equal exactly 13 op names"
      pattern: "get_args\\(FilterOp\\)"
    - from: tests/test_rate_limit.py keyword tests
      to: src/mcp_zeeker/core/middleware/rate_limit.py windows
      via: "test function names CONTAIN the literal substrings 'burst', 'sustained', 'daily' so pytest -k selects them"
      pattern: "def test_.*(burst|sustained|daily)"
---

<objective>
Land Phase 8 Wave 1: the unit-test sweep that explicitly covers every TEST-01 contract surface — all 13 filter operators (parametrized completeness sweep), hidden-table and hidden-column enforcement on `list_tables`/`describe_table` (NEW file with parametrize fan-out across the entire denylist), the three rate-limit windows (verified to be keyword-selectable via `pytest -k burst / -k sustained / -k daily`), the locked 11-code error catalog, and cursor qhash mismatch rejection.

Purpose: TEST-01 in REQUIREMENTS.md mandates "Unit tests cover filter mapping (all 11 operators), envelope shape per tool, hidden-table/column enforcement, fragment-parent join behavior, rate-limit burst+sustained+daily windows, cursor qhash mismatch rejection, and the full error code catalog." After this plan, every contract surface in TEST-01 is owned by an explicit, named, keyword-selectable test — and the audit trail from REQUIREMENTS.md → test command → code is complete. The filter-op coverage closes the "13 not 11" discrepancy (08-RESEARCH.md "Filter-Operator Coverage" — the spawn message says 11, the actual `FilterOp` Literal in filter_compiler.py declares 13: the `in/notin` and `isnull/notnull` pairs are listed jointly in some prose but are 4 distinct ops).

Output: One NEW test file (`tests/test_hidden_data_enforcement.py`), one EXTENDED test file (`tests/test_filter_compiler.py` — adds two parametrized sweeps), and three VERIFIED test files (`test_rate_limit.py`, `test_error_catalog.py`, `test_cursor.py` — rename test functions only if the keyword does not already select; do NOT add new tests to these three).
</objective>

## Phase Goal

**As a** Phase 8 reviewer auditing REQUIREMENTS.md TEST-01 traceability, **I want** every contract surface mentioned in TEST-01 (filter ops, hidden-data, rate-limit windows, error catalog, cursor binding) to map to a specific keyword-selectable pytest invocation that exists in the repo today, **so that** the TEST-01 traceability table can move from "Pending" to "Satisfied" with concrete evidence — and any future regression in any of those surfaces fails CI on the next PR.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-full-tests-24h-soak/08-RESEARCH.md
@.planning/phases/08-full-tests-24h-soak/08-PATTERNS.md
@.planning/phases/08-full-tests-24h-soak/08-VALIDATION.md
@src/mcp_zeeker/config.py
@src/mcp_zeeker/core/filter_compiler.py
@src/mcp_zeeker/core/config_lookup.py
@src/mcp_zeeker/core/visibility.py
@src/mcp_zeeker/tools/discovery.py
@tests/test_filter_compiler.py
@tests/test_rate_limit.py
@tests/test_error_catalog.py
@tests/test_cursor.py
@tests/test_config_lookup_single_source.py
@tests/tools/test_list_tables.py
@tests/tools/test_describe_table.py
@tests/conftest.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From src/mcp_zeeker/core/filter_compiler.py lines 30-44:
- `FilterOp = Literal["exact","not","contains","startswith","endswith","gt","gte","lt","lte","in","notin","isnull","notnull"]` — exactly 13 names
- `compile_filters(filters, *, visible_columns, column_types) -> list[tuple[str, str]]` — emits Datasette URL-pair query strings
- The 13 names also appear individually as one test per op in tests/test_filter_compiler.py lines 36-280

From src/mcp_zeeker/config.py:
- `ALLOWED_DATABASES: tuple[str, ...]` — the four databases (zeeker-judgements, pdpc, sg-gov-newsrooms, sglawwatch)
- `HIDDEN_TABLES: dict[str, frozenset[str]]` — per-database hidden table denylist
- `HIDDEN_COLUMNS: dict[str | tuple[str,str], frozenset[str]]` — per-(db,table) and global ('*') hidden column denylist
- D2-10 single-source-of-truth: tests MUST NOT read config.HIDDEN_COLUMNS directly — use `core.config_lookup.hidden_columns_for(db, table)` instead. tests/test_config_lookup_single_source.py AST-scans for direct-read offenders.

From src/mcp_zeeker/core/config_lookup.py:
- `hidden_columns_for(database: str, table: str) -> frozenset[str]` — returns global + per-table union
- This is the canonical lookup that all production code AND tests must use

From src/mcp_zeeker/tools/discovery.py:
- `list_tables(database: str) -> Envelope` — strips hidden tables before envelope.data
- `describe_table(database: str, table: str) -> Envelope` — strips hidden columns from envelope.data["columns"]
- Both raise `ToolError("unknown_table: ...")` for hidden or nonexistent table (DISC-05 — no presence side-channel)

From tests/conftest.py (existing fixtures Phase 8 reuses verbatim — DO NOT add new fixtures):
- `mcp_client` — FastMCP in-memory Client (asyncio context-managed)
- `bound_datasette_client`, `bound_metadata_cache`, `httpx_mock` — for stubbing upstream
- `frozen_retrieved_at` — ISO 8601 fixture for envelope tests
- The existing analog at tests/tools/test_list_tables.py lines 81-136 shows the hidden-table sweep pattern

From tests/test_error_catalog.py lines 42-65 (ALREADY GREEN per Phase 7):
- `test_all_11_codes_in_catalog` asserts CATALOG tuple equals the locked 11-code expected tuple, with set-mismatch + order-mismatch + length assertions all in a single test

From tests/test_cursor.py (Phase 3 origin; verify the qhash-mismatch test name aligns with VALIDATION.md selectability):
- The VALIDATION.md row uses test name `test_shape_mismatch_raises_invalid_cursor` for selectability via `pytest tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor -x`
- If the existing test name differs (e.g. `test_qhash_mismatch_*`), rename the function so the canonical command in VALIDATION.md selects it

From tests/test_rate_limit.py (Phase 7 GREEN body):
- Existing burst/sustained/daily tests live in this file; per 08-PATTERNS.md "tests/test_rate_limit.py MODIFIED" the action is to confirm via grep that each window has at least one test name CONTAINING the literal substring (`burst`, `sustained`, `daily`); rename if absent. Do NOT add new tests.

From .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_hidden_data_enforcement.py:
- Pattern is parametrize across HIDDEN_TABLES + HIDDEN_COLUMNS (the latter via config_lookup.hidden_columns_for to satisfy D2-10)
- Imports mirror tests/tools/test_list_tables.py (config, DatasetteClient, MetadataCache, list_tables, describe_table)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/test_filter_compiler.py with 13-op completeness sweep + numeric × column-type matrix</name>
  <files>tests/test_filter_compiler.py</files>
  <read_first>
    - src/mcp_zeeker/core/filter_compiler.py lines 30-44 (FilterOp Literal — 13 names; the source of truth this test asserts against)
    - tests/test_filter_compiler.py lines 1-280 (existing 13 per-op tests stay verbatim — Phase 8 ADDS parametrized sweeps; DO NOT delete or rewrite the existing per-op tests)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_filter_compiler.py MODIFIED (the exact ALL_OPS tuple + two parametrized test bodies — locked verbatim from RESEARCH.md lines 958-981)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Filter-Operator Coverage (TEST-01 part 1) lines 945-983 (full reference test bodies)
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task table row for `08-02 / TEST-01 (filters)` (canonical command: `pytest tests/test_filter_compiler.py -x`)
  </read_first>
  <behavior>
    - Module-level `ALL_OPS` tuple contains all 13 op names verbatim from filter_compiler.py FilterOp Literal.
    - test_op_in_locked_set: parametrized over ALL_OPS — for each op, assert it appears in `typing.get_args(FilterOp)` AND that `len(get_args(FilterOp)) == 13`. Catches both "op missing" and "op silently added" drift.
    - test_numeric_ops_across_column_types: parametrized over the cross-product of (gt, gte, lt, lte) × (INTEGER, REAL, TEXT) — asserts `compile_filters` emits the documented Datasette URL-pair shape for each combination without raising.
    - All existing 13 per-op tests at lines 36-280 stay verbatim and continue to pass.
    - All new tests are plain (sync) defs — no pytest-asyncio decoration needed.
  </behavior>
  <action>
    Append two parametrized test functions to the END of `tests/test_filter_compiler.py` (after the last existing per-op test). Per 08-PATTERNS.md "extend in place" discipline, do NOT modify or delete any existing test in this file. (a) Add a module-level constant tuple `ALL_OPS = ("exact","not","contains","startswith","endswith","gt","gte","lt","lte","in","notin","isnull","notnull")` — this MUST be the literal 13-name tuple from filter_compiler.py:30-44. (b) Add `@pytest.mark.parametrize("op", ALL_OPS)` over a function `test_op_in_locked_set(op)` that imports `FilterOp` from `mcp_zeeker.core.filter_compiler` and `get_args` from `typing`, then asserts `op in get_args(FilterOp)` AND `len(get_args(FilterOp)) == 13`. The `len == 13` assertion catches both directions of drift (op silently removed → len < 13 fails; op silently added → len > 13 fails). Failure message must name the actual op set: `f"FilterOp drift: declared={get_args(FilterOp)!r}, expected_count=13"`. (c) Add `test_numeric_ops_across_column_types` parametrized via a list comprehension `[(op, ct) for op in ("gt","gte","lt","lte") for ct in ("INTEGER","REAL","TEXT")]` (12 cases). For each case, build a single-element filter via the same `Filter`/`compile_filters` import already used at the top of the file (mirror lines 36-43 verbatim for imports), pass `visible_columns=...` and `column_types={"col": col_type}`, and assert that `compile_filters(...)` returns a list with exactly one tuple whose first element starts with `"col__"` and whose second element is a non-empty string. The exact compiled URL-pair shape per op is documented in filter_compiler.py:30-130 — extract the documented format from the source comment for each op. Per 08-RESEARCH.md "Filter-Operator Coverage" line 977-981 the test docstring must say `"TEST-01 / D3-10: numeric ops behave deterministically by column type."`. NO new fixtures in tests/conftest.py. The file MUST pass `uv run ruff check` and `uv run ruff format --check`. Per 08-VALIDATION.md per-task row: `pytest tests/test_filter_compiler.py -x` MUST exit 0 after this task — the existing 13 per-op tests + new 13 parametrized cases (test_op_in_locked_set) + new 12 parametrized cases (test_numeric_ops_across_column_types) all GREEN.
  </action>
  <acceptance_criteria>
    - ALL_OPS tuple present with 13 entries: `grep -c '^ALL_OPS = ' tests/test_filter_compiler.py` is `>= 1`.
    - `test_op_in_locked_set` function present: `grep -c 'def test_op_in_locked_set' tests/test_filter_compiler.py` is `1`.
    - `test_numeric_ops_across_column_types` function present: `grep -c 'def test_numeric_ops_across_column_types' tests/test_filter_compiler.py` is `1`.
    - Uses typing.get_args: `grep -E 'from typing import.*get_args|typing\\.get_args' tests/test_filter_compiler.py | wc -l | xargs` is `>= 1`.
    - Length assertion present (catches op-add drift): `grep -E 'len\\(get_args\\(FilterOp\\)\\) == 13' tests/test_filter_compiler.py | wc -l | xargs` is `>= 1`.
    - Existing per-op tests unchanged (count of `def test_` increased by exactly 2): track via diff.
    - Per VALIDATION.md row 08-02 / TEST-01 (filters): `uv run pytest tests/test_filter_compiler.py -x` exits 0.
    - Parametrized fan-out collected: `uv run pytest tests/test_filter_compiler.py --collect-only -q 2>&1 | grep -c 'test_op_in_locked_set\\['` is `13` (one per op).
    - Parametrized matrix collected: `uv run pytest tests/test_filter_compiler.py --collect-only -q 2>&1 | grep -c 'test_numeric_ops_across_column_types\\['` is `12` (4 ops × 3 column types).
    - Ruff clean: `uv run ruff check tests/test_filter_compiler.py` exits 0 AND `uv run ruff format --check tests/test_filter_compiler.py` exits 0.
    - No conftest changes: `git diff --name-only tests/conftest.py` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/test_filter_compiler.py -x && uv run pytest tests/test_filter_compiler.py --collect-only -q 2>&1 | grep -E 'test_op_in_locked_set|test_numeric_ops_across_column_types' | wc -l | xargs test 25 -le && uv run ruff check tests/test_filter_compiler.py && uv run ruff format --check tests/test_filter_compiler.py</automated>
  </verify>
  <done>tests/test_filter_compiler.py has the existing 13 per-op tests intact PLUS the new ALL_OPS tuple PLUS the two parametrized sweeps (13-op completeness + 12-case numeric × column-type matrix). pytest collects all 13 + 12 parametrized cases; the file is GREEN; ruff is clean. The "11 vs 13 ops" discrepancy from the spawn message is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_hidden_data_enforcement.py — TEST-01 hidden-data sweep across HIDDEN_TABLES + HIDDEN_COLUMNS</name>
  <files>tests/test_hidden_data_enforcement.py</files>
  <read_first>
    - src/mcp_zeeker/config.py (HIDDEN_TABLES + HIDDEN_COLUMNS dicts — the data the sweep parametrizes over)
    - src/mcp_zeeker/core/config_lookup.py (hidden_columns_for(db, table) — D2-10 single-source-of-truth lookup; tests MUST use this and not read config.HIDDEN_COLUMNS directly)
    - src/mcp_zeeker/tools/discovery.py (list_tables + describe_table — the production handlers under test)
    - tests/tools/test_list_tables.py (full file — analog for hidden-table sweep at lines 81-136 PLUS the imports / fixture wiring at lines 1-50; MIRROR these imports verbatim — config, DatasetteClient, MetadataCache, list_tables)
    - tests/tools/test_describe_table.py (full file — analog for describe_table call shape and httpx_mock setup)
    - tests/test_config_lookup_single_source.py (the D2-10 enforcement test; this Phase 8 test MUST NOT trigger the offender list — use hidden_columns_for, never config.HIDDEN_COLUMNS)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_hidden_data_enforcement.py (full reference pattern — parametrize across (database, hidden_table) + (database, table, hidden_column))
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Test Taxonomy & File Layout row TEST-01 (hidden table/column) — closest analog tests/test_visibility.py
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task rows for `08-02 / TEST-01 (hidden-table)` and `08-02 / TEST-01 (hidden-column)` (canonical commands)
    - tests/conftest.py (existing fixtures: bound_datasette_client, bound_metadata_cache, httpx_mock — Phase 8 reuses, does not add)
  </read_first>
  <behavior>
    - test_list_tables_strips_hidden: parametrized across every (database, hidden_table) tuple generated from `config.HIDDEN_TABLES`. For each pair, stub the upstream `/-/tables.json` to include the hidden_table in the upstream response (so the test exercises the STRIPPING code path, not the absence-from-upstream trivial pass — per 08-RESEARCH.md Pitfall 5), call `list_tables(database)`, assert `hidden_table not in {row["name"] for row in envelope.data}`.
    - test_describe_table_strips_hidden_columns: parametrized across every (database, table, hidden_column) tuple sourced via `core.config_lookup.hidden_columns_for(db, table)` for each (db, table) where the function returns non-empty AND the table is one of the production-known visible tables for that database. For each tuple, stub upstream to include the hidden_column in the column list, call `describe_table(database, table)`, assert `hidden_column not in {col["name"] for col in envelope.data["columns"]}`.
    - Failure messages name the parametrized triple (database, table, hidden_column) so triage is one-line.
    - The test file MUST NOT contain any direct read of `config.HIDDEN_COLUMNS` (would fail tests/test_config_lookup_single_source.py D2-10 enforcement).
  </behavior>
  <action>
    Create the new file `tests/test_hidden_data_enforcement.py`. (a) Imports verbatim mirror tests/tools/test_list_tables.py lines 1-15 — `from __future__ import annotations`, `import pytest`, `import pytest_httpx`, `from fastmcp.exceptions import ToolError`, `from mcp_zeeker import config`, `from mcp_zeeker.core.datasette_client import DatasetteClient`, `from mcp_zeeker.core.metadata_cache import MetadataCache`, plus `from mcp_zeeker.core.config_lookup import hidden_columns_for`, `from mcp_zeeker.tools.discovery import describe_table, list_tables`. NO direct import of `config.HIDDEN_COLUMNS` — only `hidden_columns_for(db, table)`. (b) Define a module-level helper `_iter_hidden_table_pairs() -> list[tuple[str, str]]` that iterates `config.HIDDEN_TABLES.items()` and emits `(database, hidden_table_name)` for every (database, frozenset) pair. Direct read of `config.HIDDEN_TABLES` IS allowed (D2-10 specifically protects HIDDEN_COLUMNS, not HIDDEN_TABLES — verify by inspecting tests/test_config_lookup_single_source.py to be sure). (c) Define `_iter_hidden_column_triples() -> list[tuple[str, str, str]]` that iterates `config.ALLOWED_DATABASES` and, for each database, iterates the production-known visible tables for that DB (source from the existing test fixtures in tests/tools/test_list_tables.py — copy the visible-table list inline, do NOT introspect upstream); for each (db, table), call `hidden_columns_for(db, table)` and emit one (db, table, hidden_col) triple per hidden column returned. (d) Define an async test `test_list_tables_strips_hidden` decorated with `@pytest.mark.parametrize("database, hidden_table", _iter_hidden_table_pairs())`. The test takes `bound_datasette_client`, `bound_metadata_cache`, `httpx_mock` from existing conftest.py fixtures (DO NOT add new fixtures). Stub the upstream `/{database}.json` (per the existing test_list_tables.py pattern at lines 81-110 — use `_db_url(database)` and `_tables_payload(_simple_tables([..., hidden_table]))` shape from that file; copy the helper functions inline if they are not exported). The stubbed payload MUST INCLUDE the hidden_table in the upstream response — exercising the STRIPPING code path (per 08-RESEARCH.md Pitfall 5: a stub that omits the hidden table makes the test trivially pass even if production code is broken). Call `envelope = await list_tables(database)`. Assert `hidden_table not in {row["name"] for row in envelope.data}` with diff-readable message: `f"hidden_table {hidden_table!r} leaked from list_tables({database!r}); envelope.data names: {[row['name'] for row in envelope.data]!r}"`. (e) Define an async test `test_describe_table_strips_hidden_columns` decorated with `@pytest.mark.parametrize("database, table, hidden_column", _iter_hidden_column_triples())`. Same fixture set. Stub upstream `/{database}/{table}.json` with a payload whose column list INCLUDES the hidden_column (forcing the strip path). Call `envelope = await describe_table(database, table)`. Assert `hidden_column not in {col["name"] for col in envelope.data["columns"]}`. (f) NO new fixtures in tests/conftest.py per 08-RESEARCH.md "Wave 0 Requirements" line 88. (g) Per 08-PATTERNS.md "tests/test_hidden_data_enforcement.py" — the imports follow tests/tools/test_list_tables.py:1-15 verbatim; the parametrize source iterates the entire denylist (NOT one hand-crafted case). The file MUST be ruff-clean. Per VALIDATION.md per-task rows: both `pytest tests/test_hidden_data_enforcement.py::test_list_tables_strips_hidden -x` AND `pytest tests/test_hidden_data_enforcement.py::test_describe_table_strips_hidden_columns -x` MUST exit 0.
  </action>
  <acceptance_criteria>
    - File exists: `test -f tests/test_hidden_data_enforcement.py` exits 0.
    - test_list_tables_strips_hidden present: `grep -c 'def test_list_tables_strips_hidden' tests/test_hidden_data_enforcement.py` is `1`.
    - test_describe_table_strips_hidden_columns present: `grep -c 'def test_describe_table_strips_hidden_columns' tests/test_hidden_data_enforcement.py` is `1`.
    - Uses hidden_columns_for (NOT direct config.HIDDEN_COLUMNS read): `grep -c 'hidden_columns_for' tests/test_hidden_data_enforcement.py` is `>= 1`.
    - No direct config.HIDDEN_COLUMNS access: `grep -E 'config\\.HIDDEN_COLUMNS' tests/test_hidden_data_enforcement.py | grep -v '^#' | wc -l | xargs` is `0`.
    - D2-10 enforcement test still passes: `uv run pytest tests/test_config_lookup_single_source.py -x` exits 0 (the new file does NOT trigger the offender list).
    - Per VALIDATION.md row 08-02 / TEST-01 (hidden-table): `uv run pytest tests/test_hidden_data_enforcement.py::test_list_tables_strips_hidden -x` exits 0.
    - Per VALIDATION.md row 08-02 / TEST-01 (hidden-column): `uv run pytest tests/test_hidden_data_enforcement.py::test_describe_table_strips_hidden_columns -x` exits 0.
    - Whole file passes: `uv run pytest tests/test_hidden_data_enforcement.py -x` exits 0.
    - Parametrize fan-out is non-trivial: `uv run pytest tests/test_hidden_data_enforcement.py --collect-only -q 2>&1 | grep -E 'test_list_tables_strips_hidden\\[|test_describe_table_strips_hidden_columns\\[' | wc -l | xargs` is `>= 4` (at minimum: a few hidden tables × DBs and a few hidden columns).
    - Ruff clean: `uv run ruff check tests/test_hidden_data_enforcement.py` exits 0 AND `uv run ruff format --check tests/test_hidden_data_enforcement.py` exits 0.
    - No conftest changes: `git diff --name-only tests/conftest.py` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/test_hidden_data_enforcement.py tests/test_config_lookup_single_source.py -x && uv run ruff check tests/test_hidden_data_enforcement.py && uv run ruff format --check tests/test_hidden_data_enforcement.py</automated>
  </verify>
  <done>tests/test_hidden_data_enforcement.py exists; both parametrized sweeps GREEN across the entire HIDDEN_TABLES / HIDDEN_COLUMNS denylist; D2-10 single-source-of-truth invariant preserved (config_lookup.hidden_columns_for used, never direct config.HIDDEN_COLUMNS read); ruff clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify keyword-selectability of rate-limit windows + 11-code error catalog + cursor qhash mismatch — rename only if needed</name>
  <files>tests/test_rate_limit.py, tests/test_error_catalog.py, tests/test_cursor.py</files>
  <read_first>
    - tests/test_rate_limit.py (full file — Phase 7 GREEN body; identify which test names contain the substring `burst`, `sustained`, `daily` and rename only if a window has zero matches)
    - tests/test_error_catalog.py lines 42-65 (test_all_11_codes_in_catalog — Phase 7 GREEN; this task verifies, does not edit unless a code is missing)
    - tests/test_cursor.py (full file — identify the qhash-mismatch test; verify VALIDATION.md selectability via `pytest tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor -x`. If the existing function is named differently, rename via Edit so the canonical command in VALIDATION.md selects it.)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_rate_limit.py MODIFIED (action is "Confirm by grep ... If a window has no test name containing the keyword, ADD the keyword to the function name (rename, not new test). Do NOT add new tests — Phase 7 closed RATE-01..06.")
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_error_catalog.py MODIFIED ("No edit needed unless a code is missing.")
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task rows for `08-02 / TEST-01 (rate-limit burst)`, `(rate-limit sustained)`, `(rate-limit daily)`, `(errors)`, `(cursor)` — these are the canonical commands MUST exit 0 after this task
  </read_first>
  <behavior>
    - After this task: `pytest tests/test_rate_limit.py -k burst -x` selects ≥ 1 test that exercises the burst window AND exits 0.
    - After this task: `pytest tests/test_rate_limit.py -k sustained -x` selects ≥ 1 test that exercises the sustained window AND exits 0.
    - After this task: `pytest tests/test_rate_limit.py -k daily -x` selects ≥ 1 test that exercises the daily window AND exits 0.
    - After this task: `pytest tests/test_error_catalog.py -x` exits 0 with the 11-code locked catalog assertion still passing.
    - After this task: `pytest tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor -x` exits 0 (the function exists and has that exact name).
    - NO new tests added. NO behavior changes to any existing test. The only allowed edit is renaming an existing function name to make a `pytest -k` keyword select it (per 08-PATTERNS.md "rename, not new test").
  </behavior>
  <action>
    Step (A) — RATE-LIMIT KEYWORD VERIFICATION. Run: `grep -n 'def test_' tests/test_rate_limit.py`. For each of the three keywords `burst`, `sustained`, `daily`: confirm at least one test function name CONTAINS the substring (case-insensitive grep is fine). If any keyword has zero matches: identify the existing test that exercises that window (per Phase 7 PLANs 07-02 / 07-03 the tests exist — they may simply be named differently, e.g. `test_60_per_minute_refill` instead of `test_sustained_*`). Use the Edit tool to rename ONLY the function name (and any internal references in the test docstring) so the keyword appears. DO NOT change test body, fixtures, or behavior. Per 08-PATTERNS.md: "rename, not new test." Step (B) — ERROR CATALOG VERIFICATION. Run `pytest tests/test_error_catalog.py -x`. If GREEN, no edit needed (per 08-PATTERNS.md). If RED, surface the failure verbatim — do NOT mutate the test or the catalog without an explicit catalog update. Step (C) — CURSOR QHASH-MISMATCH SELECTABILITY. Run `pytest tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor -x`. If RED with collection error ("test not found"), the function exists under a different name. Run `grep -n 'def test_.*\\(qhash\\|shape\\|invalid_cursor\\|mismatch\\)' tests/test_cursor.py` to find it. Rename the existing function to `test_shape_mismatch_raises_invalid_cursor` so the canonical VALIDATION.md command selects it. DO NOT add a new test. Step (D) — RUN FULL UNIT SUITE to confirm no regression: `uv run pytest -x -q`. Step (E) — RUN RUFF on the three files: `uv run ruff check tests/test_rate_limit.py tests/test_error_catalog.py tests/test_cursor.py` AND `uv run ruff format --check tests/test_rate_limit.py tests/test_error_catalog.py tests/test_cursor.py`. Per 08-VALIDATION.md per-task table: all five commands (`-k burst`, `-k sustained`, `-k daily`, error catalog, cursor) MUST exit 0 after this task.
  </action>
  <acceptance_criteria>
    - Per VALIDATION.md row 08-02 / TEST-01 (rate-limit burst): `uv run pytest tests/test_rate_limit.py -k burst -x` exits 0 AND selects ≥ 1 test (verify via `--collect-only -q` count `>= 1`).
    - Per VALIDATION.md row 08-02 / TEST-01 (rate-limit sustained): `uv run pytest tests/test_rate_limit.py -k sustained -x` exits 0 AND selects ≥ 1 test.
    - Per VALIDATION.md row 08-02 / TEST-01 (rate-limit daily): `uv run pytest tests/test_rate_limit.py -k daily -x` exits 0 AND selects ≥ 1 test.
    - Per VALIDATION.md row 08-02 / TEST-01 (errors): `uv run pytest tests/test_error_catalog.py -x` exits 0.
    - Per VALIDATION.md row 08-02 / TEST-01 (cursor): `uv run pytest tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor -x` exits 0.
    - No NEW tests added to any of the three files (line count of `def test_` may have stayed the same OR increased only via parametrize fan-out, never via a wholly new function): track via diff.
    - Full suite remains GREEN: `uv run pytest -x` exits 0.
    - Ruff clean on all three files: `uv run ruff check tests/test_rate_limit.py tests/test_error_catalog.py tests/test_cursor.py` exits 0 AND `uv run ruff format --check tests/test_rate_limit.py tests/test_error_catalog.py tests/test_cursor.py` exits 0.
    - No conftest changes: `git diff --name-only tests/conftest.py` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/test_rate_limit.py -k burst -x && uv run pytest tests/test_rate_limit.py -k sustained -x && uv run pytest tests/test_rate_limit.py -k daily -x && uv run pytest tests/test_error_catalog.py -x && uv run pytest "tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor" -x && uv run pytest -x -q && uv run ruff check tests/test_rate_limit.py tests/test_error_catalog.py tests/test_cursor.py</automated>
  </verify>
  <done>All five canonical VALIDATION.md commands for the rate-limit / error-catalog / cursor surfaces exit 0; renames (if any) preserve test behavior; no new tests added; full suite GREEN; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test parametrize source ↔ config.HIDDEN_TABLES / config.HIDDEN_COLUMNS | If the test reads HIDDEN_COLUMNS directly (instead of via config_lookup), it bypasses the D2-10 single-source-of-truth invariant Phase 2/3 locked — and a future schema change to HIDDEN_COLUMNS structure would break the test in confusing ways |
| Test stub upstream ↔ production strip path | If the stub omits the hidden item (per 08-RESEARCH.md Pitfall 5), the test passes trivially even when production code is broken — the stub MUST INCLUDE the hidden item to exercise the strip path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DATA-LEAK | Information Disclosure (verified by tests) | tools/discovery.py list_tables + describe_table strip paths | mitigate | Task 2's parametrized sweep across the entire HIDDEN_TABLES + HIDDEN_COLUMNS denylist asserts every entry is stripped from the corresponding tool envelope when the upstream payload includes it. The sweep is keyword-selectable via the canonical VALIDATION.md commands, so any production regression that re-exposes a denylisted name fails CI. |
| T-INPUT (filters) | Tampering (filter-op spoofing / silent op-set drift) | core/filter_compiler.py FilterOp Literal | mitigate | Task 1's `test_op_in_locked_set` parametrized sweep + `len(get_args(FilterOp)) == 13` assertion catches both directions of drift (op silently removed or silently added). Closes the spawn message's "11 vs 13" discrepancy with a single source of truth (the FilterOp Literal itself). |
| T-DOS (verified rate-limit math) | Denial of Service (verified by tests) | core/middleware/rate_limit.py burst + sustained + daily windows | mitigate | Task 3 verifies all three windows are keyword-selectable via `pytest -k burst / sustained / daily`. The underlying tests (Phase 7 GREEN body) remain unchanged — Phase 8 only ensures discoverability for the VALIDATION.md per-task table. |
| T-INPUT (cursor) | Tampering (cursor reuse across query shapes) | core/cursor.py qhash binding | mitigate | Task 3 ensures the canonical VALIDATION.md command (`pytest tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor -x`) selects an existing test that exercises shape-A-encoded → shape-B-decoded rejection. Renames if necessary; does not add new tests. |
| T-D2-10-BYPASS | Tampering (test bypasses single-source-of-truth invariant) | tests/test_hidden_data_enforcement.py parametrize source | mitigate | The test file uses `core.config_lookup.hidden_columns_for(...)` exclusively for HIDDEN_COLUMNS lookup — never reads `config.HIDDEN_COLUMNS` directly. The acceptance criteria explicitly assert `grep -E 'config\\.HIDDEN_COLUMNS' tests/test_hidden_data_enforcement.py | grep -v '^#'` returns zero matches. tests/test_config_lookup_single_source.py D2-10 enforcement test continues to pass after this plan. |
</threat_model>

<verification>
- VALIDATION.md per-task table rows covered by this plan (all must flip to ✅ green):
  - `08-02 / TEST-01 (filters)` — `pytest tests/test_filter_compiler.py -x`
  - `08-02 / TEST-01 (envelope)` — `pytest tests/test_envelope_snapshot.py -x` (already GREEN; this plan does not touch the file)
  - `08-02 / TEST-01 (hidden-table)` — `pytest tests/test_hidden_data_enforcement.py::test_list_tables_strips_hidden -x`
  - `08-02 / TEST-01 (hidden-column)` — `pytest tests/test_hidden_data_enforcement.py::test_describe_table_strips_hidden_columns -x`
  - `08-02 / TEST-01 (rate-limit burst)` — `pytest tests/test_rate_limit.py -k burst -x`
  - `08-02 / TEST-01 (rate-limit sustained)` — `pytest tests/test_rate_limit.py -k sustained -x`
  - `08-02 / TEST-01 (rate-limit daily)` — `pytest tests/test_rate_limit.py -k daily -x`
  - `08-02 / TEST-01 (errors)` — `pytest tests/test_error_catalog.py -x`
  - `08-02 / TEST-01 (cursor)` — `pytest tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor -x`
- D2-10 single-source-of-truth invariant preserved: `uv run pytest tests/test_config_lookup_single_source.py -x` exits 0.
- Full unit suite GREEN: `uv run pytest -x -q` exits 0.
- Ruff clean: `uv run ruff check src tests` exits 0 AND `uv run ruff format --check src tests` exits 0.
- File-touch discipline: `git diff --name-only` shows exactly five files from this plan — `tests/test_filter_compiler.py`, `tests/test_hidden_data_enforcement.py`, `tests/test_rate_limit.py` (rename only if needed), `tests/test_error_catalog.py` (likely no change), `tests/test_cursor.py` (rename only if needed). `tests/conftest.py` is NOT touched.
</verification>

<success_criteria>
- All 13 filter operators are covered by individual GREEN tests AND a parametrized completeness sweep, AND a numeric-ops × column-types matrix; the FilterOp Literal cannot silently drift in either direction.
- Hidden-table and hidden-column enforcement is parametrized across the entire `config.HIDDEN_TABLES` + `config.HIDDEN_COLUMNS` denylist — every entry has a regression test, and the test exercises the STRIP path (not the trivially-pass-when-stub-omits path per 08-RESEARCH.md Pitfall 5).
- The three rate-limit windows (burst, sustained, daily) are each keyword-selectable via `pytest -k <window> tests/test_rate_limit.py -x`, satisfying the VALIDATION.md per-task table.
- The locked 11-code error catalog assertion (Phase 7 GREEN) remains GREEN.
- The cursor qhash-mismatch test is selectable via the canonical VALIDATION.md path `tests/test_cursor.py::test_shape_mismatch_raises_invalid_cursor`.
- D2-10 single-source-of-truth invariant preserved (no direct `config.HIDDEN_COLUMNS` reads in the new test file).
- Full pytest suite GREEN; ruff clean across `src` and `tests`; `tests/conftest.py` untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-tests-24h-soak/08-02-SUMMARY.md` documenting:
- The exact ALL_OPS tuple as added to tests/test_filter_compiler.py (paste verbatim).
- The parametrize fan-out counts (e.g., "test_op_in_locked_set: 13 cases; test_numeric_ops_across_column_types: 12 cases; test_list_tables_strips_hidden: N cases; test_describe_table_strips_hidden_columns: M cases").
- For Task 3, list any function renames performed (or "no renames needed" if the keywords already selected).
- Confirmation that tests/conftest.py was NOT modified.
- Confirmation that tests/test_config_lookup_single_source.py D2-10 enforcement test still passes.
- A 1-line forward pointer: "TEST-01 unit-coverage gate locked; 08-03 (TEST-03/04/06) and 08-04/05/06 (TEST-02 + TEST-05 + NFR-05) may proceed."
</output>
</content>
</invoke>