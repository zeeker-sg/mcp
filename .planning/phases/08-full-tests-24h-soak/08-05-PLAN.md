---
phase: 08
plan: 05
plan_id: "08-05"
type: execute
wave: 3
depends_on:
  - "08-01"
files_modified:
  - scripts/soak/__init__.py
  - scripts/soak/run_soak.py
  - scripts/soak/workload.py
  - scripts/soak/rss_sampler.py
  - scripts/soak/report.py
  - tests/_corpus/soak_workload.py
  - .github/workflows/soak.yml
autonomous: true
requirements:
  - TEST-05
  - NFR-01
  - NFR-02
  - NFR-03
must_haves:
  truths:
    - "scripts/soak/ exists as a package (with __init__.py marker), housing five modules: run_soak.py (asyncio + httpx driver, --duration / --concurrency / --target-url / --out-dir / --workload CLI), workload.py (re-exports the canonical workload from tests/_corpus/soak_workload.py per single-source-of-truth), rss_sampler.py (pure-stdlib /proc/{pid}/status on Linux + resource.getrusage() macOS fallback — NO psutil per NFR-04), and report.py (CSV → markdown summary + CLI exit-code gate on --max-p50-ms 300 / --max-p95-ms 1500 / --max-rss-mb 256)."
    - "tests/_corpus/soak_workload.py exists with a typed `WORKLOAD: list[tuple[str, dict, float]]` constant that defines the synthetic request mix (35% discovery / 30% query_url / 20% search / 10% fetch / 5% fragment per 08-RESEARCH.md observed-traffic distribution); scripts/soak/workload.py re-exports it via `from tests._corpus.soak_workload import WORKLOAD`."
    - ".github/workflows/soak.yml exists as a sibling to live-tests.yml: workflow_dispatch ONLY (no cron — too expensive per 08-RESEARCH.md line 1174), timeout-minutes 1500 (25h budget per 08-PATTERNS.md), starts uvicorn in --workers 1 mode in background, runs the 24h driver, then runs the report gate, then uploads ./soak-results/ as an artifact via actions/upload-artifact@v4."
    - "Smoke soak verification (1 minute, 5 concurrent) passes the report-gate thresholds (p50 < 300ms, p95 < 1.5s, RSS < 256 MB) when run locally against a freshly-started single-worker uvicorn — closing the VALIDATION.md per-task row 08-05 / TEST-05 (smoke)."
    - "soak harness imports zero new runtime deps and zero new dev deps — uses only stdlib (argparse, asyncio, csv, datetime, json, os, pathlib, random, re, resource, time) plus already-pinned httpx — preserving NFR-04."
  artifacts:
    - path: scripts/soak/__init__.py
      provides: "Empty package marker (mirrors tests/_corpus/__init__.py — 0 bytes)"
      contains: ""
    - path: scripts/soak/run_soak.py
      provides: "asyncio + httpx soak driver with argparse CLI (--duration, --concurrency, --target-url, --out-dir, --workload); writes latency.csv + rss.csv"
      contains: "asyncio.Semaphore"
    - path: scripts/soak/workload.py
      provides: "Re-exports tests._corpus.soak_workload.WORKLOAD via single-source-of-truth import"
      contains: "from tests._corpus.soak_workload import WORKLOAD"
    - path: scripts/soak/rss_sampler.py
      provides: "Pure-stdlib RSS sampler — rss_kb_from_proc(pid) for Linux /proc/{pid}/status, rss_kb_from_self() macOS fallback via resource.getrusage()"
      contains: "VmRSS"
    - path: scripts/soak/report.py
      provides: "CSV reducer + CLI exit-code gate; argparse threshold flags (--max-p50-ms, --max-p95-ms, --max-rss-mb); markdown summary writer with daily-rollover detection"
      contains: "daily_rollover_observed"
    - path: tests/_corpus/soak_workload.py
      provides: "Canonical synthetic workload (typed list of (tool_name, args_payload, weight) tuples) — single source of truth for the soak driver and any future regression tests"
      contains: "WORKLOAD"
    - path: .github/workflows/soak.yml
      provides: "workflow_dispatch-only 24h soak CI workflow with background uvicorn + driver + report gate + artifact upload"
      contains: "timeout-minutes: 1500"
  key_links:
    - from: scripts/soak/run_soak.py
      to: scripts/soak/workload.py
      via: "from scripts.soak.workload import WORKLOAD — driver iterates WORKLOAD entries, weights random selection"
      pattern: "from scripts.soak.workload"
    - from: scripts/soak/workload.py
      to: tests/_corpus/soak_workload.py
      via: "from tests._corpus.soak_workload import WORKLOAD as WORKLOAD — re-export for single-source-of-truth"
      pattern: "from tests._corpus.soak_workload"
    - from: scripts/soak/run_soak.py
      to: scripts/soak/rss_sampler.py
      via: "rss_kb_from_proc(server_pid) sampled every --rss-sample-interval seconds (default 60s); written to rss.csv"
      pattern: "rss_kb_from_proc"
    - from: scripts/soak/report.py
      to: scripts/soak/run_soak.py CSV outputs
      via: "Reads ./soak-results/latency.csv + rss.csv; computes percentiles via sorted(samples)[int(p * len(samples))] (NO hdrhistogram); exits non-zero on threshold breach"
      pattern: "max_p50_ms|max_p95_ms|max_rss_mb"
    - from: .github/workflows/soak.yml
      to: scripts/soak/run_soak.py + scripts/soak/report.py
      via: "Background uvicorn --workers 1 → driver `uv run python -m scripts.soak.run_soak --duration 86400 --concurrency 50` → `uv run python -m scripts.soak.report --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256` → upload artifact"
      pattern: "scripts.soak.run_soak"
---

<objective>
Land Phase 8 Wave 3b: the 24h soak harness — five new modules under `scripts/soak/`, one shared workload corpus under `tests/_corpus/`, and a manual-trigger CI workflow under `.github/workflows/`. Closes TEST-05 (24h soak validates p50 < 300ms, p95 < 1.5s for non-fragment tools, stable RSS < 256 MB, 50 concurrent without saturation, no PoolTimeout cascade, bounded log growth, daily rate-limit rollover) and gates NFR-01 (latency), NFR-02 (concurrency), NFR-03 (memory).

Purpose: Unit tests cover handler logic against stubbed upstream; live tests cover end-to-end correctness against real upstream. Neither catches steady-state regressions: a memory leak that grows 1 MB/hour, a connection-pool that exhausts after 4 hours, a daily-counter rollover bug that surfaces only at 00:00 UTC. The soak is the only signal for these — and per 08-RESEARCH.md it MUST run in a SEPARATE process from the driver (asyncio scheduling pressure from a co-located driver inflates p95 by 3×). Harness deliverables are dev-only (zero runtime dep delta) and pure-stdlib + already-pinned httpx (zero dev dep delta — preserves NFR-04 for which Plan 08-01 just locked the gate).

Output: Six NEW Python modules (scripts/soak package + tests/_corpus/soak_workload.py) and one NEW CI workflow (.github/workflows/soak.yml — sibling of 08-04's live-tests.yml). The smoke soak (1 minute, 5 concurrent) verifies the harness end-to-end as part of this plan's verification gate; the full 24h soak is deferred to a pre-release manual workflow_dispatch invocation per 08-VALIDATION.md "Manual-Only Verifications".
</objective>

## Phase Goal

**As an** operator preparing the v1 pre-release of mcp.zeeker.sg, **I want** a one-command 24h soak that validates p50/p95 latency, 50-concurrent throughput, RSS growth, PoolTimeout cascade absence, and daily-counter rollover correctness without any runtime-dep additions and without polluting production code with test infrastructure, **so that** I have a high-confidence go/no-go signal for the Phase 9 registry submission and can reproduce the soak locally on a dev machine if a CI failure needs root-cause analysis.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-full-tests-24h-soak/08-RESEARCH.md
@.planning/phases/08-full-tests-24h-soak/08-PATTERNS.md
@.planning/phases/08-full-tests-24h-soak/08-VALIDATION.md
@src/mcp_zeeker/app.py
@src/mcp_zeeker/config.py
@src/mcp_zeeker/core/middleware/rate_limit.py
@src/mcp_zeeker/core/middleware/access_log.py
@src/mcp_zeeker/core/ip.py
@tests/_corpus/__init__.py
@tests/_corpus/hostile_inputs.py
@tests/conftest.py
@pyproject.toml
@README.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From src/mcp_zeeker/app.py:
- `app: Starlette` — the production ASGI app; soak runs against it via `uvicorn mcp_zeeker.app:app --host 127.0.0.1 --port 8000 --workers 1`
- The /mcp endpoint is mounted under POST + GET on `/mcp/`
- The /healthz endpoint is at GET /healthz (returns 200 with minimal payload — soak driver may use for warm-up)

From src/mcp_zeeker/core/middleware/rate_limit.py:
- The middleware enforces 20-burst / 60-min / 5000-per-IP-per-24h
- Soak driver uses a single TCP peer → bucket store stays size 1 (per 08-RESEARCH.md A8); the daily-rollover gate IS observable when the soak crosses UTC midnight

From src/mcp_zeeker/core/ip.py:
- `ip_prefix(addr)` — returns "_invalid" for non-parseable input or canonical /24 / /48 (Phase 7 / 07-07 closure); soak driver does NOT pre-set XFF, so requests appear from 127.0.0.1 → ip_prefix("127.0.0.1") returns "127.0.0"

From tests/_corpus/__init__.py (analog for scripts/soak/__init__.py):
- 0-byte file (verified per 08-PATTERNS.md "scripts/soak/__init__.py" — empty file, no imports, no docstring)

From tests/_corpus/hostile_inputs.py (analog for tests/_corpus/soak_workload.py):
- Module shape: `from __future__ import annotations` + module docstring + module-level typed constant + helper functions colocated
- Re-export pattern: 08-PATTERNS.md "tests/_corpus/soak_workload.py" allows EITHER (a) canonical in scripts/soak/workload.py + re-export from tests/_corpus, OR (b) canonical in tests/_corpus/soak_workload.py + re-export from scripts/soak/workload.py. Per 08-PATTERNS.md "Recommendation per Phase 7 PATTERNS.md single-source-of-truth discipline: canonical in scripts/soak/workload.py; tests/_corpus/soak_workload.py re-exports via from scripts.soak.workload import WORKLOAD." — BUT scripts/ is not pytest-rooted, so importing the other direction (tests/_corpus → scripts/soak) requires sys.path adjustment. THIS PLAN INVERTS the recommendation per ergonomic concerns: canonical in tests/_corpus/soak_workload.py (which is on the pytest path); scripts/soak/workload.py re-exports via `from tests._corpus.soak_workload import WORKLOAD as WORKLOAD` with a one-line sys.path adjustment if needed. Document the choice in this plan's SUMMARY.

From src/mcp_zeeker/core/ip.py (pure-stdlib pattern analog for scripts/soak/rss_sampler.py):
- `from __future__ import annotations` + stdlib-only imports + tight single-purpose helpers

From .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § scripts/soak/rss_sampler.py:
- Concrete code locked verbatim (rss_kb_from_proc + rss_kb_from_self with macOS / Linux unit branch)
- The `os.uname().sysname == "Darwin"` branch is unprecedented elsewhere in the codebase — document inline

From .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § scripts/soak/run_soak.py:
- Driver shape: argparse CLI + asyncio.Semaphore(N) concurrency + httpx.AsyncClient(timeout=httpx.Timeout(connect=5.0, read=30.0, write=5.0, pool=5.0)) + per-request error categorization (pool_timeout / request_timeout / 5xx / 4xx / 429) + CSV outputs
- The driver does NOT spawn the server (per 08-PATTERNS.md "the operator runs uvicorn in terminal A; the driver only takes --target-url")
- The driver does NOT retry (per 08-RESEARCH.md Open Question 5 — failure rate IS a measurement)

From .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § scripts/soak/report.py:
- CSV reducer + CLI threshold gate; sort-then-index percentile per 08-RESEARCH.md "Don't Hand-Roll" line 316
- Daily-rollover detection: bucket 429 count per UTC minute; flag drop > 50% within ±60s of any midnight (per 08-PATTERNS.md "Daily-rollover detection pseudocode")
- Markdown summary writer (no jinja2): plain string formatting; sections for Latency / RSS / Errors / Daily rollover

From .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § .github/workflows/soak.yml:
- Sibling of live-tests.yml (08-04) with deltas: workflow_dispatch ONLY (no cron), timeout-minutes 1500 (25h), background uvicorn step, driver step, report step, actions/upload-artifact@v4 with `if: always()`

From CLAUDE.md tech stack:
- Single Uvicorn worker (RATE-06) — soak MUST run with --workers 1
- uv 0.11.14 pinned (matches Plan 08-04's setup-uv version)
- Python 3.11+ (resource module + tomllib both available)

From pyproject.toml:
- httpx is already a runtime dep (`httpx~=0.28`) — soak driver imports it freely
- argparse, asyncio, csv, datetime, json, os, pathlib, random, re, resource, time are all stdlib — zero dep delta
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/soak/ package — __init__.py + rss_sampler.py + workload.py + tests/_corpus/soak_workload.py</name>
  <files>scripts/soak/__init__.py, scripts/soak/rss_sampler.py, scripts/soak/workload.py, tests/_corpus/soak_workload.py</files>
  <read_first>
    - tests/_corpus/__init__.py (analog for the empty package marker — confirm it is 0 bytes via `wc -c`)
    - tests/_corpus/hostile_inputs.py lines 1-30 (analog for the soak_workload.py module shape — `from __future__` + docstring + module-level typed constant)
    - src/mcp_zeeker/core/ip.py lines 1-15 (pure-stdlib helper pattern analog for rss_sampler.py)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § scripts/soak/__init__.py (empty file rule)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § scripts/soak/rss_sampler.py (verbatim concrete code with macOS / Linux unit branch)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § scripts/soak/workload.py (single-source-of-truth re-export pattern; this plan picks: canonical in tests/_corpus/soak_workload.py, re-export from scripts/soak/workload.py)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/_corpus/soak_workload.py (canonical workload definition pattern)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Soak workload definition (NEW) lines 766-868 (full reference workload module body)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § RSS sampling — process placement decision lines 1114-1149 (rss_sampler concrete body)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Soak Harness § "Distribution mirrors observed Phase 6.1 manual-UAT traffic" lines 772-779 (35% discovery / 30% query_url / 20% search / 10% fetch / 5% fragment)
  </read_first>
  <behavior>
    - scripts/soak/__init__.py is exactly 0 bytes (matches tests/_corpus/__init__.py).
    - scripts/soak/rss_sampler.py exposes two functions: `rss_kb_from_proc(pid: int) -> int | None` (reads /proc/{pid}/status VmRSS field; returns None on non-Linux or read failure) and `rss_kb_from_self() -> int` (resource.getrusage(RUSAGE_SELF).ru_maxrss with macOS-vs-Linux unit normalization to KB). Stdlib-only imports.
    - tests/_corpus/soak_workload.py exposes `WORKLOAD: list[tuple[str, dict[str, Any], float]]` — the canonical (tool_name, args_payload, weight) tuple list with the 5-bucket distribution (35/30/20/10/5).
    - scripts/soak/workload.py re-exports the same WORKLOAD via `from tests._corpus.soak_workload import WORKLOAD as WORKLOAD`. If `scripts/` is not on the Python path when invoked as `python -m scripts.soak.run_soak`, the import-time sys.path adjustment is a one-line `Path(__file__).resolve().parent.parent.parent` insert at the top of workload.py (executed before the import).
    - All four files are stdlib-only or use already-pinned modules (no new deps); ruff-clean.
  </behavior>
  <action>
    Step (A) — Create the parent directory `scripts/soak/`. Verify with `ls scripts 2>&1` first; the directory does NOT yet exist (per `ls /Users/houfu/Projects/zeeker-mcp/` from the planning context — only `src`, `tests`, etc. exist). The Write tool creates parent directories as needed when given a deep path. Step (B) — Create `scripts/soak/__init__.py` as a 0-byte file. Per 08-PATTERNS.md "Empty file. The tests/_corpus/__init__.py is a 0-byte file." Use the Write tool with empty content (the Write tool may add a trailing newline; if so, this is acceptable — `wc -c` ≤ 1). Step (C) — Create `scripts/soak/rss_sampler.py` with the concrete code locked in 08-PATTERNS.md / 08-RESEARCH.md lines 1126-1149. Module starts with `from __future__ import annotations` then stdlib imports `os`, `re`, `pathlib.Path`. Define `rss_kb_from_proc(pid: int) -> int | None` exactly per the locked snippet (reads `/proc/{pid}/status`, regex extracts `VmRSS:\\s+(\\d+)\\s*kB`, returns int or None on OSError/AttributeError). Define `rss_kb_from_self() -> int` per the locked snippet — imports `resource` LOCALLY inside the function (so importing the module does not require POSIX `resource`), branches on `os.uname().sysname == "Darwin"` to normalize macOS bytes → KB, returns `rusage.ru_maxrss` for Linux (already KB). Each function has a docstring naming the OS branch. Per 08-PATTERNS.md "Divergence from analog: Conditional branch on os.uname().sysname == 'Darwin' is unusual in this codebase (no other module checks platform). Document the macOS-vs-Linux unit difference inline; this is the entire reason the helper exists." Step (D) — Create `tests/_corpus/soak_workload.py` per 08-PATTERNS.md § tests/_corpus/soak_workload.py + 08-RESEARCH.md lines 766-868. Module starts with `from __future__ import annotations`, has a docstring naming TEST-05 ownership and the 5-bucket distribution, then defines a typed `WORKLOAD: list[tuple[str, dict[str, Any], float]]` constant. The WORKLOAD entries follow 08-RESEARCH.md "Distribution mirrors observed Phase 6.1 manual-UAT traffic" — five entries, weights summing to 1.0:
       - `("list_databases", {}, 0.35)` — discovery (cheap)
       - `("query_table", {"database": "pdpc", "table": "enforcement_decisions", "limit": 5}, 0.30)` — URL-keyed table query
       - `("search", {"query": "data protection", "limit": 5}, 0.20)` — cross-DB FTS fan-out (expensive)
       - `("fetch", {"database": "pdpc", "table": "enforcement_decisions", "url": <known-stable-pdpc-url>}, 0.10)` — URL fetch; reuse the stable URL identified by Plan 08-04's test_live_fetch
       - `("query_table", {"database": "zeeker-judgements", "table": "judgments_fragments", "filters": [{"column": "source_url", "op": "exact", "value": <known-stable-judgment-url>}], "limit": 50}, 0.05)` — fragment join (heavy 2-step join)
       Add inline comments naming the rationale for each weight (per 08-RESEARCH.md). The weights MUST sum to 1.0 (assertion in `_pick_kind` derived implicitly via cum < 1.0). Imports from `typing` may use `Any` for the args dict value type. Step (E) — Create `scripts/soak/workload.py` as a thin re-exporter. Module starts with `from __future__ import annotations`, then a docstring naming the single-source-of-truth choice ("Canonical workload lives in tests/_corpus/soak_workload.py; scripts/soak/workload.py re-exports for the soak driver"). Per 08-PATTERNS.md "scripts/ is not pytest-rooted, so scripts/soak/workload.py must adjust sys.path", add a 3-line sys.path insertion before the import: `import sys; from pathlib import Path; sys.path.insert(0, str(Path(__file__).resolve().parent.parent.parent))`. Then `from tests._corpus.soak_workload import WORKLOAD as WORKLOAD` — the `as WORKLOAD` makes the re-export explicit (ruff F401 wouldn't flag, but the explicit alias documents intent). Optionally add `__all__ = ["WORKLOAD"]`. NO new deps. Step (F) — Verify all four files import cleanly: `python -c "from tests._corpus.soak_workload import WORKLOAD; assert len(WORKLOAD) == 5 and abs(sum(w for _,_,w in WORKLOAD) - 1.0) < 0.001; print('ok', len(WORKLOAD))"` exits 0. `python -c "from scripts.soak.workload import WORKLOAD as W; print('re-export ok', len(W))"` exits 0. `python -c "from scripts.soak.rss_sampler import rss_kb_from_proc, rss_kb_from_self; print('rss ok')"` exits 0. Step (G) — Ruff: `uv run ruff check scripts tests/_corpus/soak_workload.py` exits 0; `uv run ruff format --check scripts tests/_corpus/soak_workload.py` exits 0.
  </action>
  <acceptance_criteria>
    - scripts/soak/__init__.py exists and is empty (≤ 1 byte for trailing newline): `test -f scripts/soak/__init__.py` exits 0 AND `wc -c scripts/soak/__init__.py | awk '{print $1}'` is `<= 1`.
    - scripts/soak/rss_sampler.py exposes both helpers: `python -c "from scripts.soak.rss_sampler import rss_kb_from_proc, rss_kb_from_self; print('ok')"` prints `ok`.
    - rss_sampler stdlib-only — no third-party imports: `grep -E '^import |^from ' scripts/soak/rss_sampler.py | grep -vE 'os|re|pathlib|resource|__future__'` returns nothing.
    - rss_sampler has the macOS branch: `grep -c 'Darwin' scripts/soak/rss_sampler.py` is `>= 1`.
    - rss_sampler reads VmRSS from /proc/{pid}/status: `grep -c 'VmRSS' scripts/soak/rss_sampler.py` is `>= 1`.
    - tests/_corpus/soak_workload.py exists with 5-entry WORKLOAD whose weights sum to 1.0: `python -c "from tests._corpus.soak_workload import WORKLOAD; assert len(WORKLOAD) == 5 and abs(sum(w for _,_,w in WORKLOAD) - 1.0) < 0.001"` exits 0.
    - WORKLOAD is typed: `grep -E 'WORKLOAD\\s*:\\s*list\\[tuple' tests/_corpus/soak_workload.py | wc -l | xargs` is `>= 1`.
    - scripts/soak/workload.py re-exports from tests/_corpus/soak_workload via the canonical pattern: `grep -c 'from tests._corpus.soak_workload import WORKLOAD' scripts/soak/workload.py` is `>= 1`.
    - sys.path adjustment present in workload.py for non-pytest-rooted execution: `grep -c 'sys.path' scripts/soak/workload.py` is `>= 1`.
    - Re-export functional: `python -c "from scripts.soak.workload import WORKLOAD; assert len(WORKLOAD) == 5; print('re-export ok')"` exits 0.
    - NFR-04 still locked: `uv run pytest tests/test_dependency_footprint.py -x` exits 0 (the soak harness MUST NOT have grown the dep set; from Plan 08-01).
    - Full unit suite remains GREEN: `uv run pytest -x -q` exits 0.
    - Ruff clean: `uv run ruff check scripts tests/_corpus/soak_workload.py` exits 0 AND `uv run ruff format --check scripts tests/_corpus/soak_workload.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && test -f scripts/soak/__init__.py && python -c "from scripts.soak.rss_sampler import rss_kb_from_proc, rss_kb_from_self; from scripts.soak.workload import WORKLOAD; from tests._corpus.soak_workload import WORKLOAD as W2; assert len(WORKLOAD) == 5 and W2 is WORKLOAD and abs(sum(w for _,_,w in WORKLOAD) - 1.0) < 0.001; print('all ok')" && uv run pytest tests/test_dependency_footprint.py -x && uv run ruff check scripts tests/_corpus/soak_workload.py && uv run ruff format --check scripts tests/_corpus/soak_workload.py</automated>
  </verify>
  <done>scripts/soak/ package marker + rss_sampler.py + workload.py exist; tests/_corpus/soak_workload.py exists with the canonical 5-entry weighted WORKLOAD; the re-export shim works; rss_sampler is stdlib-only with the documented macOS / Linux unit branch; NFR-04 dependency-footprint test still passes (no new deps); ruff clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create scripts/soak/run_soak.py — asyncio + httpx driver with argparse CLI</name>
  <files>scripts/soak/run_soak.py</files>
  <read_first>
    - scripts/soak/workload.py + scripts/soak/rss_sampler.py from Task 1 (the imports)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § scripts/soak/run_soak.py (locked driver shape — argparse CLI args, asyncio.Semaphore concurrency primitive, error categorization, CSV outputs)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Soak driver entrypoint (NEW) lines 637-761 (full reference driver body)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Driver requirements (TEST-05 expansion) lines 1102-1112 (per-request latency capture, RSS every 60s, PoolTimeout cascade detection)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Pitfall 1 (driver MUST run in separate process — soak does NOT spawn server in same interpreter) + Pitfall 2 (daily-rollover detection)
    - src/mcp_zeeker/core/middleware/rate_limit.py (the 429 contract — driver categorizes 429 as a separate error class, NOT pool_timeout)
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task rows for `08-05 / TEST-05 (smoke)` (canonical command: `python -m scripts.soak.run_soak --duration 60 --concurrency 5 && python -m scripts.soak.report ...`)
  </read_first>
  <behavior>
    - argparse CLI with required/default args:
      - `--duration` (int seconds, default 86400 — 24h)
      - `--concurrency` (int, default 50 — NFR-02)
      - `--target-url` (default `http://127.0.0.1:8000` — operator runs uvicorn on this in terminal A)
      - `--out-dir` (default `./soak-results`)
      - `--rss-sample-interval` (float seconds, default 60.0)
      - `--server-pid` (int, default = parse from a `--server-pid-file` if absent — for /proc/{pid}/status sampling. If neither is provided, sample the driver's own RSS via rss_kb_from_self() with a clear caveat in the CSV header that the sample is the DRIVER not the server)
    - Driver opens ONE httpx.AsyncClient(base_url=target_url, timeout=httpx.Timeout(connect=5.0, read=30.0, write=5.0, pool=5.0)) for the whole soak
    - Concurrency bounded by `asyncio.Semaphore(--concurrency)`
    - Per request: time.perf_counter() around the await; categorize errors as `pool_timeout` / `request_timeout` / `5xx` / `4xx` / `429` / `ok`; do NOT retry (per 08-RESEARCH.md Open Q5 — failure rate IS a measurement)
    - CSV outputs: `{out_dir}/latency.csv` (header: wall_ts, status, duration_seconds, error_class) and `{out_dir}/rss.csv` (header: wall_ts, rss_kb)
    - Output directory created via `Path(out_dir).mkdir(parents=True, exist_ok=True)`
    - Driver does NOT spawn the server (per 08-PATTERNS.md "the operator runs uvicorn in terminal A; the driver only takes --target-url"); the soak.yml workflow handles the background server start
    - Stdlib + httpx only — zero new deps
    - `if __name__ == "__main__": main()` entrypoint so `python -m scripts.soak.run_soak --duration 60 --concurrency 5` works
  </behavior>
  <action>
    Create `scripts/soak/run_soak.py` per the locked driver skeleton in 08-PATTERNS.md + 08-RESEARCH.md lines 637-761. Module structure: (a) Module docstring naming TEST-05 + NFR-01/02/03 ownership, the operator usage (terminal A: uvicorn with --workers 1; terminal B: this driver), and the no-retry / failure-rate-IS-measurement contract. Reference 08-RESEARCH.md "Soak driver entrypoint" for the full docstring shape. (b) Imports: `from __future__ import annotations`, stdlib `argparse`, `asyncio`, `csv`, `random`, `time`, `pathlib.Path`, plus `httpx`. From scripts.soak: `from scripts.soak.workload import WORKLOAD`, `from scripts.soak.rss_sampler import rss_kb_from_proc, rss_kb_from_self`. (c) Define `_pick_request(rng) -> tuple[str, dict, float]` that draws from WORKLOAD weighted by the third tuple element — implement as `r = rng.random(); cum = 0.0; for tool, args, w in WORKLOAD: cum += w; if r < cum: return (tool, args, w)` with a fall-through return of WORKLOAD[0]. (d) Define an async helper `_one_request(client: httpx.AsyncClient, sem: asyncio.Semaphore, latency_log: list, rng: random.Random) -> None`. Inside the semaphore: pick a request via `_pick_request(rng)`, build the JSON-RPC envelope (`{"jsonrpc": "2.0", "method": "tools/call", "params": {"name": tool, "arguments": args}, "id": rng.randint(1, 1_000_000)}`), record `wall_ts = time.time(); start = time.perf_counter()`, then `try: resp = await client.post("/mcp/", content=json.dumps(envelope).encode("utf-8"), headers={"content-type": "application/json", "accept": "application/json, text/event-stream"})` with the locked error categorization in `except` branches (per 08-PATTERNS.md): `httpx.PoolTimeout` → status=-1, error_class="pool_timeout"; `httpx.TimeoutException` → status=-2, error_class="request_timeout"; bare `Exception` (last-resort) → status=-3, error_class=type(exc).__name__. On success, derive error_class: `if 200 <= resp.status_code < 300: "ok"; elif resp.status_code == 429: "rate_limited"; elif 500 <= resp.status_code < 600: "5xx"; elif 400 <= resp.status_code < 500: "4xx"; else: "unknown"`. Append `(wall_ts, status, time.perf_counter() - start, error_class)` to latency_log. Per 08-RESEARCH.md Open Q5: NO retries. (e) Define an async helper `_rss_sampler_loop(rss_log, server_pid, interval_seconds, deadline_mono)`. While `time.monotonic() < deadline_mono`: sample via `rss_kb_from_proc(server_pid)` if server_pid is set, else fall back to `rss_kb_from_self()` with a logged warning. Append `(time.time(), rss_kb)` to rss_log. `await asyncio.sleep(interval_seconds)`. (f) Define `async def run_soak(args: argparse.Namespace) -> None` per 08-RESEARCH.md driver: `deadline_mono = time.monotonic() + args.duration; latency_log: list = []; rss_log: list = []; rng = random.Random(0)` (deterministic seed for reproducibility). Open `async with httpx.AsyncClient(base_url=args.target_url, timeout=httpx.Timeout(connect=5.0, read=30.0, write=5.0, pool=5.0)) as client: sem = asyncio.Semaphore(args.concurrency); sampler = asyncio.create_task(_rss_sampler_loop(rss_log, args.server_pid, args.rss_sample_interval, deadline_mono)); try: while time.monotonic() < deadline_mono: await asyncio.gather(*[_one_request(client, sem, latency_log, rng) for _ in range(args.concurrency)]); finally: sampler.cancel()`. After the async with exits, write CSVs: `out_dir = Path(args.out_dir); out_dir.mkdir(parents=True, exist_ok=True); with (out_dir / "latency.csv").open("w", newline="") as f: csv.writer(f).writerows([("wall_ts","status","duration_seconds","error_class"), *latency_log])` and similarly for rss.csv with header `("wall_ts","rss_kb")`. (g) Define `def main() -> None`: build argparse parser per the args list above, parse args, `asyncio.run(run_soak(args))`. Support `--server-pid-file` as an alternative to `--server-pid` (read the int from the file at start; the soak.yml workflow writes the uvicorn PID there after backgrounding). (h) `if __name__ == "__main__": main()` entrypoint. (i) Smoke verification: `uv run python -m scripts.soak.run_soak --duration 5 --concurrency 2 --target-url http://127.0.0.1:8000 --out-dir ./tmp-soak-smoke` requires a running uvicorn — the executor SHOULD start one in the background for verification: `uv run uvicorn mcp_zeeker.app:app --host 127.0.0.1 --port 8000 --workers 1 &`, sleep 2, run the 5-second smoke, kill the uvicorn pid, then verify `./tmp-soak-smoke/latency.csv` exists with at least the header line + ≥ 1 data row. If the executor cannot reliably background-and-cleanup uvicorn from the verification script, defer end-to-end smoke to Task 5's CI workflow. NO new deps. Ruff clean.
  </action>
  <acceptance_criteria>
    - File exists: `test -f scripts/soak/run_soak.py` exits 0.
    - Imports include httpx + WORKLOAD + rss helpers: `grep -c 'import httpx' scripts/soak/run_soak.py` is `>= 1` AND `grep -c 'from scripts.soak.workload import WORKLOAD' scripts/soak/run_soak.py` is `>= 1` AND `grep -c 'from scripts.soak.rss_sampler import' scripts/soak/run_soak.py` is `>= 1`.
    - asyncio.Semaphore concurrency primitive present: `grep -c 'asyncio.Semaphore' scripts/soak/run_soak.py` is `>= 1`.
    - argparse CLI args declared: `grep -c -- '--duration\\|--concurrency\\|--target-url\\|--out-dir' scripts/soak/run_soak.py` is `>= 4`.
    - Error categorization present (pool_timeout, request_timeout, 429): `grep -c 'pool_timeout\\|request_timeout\\|rate_limited' scripts/soak/run_soak.py` is `>= 3`.
    - No retry logic: `grep -E 'for.*retry|retry.*for|stamina|tenacity' scripts/soak/run_soak.py | wc -l | xargs` is `0`.
    - CSV outputs configured: `grep -c 'latency.csv\\|rss.csv' scripts/soak/run_soak.py` is `>= 2`.
    - `if __name__ == "__main__"` entrypoint: `grep -c '__main__' scripts/soak/run_soak.py` is `>= 1`.
    - NFR-04 still locked: `uv run pytest tests/test_dependency_footprint.py -x` exits 0.
    - Module imports cleanly: `python -c "from scripts.soak import run_soak; print('import ok')"` exits 0.
    - argparse --help works: `uv run python -m scripts.soak.run_soak --help 2>&1 | grep -c -- '--duration'` is `>= 1`.
    - Full unit suite remains GREEN: `uv run pytest -x -q` exits 0.
    - Ruff clean: `uv run ruff check scripts/soak/run_soak.py` exits 0 AND `uv run ruff format --check scripts/soak/run_soak.py` exits 0.
    - End-to-end smoke (OPTIONAL — defer to Task 5 / CI if executor cannot reliably manage background uvicorn): start uvicorn → run 5s soak → confirm latency.csv has ≥ 2 lines (header + 1 row).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && python -c "from scripts.soak import run_soak; print('import ok')" && uv run python -m scripts.soak.run_soak --help 2>&1 | grep -c -- '--duration' && uv run pytest tests/test_dependency_footprint.py -x && uv run ruff check scripts/soak/run_soak.py && uv run ruff format --check scripts/soak/run_soak.py</automated>
  </verify>
  <done>scripts/soak/run_soak.py exists with the locked driver shape: argparse CLI, single httpx.AsyncClient, asyncio.Semaphore-bounded concurrency, no-retry error categorization, CSV outputs to ./soak-results/. Module is stdlib + httpx only. NFR-04 still locked. Ruff clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create scripts/soak/report.py — CSV reducer + CLI exit-code gate + markdown summary writer</name>
  <files>scripts/soak/report.py</files>
  <read_first>
    - scripts/soak/run_soak.py from Task 2 (the CSV schemas this report consumes — latency.csv columns and rss.csv columns)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § scripts/soak/report.py (locked report shape — CSV reducer + CLI exit-code gate + daily-rollover detection pseudocode + markdown summary writer)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Don't Hand-Roll line 316 (sort + index percentile: `sorted(samples)[int(p * len(samples))]` — NO hdrhistogram)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Pitfall 2 (daily-rollover detection: bucket 429 count per UTC minute; flag drop > 50% within ±60s of midnight)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Validation Architecture lines 1212-1218 (NFR-01 thresholds: --max-p50-ms 300, --max-p95-ms 1500; NFR-03 threshold: --max-rss-mb 256)
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task rows for `08-05 / NFR-01 (p50)`, `(p95)`, `NFR-03 (RSS)` (canonical commands)
  </read_first>
  <behavior>
    - argparse CLI with required/default args:
      - `--results-dir` (default `./soak-results`)
      - `--max-p50-ms` (int milliseconds; default 300 per NFR-01)
      - `--max-p95-ms` (int; default 1500 per NFR-01)
      - `--max-rss-mb` (int; default 256 per NFR-03)
      - `--summary-out` (default `<results-dir>/soak-summary.md`)
    - Reads `<results-dir>/latency.csv` and `<results-dir>/rss.csv`
    - Computes p50/p95/max latency in ms, max RSS in MB, error counts by category
    - Detects daily rollover: bucket 429 count per UTC minute; flag drop > 50% within ±60s of any midnight crossed
    - Writes markdown summary with sections: Latency (p50/p95/max), RSS (max in MB), Errors (counts by category), Daily rollover (true/false + reasoning), Threshold breaches (if any)
    - Exits 0 on pass, 1 on threshold breach (any of: p50 > max_p50_ms, p95 > max_p95_ms, max RSS > max_rss_mb)
    - Stdlib only (argparse, csv, collections, datetime, pathlib, sys); no httpx, no third-party deps
  </behavior>
  <action>
    Create `scripts/soak/report.py` per 08-PATTERNS.md § scripts/soak/report.py + 08-RESEARCH.md lines 218-244. Module structure: (a) Module docstring naming TEST-05 + NFR-01/03 ownership, the input contract (latency.csv + rss.csv produced by run_soak.py), the threshold semantics (NFR-01 = 300ms / 1500ms; NFR-03 = 256 MB), and the exit-code contract (0 = pass, 1 = breach). (b) Imports: `from __future__ import annotations`, stdlib `argparse`, `csv`, `collections`, `datetime` (for UTC midnight detection), `pathlib.Path`, `sys`. NO httpx, NO third-party. (c) Define `_percentile(samples: list[float], p: float) -> float`: sort samples and return `sorted(samples)[int(p * len(samples))]` per 08-RESEARCH.md (no hdrhistogram). Handle empty list → return 0.0. (d) Define `_load_latency(path: Path) -> tuple[list[float], collections.Counter]`: open CSV, skip header, parse each row's `duration_seconds` (column 2) and `error_class` (column 3); return (durations_in_ms_list, error_counter). (e) Define `_load_rss(path: Path) -> list[int]`: open rss.csv, skip header, return list of `rss_kb` values (column 1). (f) Define `_detect_daily_rollover(latency_csv_path: Path) -> tuple[bool, str]`: parse latency.csv rows; bucket 429 events per UTC minute via `collections.Counter` keyed by `datetime.fromtimestamp(wall_ts, tz=UTC).replace(second=0, microsecond=0)`; find UTC midnights crossed by checking minute keys with hour==0 and minute==0; for each midnight, compare 429 count at that minute vs at midnight - 1 minute; flag rollover if any midnight has count < 0.5 × prev_minute_count. Return `(observed: bool, reason: str)`. (g) Define `_write_markdown_summary(out_path, latency_ms, errors, rss_kb, rollover_observed, breaches) -> None`: write a 4-section markdown file:
       ```
       # Soak Report
       ## Latency
       - p50: {p50:.1f} ms
       - p95: {p95:.1f} ms
       - max: {max_lat:.1f} ms
       ## RSS
       - max: {max_rss_mb:.1f} MB
       - samples: {len(rss_kb)}
       ## Errors (by class)
       - ok: {errors['ok']}
       - rate_limited: {errors['rate_limited']}
       - pool_timeout: {errors['pool_timeout']}
       - 5xx: {errors['5xx']}
       ## Daily Rollover
       - observed: {rollover_observed}
       - reason: {reason}
       ## Threshold Breaches
       - {list of breaches or "none"}
       ```
       Plain-string formatting; NO jinja2.
    (h) Define `def main() -> int`: parse argparse, load latency + rss CSVs, compute p50 / p95 / max latency in ms (durations are in seconds in the CSV — multiply by 1000), max RSS in KB → MB (divide by 1024), detect rollover, write markdown summary. Compute breaches list: if `p50_ms > args.max_p50_ms`: append "p50_ms={p50:.1f} > limit {args.max_p50_ms}"; same for p95 and max_rss_mb. Return 0 if breaches empty, 1 otherwise. (i) `if __name__ == "__main__": sys.exit(main())`. (j) Smoke: with a hand-built `./tmp-soak-smoke/latency.csv` containing the header + 5 mock rows (e.g., `1000.0,200,0.1,ok` × 5), run `uv run python -m scripts.soak.report --results-dir ./tmp-soak-smoke --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256`. With duration_seconds=0.1 → 100ms, all p50/p95 are 100ms, well under thresholds → exit 0. With one row at `1.0,200,5.0,ok` → 5000ms, p95 > 1500 → exit 1. The executor SHOULD run both smoke variants to verify the gate logic. NO new deps. Ruff clean.
  </action>
  <acceptance_criteria>
    - File exists: `test -f scripts/soak/report.py` exits 0.
    - argparse CLI flags present: `grep -c -- '--max-p50-ms\\|--max-p95-ms\\|--max-rss-mb\\|--results-dir' scripts/soak/report.py` is `>= 4`.
    - Stdlib-only — no httpx import: `grep -c 'import httpx\\|from httpx' scripts/soak/report.py` is `0`.
    - Sort-then-index percentile (NO hdrhistogram): `grep -E 'sorted\\(.*\\)\\[int\\(' scripts/soak/report.py | wc -l | xargs` is `>= 1` AND `grep -c 'hdrhistogram' scripts/soak/report.py` is `0`.
    - Daily-rollover detection: `grep -c 'daily_rollover\\|midnight' scripts/soak/report.py` is `>= 1`.
    - sys.exit on threshold breach: `grep -c 'sys.exit' scripts/soak/report.py` is `>= 1`.
    - argparse --help works: `uv run python -m scripts.soak.report --help 2>&1 | grep -c -- '--max-p50-ms'` is `>= 1`.
    - Module imports cleanly: `python -c "from scripts.soak import report; print('import ok')"` exits 0.
    - Smoke pass case: build a tmp latency.csv with 5 fast rows, run report with default thresholds → exits 0. (Executor judgment: build the CSV inline via Python or shell, run the report, verify exit code.)
    - Smoke breach case: build a tmp latency.csv with one slow row (5s), run report → exits 1.
    - NFR-04 still locked: `uv run pytest tests/test_dependency_footprint.py -x` exits 0.
    - Full unit suite remains GREEN: `uv run pytest -x -q` exits 0.
    - Ruff clean: `uv run ruff check scripts/soak/report.py` exits 0 AND `uv run ruff format --check scripts/soak/report.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && python -c "from scripts.soak import report; print('import ok')" && uv run python -m scripts.soak.report --help 2>&1 | grep -c -- '--max-p50-ms' && mkdir -p /tmp/soak-smoke-pass && printf 'wall_ts,status,duration_seconds,error_class\n1000.0,200,0.1,ok\n1000.5,200,0.05,ok\n1001.0,200,0.08,ok\n' > /tmp/soak-smoke-pass/latency.csv && printf 'wall_ts,rss_kb\n1000.0,50000\n' > /tmp/soak-smoke-pass/rss.csv && uv run python -m scripts.soak.report --results-dir /tmp/soak-smoke-pass --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256 && mkdir -p /tmp/soak-smoke-fail && printf 'wall_ts,status,duration_seconds,error_class\n1000.0,200,5.0,ok\n' > /tmp/soak-smoke-fail/latency.csv && printf 'wall_ts,rss_kb\n1000.0,50000\n' > /tmp/soak-smoke-fail/rss.csv && (uv run python -m scripts.soak.report --results-dir /tmp/soak-smoke-fail --max-p95-ms 1500 || echo "expected breach exit code observed") && uv run ruff check scripts/soak/report.py && uv run ruff format --check scripts/soak/report.py</automated>
  </verify>
  <done>scripts/soak/report.py exists with argparse CLI, sort-then-index percentile reducer (no hdrhistogram), daily-rollover detection, markdown summary writer, exit-code gate (0 pass / 1 breach). Smoke pass case exits 0; smoke breach case exits non-zero. Stdlib-only. NFR-04 still locked. Ruff clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: End-to-end smoke soak — start uvicorn, run 60s driver, run report, verify gate passes</name>
  <files>(verification only — no source edits)</files>
  <read_first>
    - scripts/soak/run_soak.py + scripts/soak/report.py from Tasks 2 + 3
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task row `08-05 / TEST-05 + NFR-01..03 (smoke)` — the canonical command: `python -m scripts.soak.run_soak --duration 60 --concurrency 5 && python -m scripts.soak.report --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256`
    - src/mcp_zeeker/app.py (the production app — uvicorn binds to it)
    - README.md "Deployment" section (the canonical uvicorn invocation)
  </read_first>
  <behavior>
    - Background uvicorn process started: `uv run uvicorn mcp_zeeker.app:app --host 127.0.0.1 --port 8000 --workers 1 &`
    - Driver runs for 60 seconds at concurrency 5 against the local server
    - Report runs against the produced CSVs with the canonical NFR thresholds
    - All three NFR thresholds pass: p50 < 300ms, p95 < 1.5s, RSS < 256 MB
    - uvicorn cleaned up (kill the PID; do NOT leave a zombie)
    - This is the canonical TEST-05 (smoke) verification per VALIDATION.md
  </behavior>
  <action>
    Step (A) — Verify scripts from Tasks 2 + 3 are importable and executable: `python -c "from scripts.soak import run_soak, report; print('ok')"`. Step (B) — Start uvicorn in the background, capture its PID: `uv run uvicorn mcp_zeeker.app:app --host 127.0.0.1 --port 8000 --workers 1 > /tmp/soak-uvicorn.log 2>&1 &` then capture `UVICORN_PID=$!` and `sleep 3` to let uvicorn bind. Verify it's running: `curl -sS http://127.0.0.1:8000/healthz` returns `{"status":"ok"}` (uses the existing /healthz endpoint per app.py). Step (C) — Run the smoke driver: `uv run python -m scripts.soak.run_soak --duration 60 --concurrency 5 --target-url http://127.0.0.1:8000 --out-dir ./soak-smoke-results --server-pid $UVICORN_PID --rss-sample-interval 5`. The 60-second window with 5 concurrency produces ~50-300 requests depending on upstream variability. Step (D) — Run the report gate: `uv run python -m scripts.soak.report --results-dir ./soak-smoke-results --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256`. Capture the exit code; it MUST be 0 (all NFR thresholds passed). If it exits non-zero, capture the markdown summary at `./soak-smoke-results/soak-summary.md` and surface the breach in SUMMARY — the breach itself is informative (e.g., "smoke ran on a CPU-constrained dev box and p95 was 1700ms; deferred to CI runner where the spec'd hardware passes"). Step (E) — Cleanup: `kill $UVICORN_PID` and `wait $UVICORN_PID 2>/dev/null` (suppress the wait error if already gone). Step (F) — Verify the artifacts produced: `test -f ./soak-smoke-results/latency.csv && test -f ./soak-smoke-results/rss.csv && test -f ./soak-smoke-results/soak-summary.md`. Step (G) — Add `./soak-smoke-results/` to `.gitignore` if not already present (gate against accidental commit of soak artifacts): `grep -q '^soak-smoke-results' .gitignore || echo 'soak-smoke-results/' >> .gitignore`. Step (H) — If the smoke gate exits non-zero on the dev machine due to load conditions outside Phase 8's control (CPU-bound dev box, network jitter, etc.), document the hardware spec and runtime in SUMMARY and forward-point to the CI workflow's first nightly run as the canonical pass. Per 08-VALIDATION.md "Sampling Rate" the smoke gate is required `Before /gsd-verify-work`; if the dev machine cannot pass, the operator MUST run the smoke on the CI runner before phase verification. Step (I) — Do NOT commit the soak-smoke-results/ directory; do NOT add it to git tracking. NO source edits in this task.
  </action>
  <acceptance_criteria>
    - uvicorn starts cleanly and /healthz returns 200: `curl -sS http://127.0.0.1:8000/healthz | grep -c 'ok'` is `>= 1` (executed during Step B).
    - Smoke driver produces the two CSV artifacts: `test -f ./soak-smoke-results/latency.csv && test -f ./soak-smoke-results/rss.csv` exits 0.
    - latency.csv has at least the header + 10 data rows (60s × ~1 req/s minimum even on a slow box): `wc -l ./soak-smoke-results/latency.csv | awk '{print $1}' | xargs test 11 -le` (≥ 11 total).
    - Report writes markdown summary: `test -f ./soak-smoke-results/soak-summary.md` exits 0 AND the file is non-empty (`wc -l` ≥ 5).
    - Per VALIDATION.md row 08-05 / TEST-05 (smoke): `uv run python -m scripts.soak.report --results-dir ./soak-smoke-results --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256` exits 0 (OR document in SUMMARY a deferred-to-CI breach with the captured markdown summary).
    - uvicorn cleaned up (no zombies): `pgrep -f 'uvicorn mcp_zeeker' | wc -l | xargs test 0 -ge` (zero matching processes after cleanup).
    - .gitignore covers smoke artifacts: `grep -c 'soak-smoke-results' .gitignore` is `>= 1` AND `git status --porcelain ./soak-smoke-results/ 2>&1 | wc -l | xargs test 0 -ge` (gitignored, no untracked files surfacing).
    - NFR-04 still locked: `uv run pytest tests/test_dependency_footprint.py -x` exits 0.
    - Full unit suite GREEN: `uv run pytest -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && (uv run uvicorn mcp_zeeker.app:app --host 127.0.0.1 --port 8000 --workers 1 > /tmp/soak-uvicorn.log 2>&1 & echo $! > /tmp/soak-uvicorn.pid; sleep 3; curl -sS http://127.0.0.1:8000/healthz) && uv run python -m scripts.soak.run_soak --duration 60 --concurrency 5 --target-url http://127.0.0.1:8000 --out-dir ./soak-smoke-results --server-pid $(cat /tmp/soak-uvicorn.pid) --rss-sample-interval 5 && uv run python -m scripts.soak.report --results-dir ./soak-smoke-results --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256; SOAK_RC=$?; kill $(cat /tmp/soak-uvicorn.pid) 2>/dev/null; wait $(cat /tmp/soak-uvicorn.pid) 2>/dev/null; rm -f /tmp/soak-uvicorn.pid; grep -q 'soak-smoke-results' .gitignore || echo 'soak-smoke-results/' >> .gitignore; uv run pytest tests/test_dependency_footprint.py -x; exit $SOAK_RC</automated>
  </verify>
  <done>End-to-end smoke soak passes (or breach documented in SUMMARY with hardware-spec context for CI deferral). All three NFR thresholds (NFR-01 p50 < 300ms, NFR-01 p95 < 1.5s, NFR-03 RSS < 256 MB) verified at 5-concurrency baseline. uvicorn cleaned up; soak-smoke-results/ gitignored; NFR-04 still locked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create .github/workflows/soak.yml — workflow_dispatch-only 24h soak with background uvicorn + driver + report + artifact upload</name>
  <files>.github/workflows/soak.yml</files>
  <read_first>
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § .github/workflows/soak.yml (locked sibling-of-live-tests.yml skeleton with deltas: workflow_dispatch ONLY, timeout-minutes 1500, background uvicorn step, artifact upload via actions/upload-artifact@v4 with if: always())
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § CI scheduling lines 1167-1175 (soak.yml row: `workflow_dispatch` (manual), `python -m scripts.soak.run_soak --duration 86400` (full 24h), ~25h)
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md "Manual-Only Verifications" (full 24h soak with daily-rollover crossing UTC midnight)
    - 08-04's `.github/workflows/live-tests.yml` (the sibling — copy the skeleton with the documented deltas)
    - scripts/soak/run_soak.py + scripts/soak/report.py (the modules the workflow invokes)
  </read_first>
  <behavior>
    - File `.github/workflows/soak.yml` exists.
    - Workflow named "24h Soak (manual)" with `on: workflow_dispatch:` ONLY (no cron — too expensive per 08-RESEARCH.md line 1174).
    - Single job `soak` on `ubuntu-latest` with `timeout-minutes: 1500` (25h budget).
    - Steps: checkout (actions/checkout@v4), setup uv (astral-sh/setup-uv@v3 pinned to 0.11.14), `uv sync --frozen`, start uvicorn in background and capture PID into a file, run the 24h driver (`uv run python -m scripts.soak.run_soak --duration 86400 --concurrency 50 --server-pid-file /tmp/uvicorn.pid`), run the report gate (`uv run python -m scripts.soak.report --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256`), upload `./soak-results/` as artifact via actions/upload-artifact@v4 with `if: always()` (so failures still produce diagnostic CSVs).
    - Header comment matches the live-tests.yml / Caddyfile.prod operator-config style.
  </behavior>
  <action>
    Step (A) — Verify .github/workflows/ exists from Plan 08-04: `test -d .github/workflows`. (Plan 08-04 creates it; if for any reason the executor is running 08-05 before 08-04 completes, the directory creation will happen via the Write tool's parent-creation behavior.) Step (B) — Create `.github/workflows/soak.yml` per 08-PATTERNS.md skeleton. Header comment block:
       ```
       # .github/workflows/soak.yml
       # Source: .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md "CI Scheduling" line 1172
       # Purpose: TEST-05 + NFR-01..03 — full 24h soak under 50-concurrent synthetic load.
       # Trigger: workflow_dispatch ONLY (no cron — 25h CI burn per run).
       # Sibling: .github/workflows/live-tests.yml (08-04) — copy skeleton with deltas below.
       ```
    Workflow body per 08-PATTERNS.md "Reuse the live-tests.yml skeleton with these deltas":
       - `name: 24h Soak (manual)`
       - `on: workflow_dispatch:` (NO `schedule` block per 08-RESEARCH.md line 1174 CI-minutes budget)
       - `jobs: soak: runs-on: ubuntu-latest, timeout-minutes: 1500` (25h cap per 08-PATTERNS.md skeleton line "timeout-minutes: 1500 # 25h budget")
       - Steps:
         1. `uses: actions/checkout@v4`
         2. `uses: astral-sh/setup-uv@v3` with `with: {version: "0.11.14"}` (matches CLAUDE.md and 08-04's pin)
         3. `run: uv sync --frozen`
         4. `name: Start uvicorn (single worker per RATE-06)` — `run: uv run uvicorn mcp_zeeker.app:app --host 127.0.0.1 --port 8000 --workers 1 > /tmp/uvicorn.log 2>&1 & echo $! > /tmp/uvicorn.pid; sleep 5` (the `&` backgrounds; the sleep is a coarse poll-until-ready; per 08-PATTERNS.md "poll-until-ready preferred — see live_server pattern" — but for a manual 24h workflow, sleep 5 is acceptable and matches the locked skeleton)
         5. `name: Run 24h soak` — `run: uv run python -m scripts.soak.run_soak --duration 86400 --concurrency 50 --target-url http://127.0.0.1:8000 --server-pid-file /tmp/uvicorn.pid --rss-sample-interval 60 --out-dir ./soak-results`
         6. `name: Report + gate` — `run: uv run python -m scripts.soak.report --results-dir ./soak-results --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256`
         7. `uses: actions/upload-artifact@v4` with `if: always()` (so report-gate failures still upload diagnostic CSVs), `with: {name: soak-results, path: ./soak-results/}`. Per 08-PATTERNS.md "Divergence from sibling: ... Background-uvicorn step (live tests don't need it — they hit the public endpoint)."
    Step (C) — Pin all action versions to majors per the live-tests.yml security-posture pattern (actions/checkout@v4, astral-sh/setup-uv@v3, actions/upload-artifact@v4). Step (D) — NO source / test changes. NO Python file changes. The workflow is YAML-only. Step (E) — Verify after the Write tool call: `test -f .github/workflows/soak.yml`, `grep -c 'workflow_dispatch:' .github/workflows/soak.yml` is `>= 1`, `grep -c 'timeout-minutes: 1500' .github/workflows/soak.yml` is `>= 1`, `grep -c '86400' .github/workflows/soak.yml` is `>= 1`, `grep -c 'concurrency 50' .github/workflows/soak.yml` is `>= 1`, `grep -c 'workers 1' .github/workflows/soak.yml` is `>= 1` (RATE-06 single-worker constraint), `grep -c 'upload-artifact@v4' .github/workflows/soak.yml` is `>= 1`. Step (F) — Confirm there is NO `cron:` line in the workflow (per 08-RESEARCH.md line 1174 / 08-PATTERNS.md "no cron — too expensive"): `grep -c 'cron:' .github/workflows/soak.yml` MUST be `0`.
  </action>
  <acceptance_criteria>
    - File exists: `test -f .github/workflows/soak.yml` exits 0.
    - Header comment with source pointer present: `grep -c 'CI Scheduling' .github/workflows/soak.yml` is `>= 1`.
    - workflow_dispatch trigger present, NO cron: `grep -c 'workflow_dispatch:' .github/workflows/soak.yml` is `>= 1` AND `grep -c '^[[:space:]]*cron:' .github/workflows/soak.yml` is `0` AND `grep -c 'schedule:' .github/workflows/soak.yml` is `0`.
    - timeout-minutes 1500 (25h cap): `grep -E 'timeout-minutes:\\s*1500' .github/workflows/soak.yml | wc -l | xargs` is `>= 1`.
    - 24h soak duration encoded: `grep -c '86400' .github/workflows/soak.yml` is `>= 1`.
    - 50 concurrency encoded (NFR-02): `grep -c 'concurrency 50' .github/workflows/soak.yml` is `>= 1`.
    - --workers 1 single-worker (RATE-06): `grep -c 'workers 1' .github/workflows/soak.yml` is `>= 1`.
    - actions pinned to majors: `grep -c 'actions/checkout@v4' .github/workflows/soak.yml` is `>= 1` AND `grep -c 'astral-sh/setup-uv@v3' .github/workflows/soak.yml` is `>= 1` AND `grep -c 'actions/upload-artifact@v4' .github/workflows/soak.yml` is `>= 1`.
    - uv version pinned to 0.11.14: `grep -c '"0.11.14"' .github/workflows/soak.yml` is `>= 1`.
    - if: always() on artifact upload (so failures still upload CSVs): `grep -E "if:\\s*always\\(\\)" .github/workflows/soak.yml | wc -l | xargs` is `>= 1`.
    - Driver invocation uses run_soak module: `grep -c 'scripts.soak.run_soak' .github/workflows/soak.yml` is `>= 1`.
    - Report invocation uses report module with all three thresholds: `grep -c 'scripts.soak.report' .github/workflows/soak.yml` is `>= 1` AND `grep -c -- '--max-p50-ms 300' .github/workflows/soak.yml` is `>= 1` AND `grep -c -- '--max-p95-ms 1500' .github/workflows/soak.yml` is `>= 1` AND `grep -c -- '--max-rss-mb 256' .github/workflows/soak.yml` is `>= 1`.
    - Two .github workflows now exist (live-tests.yml + soak.yml): `find .github -type f -name '*.yml' | wc -l | xargs` is `>= 2` (assumes 08-04 ran first; if 08-05 runs before 08-04 in parallel, this count is 1 — both are valid).
    - NFR-04 still locked: `uv run pytest tests/test_dependency_footprint.py -x` exits 0.
    - Full unit suite GREEN: `uv run pytest -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && test -f .github/workflows/soak.yml && grep -c 'workflow_dispatch:' .github/workflows/soak.yml && grep -E 'timeout-minutes:\\s*1500' .github/workflows/soak.yml && grep -c '86400' .github/workflows/soak.yml && grep -c 'concurrency 50' .github/workflows/soak.yml && grep -c 'workers 1' .github/workflows/soak.yml && grep -c 'upload-artifact@v4' .github/workflows/soak.yml && grep -c '"0.11.14"' .github/workflows/soak.yml && grep -E "if:\\s*always" .github/workflows/soak.yml && (grep -c 'cron:\\|schedule:' .github/workflows/soak.yml; true) | head -1 | xargs test 0 -ge && uv run pytest tests/test_dependency_footprint.py -x</automated>
  </verify>
  <done>.github/workflows/soak.yml exists with the locked sibling skeleton: workflow_dispatch ONLY (no cron), 1500-min timeout, ubuntu-latest runner, all action versions pinned to majors with uv at 0.11.14, background single-worker uvicorn, 24h driver at 50 concurrency, report gate with NFR thresholds, artifact upload with if: always(). Full 24h soak is now reproducible by an operator via one workflow_dispatch trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Soak driver process ↔ uvicorn server process | Driver and server MUST run in separate Python interpreters (per 08-RESEARCH.md Pitfall 1) — co-located driver inflates p95 by 3× via asyncio scheduling pressure |
| Soak harness ↔ runtime dep set | NFR-04 caps runtime deps at exactly 6 packages; the soak harness MUST NOT add a runtime or dev dep (psutil, locust, syrupy, freezegun, hdrhistogram all explicitly REJECTED in 08-RESEARCH.md) |
| Soak CSV outputs ↔ git tracking | Soak artifacts can be 170 MB+ for a 24h run (per 08-RESEARCH.md "24h soak data volume"); MUST be gitignored to prevent accidental commit |
| Soak CI workflow ↔ CI minutes budget | A 25h soak burns 1500 CI minutes per run; workflow_dispatch ONLY (NO cron) prevents accidental nightly burns (per 08-RESEARCH.md line 1174) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DEP-DRIFT | Tampering / Information Disclosure | scripts/soak/* + tests/_corpus/soak_workload.py NFR-04 invariant | mitigate | All five new modules import only stdlib + already-pinned httpx. Acceptance criteria explicitly assert `pytest tests/test_dependency_footprint.py -x` exits 0 after Tasks 1-3 (and the file is checked again after each subsequent task). Per 08-PATTERNS.md "Stdlib-only Discipline (NFR-04)": every Phase 8 NEW file imports only stdlib + pinned modules; the dep-footprint test catches violations. |
| T-DOS-DRIVER-INFLATION | Denial of Service / measurement distortion | scripts/soak/run_soak.py running in same interpreter as uvicorn server | mitigate | Per 08-RESEARCH.md Pitfall 1: driver and server in separate processes (terminal A: uvicorn; terminal B: driver). Task 4's smoke uses `uvicorn ... &` background + driver in separate `uv run`. The soak.yml workflow uses the same pattern. Driver records timestamps with time.perf_counter() AROUND the await, NOT inside httpx hooks. |
| T-SOAK-IP-LEAK | Information Disclosure | Soak driver logs in CI artifacts | mitigate | Driver writes only timestamps + status codes + latency + error class to latency.csv; NEVER request bodies, never tokens, never full IPs. Driver does NOT pass auth headers (anonymous tier per v1 scope). The soak.yml artifact upload contains only the gitignored ./soak-results/ directory which holds these scrubbed CSVs. Per 08-RESEARCH.md "Known Threat Patterns for Test/Soak Code" — same disposition. |
| T-CI-COST-OVERRUN | Denial of Service (CI minutes) | .github/workflows/soak.yml accidental cron trigger | mitigate | soak.yml uses `workflow_dispatch:` ONLY — NO `schedule:` block. The acceptance criteria assert `grep -c 'cron:' soak.yml` is 0 AND `grep -c 'schedule:' soak.yml` is 0. The 25h timeout-minutes: 1500 cap prevents runaway runs. |
| T-PSUTIL-CREEP | Tampering | scripts/soak/rss_sampler.py temptation to add psutil | accept | The rss_sampler.py file uses pure-stdlib (resource.getrusage + /proc/{pid}/status) per 08-PATTERNS.md locked code. Adding psutil would fail tests/test_dependency_footprint.py and is explicitly REJECTED in 08-RESEARCH.md "What NOT to Use" + "Don't Hand-Roll" tables. Documented inline in the rss_sampler module docstring. |
| T-WORKLOAD-DUPLICATION | Tampering | tests/_corpus/soak_workload.py vs scripts/soak/workload.py drift | mitigate | Single-source-of-truth: canonical WORKLOAD lives in tests/_corpus/soak_workload.py; scripts/soak/workload.py re-exports via `from tests._corpus.soak_workload import WORKLOAD as WORKLOAD`. Acceptance criteria explicitly assert `python -c "from scripts.soak.workload import WORKLOAD; from tests._corpus.soak_workload import WORKLOAD as W2; assert WORKLOAD is W2"` — the two names are bound to the SAME list object. |
| T-SOAK-ARTIFACT-COMMIT | Information Disclosure (accidental commit of large CSVs) | ./soak-results/ + ./soak-smoke-results/ untracked files | mitigate | Task 4 adds these directories to .gitignore. Acceptance criteria assert `grep -c 'soak-smoke-results' .gitignore` is `>= 1`. The 24h CI run uploads via actions/upload-artifact (artifact retention is per-CI-config, not git tracking). |
</threat_model>

<verification>
- VALIDATION.md per-task table rows covered by this plan (all must flip to ✅ green):
  - `08-05 / TEST-05 + NFR-01..03 (smoke)` — `python -m scripts.soak.run_soak --duration 60 --concurrency 5 && python -m scripts.soak.report --max-p50-ms 300 --max-p95-ms 1500 --max-rss-mb 256` (executed in Task 4)
  - `08-05 / NFR-01 (p50)` — covered by smoke + 24h workflow gate
  - `08-05 / NFR-01 (p95)` — covered by smoke + 24h workflow gate
  - `08-05 / NFR-02 (concurrency)` — implicit via driver `--concurrency 50` in soak.yml
  - `08-05 / NFR-03 (RSS)` — covered by smoke + 24h workflow gate
  - `08-05 / TEST-05 (24h, daily rollover)` — manual / pre-release workflow_dispatch (deferred per 08-VALIDATION.md "Manual-Only Verifications")
- NFR-04 still locked after all tasks: `uv run pytest tests/test_dependency_footprint.py -x` exits 0 — no new runtime or dev deps introduced.
- Full unit suite remains GREEN: `uv run pytest -x -q` exits 0.
- Ruff clean: `uv run ruff check src tests scripts` exits 0 AND `uv run ruff format --check src tests scripts` exits 0.
- Single-source-of-truth invariant for WORKLOAD: `python -c "from scripts.soak.workload import WORKLOAD; from tests._corpus.soak_workload import WORKLOAD as W2; assert WORKLOAD is W2; print('shared')"` prints `shared`.
- Stdlib + httpx only across all new files: `grep -rE '^import |^from ' scripts/ | grep -vE 'argparse|asyncio|csv|collections|datetime|json|os|pathlib|random|re|resource|sys|time|typing|httpx|tests\\._corpus|scripts\\.soak|__future__'` returns nothing.
- File-touch discipline: `git diff --name-only` shows exactly seven NEW paths from this plan (scripts/soak/__init__.py, scripts/soak/run_soak.py, scripts/soak/workload.py, scripts/soak/rss_sampler.py, scripts/soak/report.py, tests/_corpus/soak_workload.py, .github/workflows/soak.yml) PLUS the .gitignore edit from Task 4. tests/conftest.py is NOT touched. src/ is NOT touched.
- Two .github workflows exist after this plan + 08-04 merge: `find .github -type f -name '*.yml' | wc -l | xargs` is `2`.
</verification>

<success_criteria>
- TEST-05 closes (smoke level): the smoke soak (60s, 5 concurrent) runs end-to-end and the report gate passes the NFR-01/03 thresholds OR a documented hardware-spec deferral to the CI runner is recorded in SUMMARY.
- TEST-05 (full 24h) is one workflow_dispatch click away — `.github/workflows/soak.yml` is wired with the locked skeleton.
- NFR-01 (p50 < 300ms, p95 < 1.5s), NFR-02 (50 concurrent), NFR-03 (RSS < 256 MB) are gated by the report tool — any future regression that pushes a metric over the threshold fails the soak.yml workflow's report-gate step.
- NFR-04 is preserved: zero new runtime deps, zero new dev deps. The soak harness is pure-stdlib + already-pinned httpx. `tests/test_dependency_footprint.py` (Plan 08-01) continues to pass.
- Single-source-of-truth WORKLOAD: tests/_corpus/soak_workload.py is canonical; scripts/soak/workload.py re-exports the SAME object — no drift possible.
- Driver and server run in separate processes (per 08-RESEARCH.md Pitfall 1); the smoke verification in Task 4 uses background uvicorn + foreground driver and the soak.yml workflow follows the same pattern.
- soak.yml has NO cron trigger — workflow_dispatch ONLY — preventing accidental 25h CI burns.
- soak artifacts (./soak-results/, ./soak-smoke-results/) are gitignored.
- tests/conftest.py untouched (single-plan-touch enforced); src/ untouched (Phase 8 is a coverage phase, no production code changes in this plan).
- Full pytest suite GREEN; ruff clean across `src`, `tests`, and `scripts`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-tests-24h-soak/08-05-SUMMARY.md` documenting:
- The five new module paths under scripts/soak/ + tests/_corpus/soak_workload.py + .github/workflows/soak.yml — confirm each exists and its byte count.
- The single-source-of-truth choice for WORKLOAD (canonical: tests/_corpus/soak_workload.py; re-export: scripts/soak/workload.py) and the rationale.
- The smoke soak run output: number of requests issued, p50/p95/max latency, max RSS, error counts by category. If the smoke gate exited 0, note that the NFR thresholds passed at smoke baseline. If it exited non-zero, paste the markdown summary verbatim from `./soak-smoke-results/soak-summary.md` and document the hardware spec + load conditions; forward-point to the CI runner's first workflow_dispatch run as the canonical pass.
- Confirmation that tests/conftest.py and src/ were NOT modified.
- Confirmation that NFR-04 dependency-footprint test still passes (`pytest tests/test_dependency_footprint.py -x` exits 0).
- Confirmation that .gitignore covers `soak-results/` and `soak-smoke-results/`.
- Confirmation that soak.yml has NO cron trigger.
- A 1-line forward pointer: "Soak harness in place; full 24h run is one workflow_dispatch trigger away. 08-06 (NFR-05 README delta + traceability sweep) follows in Wave 4 as the final Phase 8 plan."
</output>
</content>
</invoke>