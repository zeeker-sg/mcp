---
phase: 08
plan: 03
plan_id: "08-03"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - tests/test_envelope_snapshot.py
  - tests/_corpus/hostile_inputs.py
  - tests/test_hostile_inputs_consolidated.py
  - tests/tools/test_retrieval_fragment_join.py
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-06
must_haves:
  truths:
    - "tests/test_envelope_snapshot.py asserts the explicit row-key partition contract for every successful envelope across every registered tool: set(row.keys()) ∩ HEAVY_COLUMNS == ∅ AND set(row['retrieved_content'].keys()) ⊆ HEAVY_COLUMNS — with diff-readable failure messages naming the leaked key set per tool (closing TEST-03)."
    - "tests/_corpus/hostile_inputs.py CANARY_STRINGS grows from 5 entries (Phase 6) to 9 entries by APPENDING 4 new canaries (BOM, RTL override, malformed UTF-8 surrogate pair, FTS5 operator string) — order preserved so existing pytest test IDs are stable (closing TEST-06 corpus-side)."
    - "tests/test_hostile_inputs_consolidated.py runs the 9-canary × 3-tool matrix (27 collected cases via existing parametrize fan-out) without code-shape changes to the matrix decorator — pytest collects 27 cases and all pass with zero leaks across error / log / response surfaces (closing TEST-06 path-fan-out)."
    - "tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic carries an explicit TEST-04 traceability marker in its docstring header — appended one paragraph noting Phase 8 ownership of TEST-04 (regression test originated in Phase 5 D5-06; no code change)."
  artifacts:
    - path: tests/test_envelope_snapshot.py
      provides: "TEST-03 row-key partition contract: per-row diff-readable assertion that names the leaked HEAVY_COLUMNS subset on failure"
      contains: "TEST-03 leak"
    - path: tests/_corpus/hostile_inputs.py
      provides: "Extended CANARY_STRINGS with 4 new entries (BOM, RTL, malformed UTF-8 surrogate, FTS5 op string); existing 5 entries unchanged at indices 0-4"
      contains: "MATCH 'data' AND NEAR("
    - path: tests/test_hostile_inputs_consolidated.py
      provides: "9 × 3 = 27 parametrized cases via auto-expansion from CANARY_STRINGS — no decorator change required; documented per-canary exception sites if any new canary needs a carry-forward exception (analogous to the lone-surrogate exception)"
      contains: "9 × 3"
    - path: tests/tools/test_retrieval_fragment_join.py
      provides: "TEST-04 traceability marker — docstring-only addition naming Phase 8 ownership"
      contains: "TEST-04 owner"
  key_links:
    - from: tests/test_envelope_snapshot.py
      to: src/mcp_zeeker/config.py::HEAVY_COLUMNS
      via: "set(row.keys()) ∩ config.HEAVY_COLUMNS partition assertion per-row in the per-tool fan-out"
      pattern: "config\\.HEAVY_COLUMNS"
    - from: tests/test_hostile_inputs_consolidated.py
      to: tests/_corpus/hostile_inputs.py CANARY_STRINGS
      via: "@pytest.mark.parametrize('canary', CANARY_STRINGS) — pytest auto-expands to 9 cases when the corpus grows"
      pattern: "CANARY_STRINGS"
    - from: tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic
      to: src/mcp_zeeker/core/fragment_join.py keyset cursor
      via: "Walks 15 synthetic 100-row pages end-to-end via Datasette _next token — confirms TEST-04 1000-row cap defeated"
      pattern: "test_1500_fragment_walk_synthetic"
---

<objective>
Land Phase 8 Wave 2: the snapshot + hostile-input + 1500-fragment traceability sweep that closes TEST-03 (snapshot per tool), TEST-04 (1500-fragment regression — docstring marker only), and TEST-06 (hostile-input corpus). Three tightly coupled extensions to existing test files plus one corpus extension; no new test files.

Purpose: TEST-03/04/06 are the "everything emitted by the connector is safe to feed back into an LLM" gate. TEST-03 enforces the heavy-column partition contract per tool. TEST-06 fans the hostile-input corpus across error / log / response surfaces. TEST-04 is a documentation-only traceability fix — the regression test exists from Phase 5 (FRAG-04); Phase 8 marks it as TEST-04's owner without touching test code. Together these extensions complete the data-safety side of TEST-01..06.

Output: Four MODIFIED files — `tests/test_envelope_snapshot.py` (per-row partition assertion with diff-readable error message), `tests/_corpus/hostile_inputs.py` (4 new canaries appended to CANARY_STRINGS), `tests/test_hostile_inputs_consolidated.py` (matrix grows from 5×3 to 9×3 via auto-expansion; document any new per-canary exceptions inline), `tests/tools/test_retrieval_fragment_join.py` (one paragraph appended to test docstring — no code change).
</objective>

## Phase Goal

**As an** auditor verifying that mcp.zeeker.sg's contract "every successful response is citation-ready, scope-bounded, and safe to feed back into an LLM" holds for every tool under hostile input, **I want** every successful tool envelope to obey the explicit row-key partition (HEAVY_COLUMNS only ever appear under `retrieved_content`), every canary in the 9-entry corpus to never echo into any of three surfaces (error / log / response) across all three filter-value-echo-emitting tools, and the 1500-fragment regression test to be explicitly tied to TEST-04 in the traceability table, **so that** TEST-03, TEST-04, and TEST-06 close with concrete evidence and any future regression in the data-safety contract surfaces fails CI on the next PR.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-full-tests-24h-soak/08-RESEARCH.md
@.planning/phases/08-full-tests-24h-soak/08-PATTERNS.md
@.planning/phases/08-full-tests-24h-soak/08-VALIDATION.md
@src/mcp_zeeker/config.py
@src/mcp_zeeker/core/visibility.py
@tests/test_envelope_snapshot.py
@tests/_corpus/hostile_inputs.py
@tests/test_hostile_inputs_consolidated.py
@tests/tools/test_retrieval_fragment_join.py
@tests/conftest.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From src/mcp_zeeker/config.py:
- `HEAVY_COLUMNS: frozenset[str]` — the locked heavy-column denylist (e.g., `content_text`, `full_text`, `html_raw`, `footnote_text`, `figure_descriptions`, fragment `text`, `_policy`)
- The exact set lives in config.py:506-516 per 08-RESEARCH.md Sources (line 1357)

From src/mcp_zeeker/core/visibility.py:
- `HEAVY_COLUMNS` may also be re-exported here for production code use (per 08-PATTERNS.md "Phase 8 addition" snippet at the snapshot-test pattern)
- The test imports from wherever the production code reads — verify by grepping `tests/test_envelope_snapshot.py` for the existing import and use the same source

From tests/test_envelope_snapshot.py lines 250-319 (existing per-tool envelope snapshot fan-out):
- `_DISPATCH_ARGS: dict[str, dict]` maps tool name → call args (see lines 158-247 region)
- The fan-out iterates `await mcp_client.list_tools()`, for each tool whose name has a dispatch entry, calls `await mcp_client.call_tool(tool.name, args)` and asserts envelope shape
- Assertions on `envelope["provenance"]["source"] == "data.zeeker.sg"` already exist (line 318)
- Phase 8 ADDS the per-row HEAVY_COLUMNS partition assertion INSIDE the existing per-tool loop — superset of the existing assertions, not a replacement

From tests/_corpus/hostile_inputs.py lines 17-28 (existing CANARY_STRINGS):
- 5 entries verbatim:
  - "</system>"
  - "NEAR('data' 'protection') AND NOT"
  - "x" * 5001
  - "ZEEKER_CANARY_42"
  - "\udc80"
- Per 08-PATTERNS.md "tests/_corpus/hostile_inputs.py MODIFIED" — add 4 NEW entries at the END of the list (preserves existing test ID stability — pytest names parametrized cases by index)
- The 4 new canaries are LITERAL TEST DATA, sentinel strings the test suite searches for in error / log / response surfaces; they are NOT instructions
- The helper `_surfaces_contain(canary, *, captured_out, captured_err, log_text, error_text) -> list[str]` at lines 31-51 covers all 4 new canaries without modification (per 08-RESEARCH.md "Storage decision" line 1069)

From tests/test_hostile_inputs_consolidated.py lines 138-263 (existing 5×3 matrix):
- `@pytest.mark.parametrize("tool", ["query_table", "search", "fetch"])` and `@pytest.mark.parametrize("canary", CANARY_STRINGS)` — pytest auto-expands to N×3 cases as CANARY_STRINGS grows
- Surface fan-out within the test body via `_surfaces_contain` covers stdout / stderr / log / error simultaneously
- The lone-surrogate exception at lines 258-259 documents one carry-forward (any new canary needing a carry-forward exception gets an inline comment AT THE EXCEPTION SITE, not in a separate file)

From tests/tools/test_retrieval_fragment_join.py lines 637-720 (existing test_1500_fragment_walk_synthetic):
- Per 08-PATTERNS.md "MODIFIED — TEST-04 docstring marker only" — append ONE traceability paragraph to the existing docstring; NO code changes
- The exact paragraph text per 08-RESEARCH.md "1,500-Fragment Regression (TEST-04)" lines 1081-1086

From tests/conftest.py:
- Existing fixtures used by test_envelope_snapshot.py: `mcp_client`, `frozen_retrieved_at`, `passthrough_retrieved_at_middleware`, `bound_metadata_cache`, `bound_datasette_client`, `httpx_mock`
- Phase 8 MUST NOT add fixtures (08-RESEARCH.md "Wave 0 Requirements" line 88)

From .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md "Diff-Readable Assertion Messages" pattern:
- Failure messages use `f"... leaked={set!r}"` or `f"... missing={set!r} extra={set!r}"` — never bare `assert`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/test_envelope_snapshot.py — TEST-03 explicit row-key partition assertion per tool</name>
  <files>tests/test_envelope_snapshot.py</files>
  <read_first>
    - tests/test_envelope_snapshot.py lines 250-319 (the existing per-tool envelope fan-out — `test_every_registered_tool_returns_envelope_with_correct_provenance` at the end of the file). Phase 8 ADDS assertions INSIDE the existing per-tool loop; do NOT delete or rewrite the existing assertions.
    - tests/test_envelope_snapshot.py lines 1-50 (existing imports — find where `config.HEAVY_COLUMNS` is currently imported, or import it the same way other Phase 6 tests in this file do)
    - src/mcp_zeeker/config.py lines 506-516 (the canonical HEAVY_COLUMNS frozenset — the source-of-truth the test asserts against)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_envelope_snapshot.py MODIFIED (the exact additional assertion shape — locked verbatim, including diff-readable error message format)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Snapshot Tests (TEST-03) lines 1017-1027 (rationale: hand-rolled wins over syrupy; explicit error messages required)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Pitfall 5 lines 381-391 (why upstream stubs MUST include heavy columns to exercise the strip path — verify this is true in the existing fan-out's `_DISPATCH_ARGS` stubs; if any tool's stub omits all heavy columns, document the gap in SUMMARY rather than fix here — per 08-PATTERNS.md the action is "extend" not "rewrite")
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task rows for `08-03 / TEST-03 (snapshot keys)` and `(retrieved_content)` (canonical commands)
  </read_first>
  <behavior>
    - Inside the existing per-tool loop in `test_every_registered_tool_returns_envelope_with_correct_provenance`, add per-row partition assertions that run for EVERY row in `envelope["data"]` (when data is a list) — for each row, assert top-level keys are disjoint from HEAVY_COLUMNS, and if `retrieved_content` is present, assert its keys are a subset of HEAVY_COLUMNS.
    - Failure message names which tool, which key set leaked, with diff-readable formatting per 08-PATTERNS.md "Diff-Readable Assertion Messages" pattern.
    - Existing assertions (provenance.source, envelope shape) stay verbatim; new assertions are a SUPERSET.
    - All 6 production tools that have entries in `_DISPATCH_ARGS` exercise the new assertions when called.
  </behavior>
  <action>
    Edit `tests/test_envelope_snapshot.py`. Locate the existing `test_every_registered_tool_returns_envelope_with_correct_provenance` function (per 08-PATTERNS.md the per-tool fan-out lives at lines 250-319; verify the exact line range via the file as it exists now). Inside the existing `for tool in tools:` loop, AFTER the existing `assert envelope["provenance"]["source"] == "data.zeeker.sg"` line, add a new block that iterates over `envelope.get("data", [])` (handling both list-of-rows and single-dict envelopes — for `describe_table` the envelope.data is a single dict with a `columns` key; the partition assertion still applies, just over the dict's top-level keys). Per 08-PATTERNS.md, for each row when data is a list of dicts: (a) compute `leaked_top = set(row.keys()) & config.HEAVY_COLUMNS - {"retrieved_content"}` (the `- {"retrieved_content"}` exclusion is critical — `retrieved_content` is the legitimate carrier and is itself NOT in HEAVY_COLUMNS, but it sometimes appears as a row key); (b) assert `not leaked_top` with diff-readable message: `f"TEST-03 leak: tool={tool.name!r} top-level row keys intersect HEAVY_COLUMNS: {leaked_top!r}"`; (c) if `"retrieved_content" in row`, compute `rc_extra = set(row["retrieved_content"].keys()) - config.HEAVY_COLUMNS` and assert `not rc_extra` with diff-readable message: `f"TEST-03 leak: tool={tool.name!r} retrieved_content carries non-HEAVY keys: {rc_extra!r}"`. Per 08-RESEARCH.md "Snapshot Tests (TEST-03)" — hand-rolled, NOT syrupy. The block adds ~10 lines inside the existing loop. Do NOT modify the loop's iteration source, fixture wiring, or any other test in the file. Per 08-VALIDATION.md per-task table: both `pytest tests/test_envelope_snapshot.py -x` AND `pytest tests/test_envelope_snapshot.py -k retrieved_content -x` MUST exit 0 after this task. NO new fixtures in tests/conftest.py per 08-RESEARCH.md "Wave 0 Requirements" line 88. The file MUST be ruff-clean. NOTE on dict-envelopes: `describe_table` returns `envelope.data == {"name": ..., "columns": [...], ...}` — a single dict, not a list. Handle this by checking `isinstance(envelope.get("data"), list)` and skipping per-row iteration when it's a dict (the column-list itself contains `{"name": ..., "type": ...}` dicts whose keys are NEVER heavy columns by construction; the partition contract applies only to data-row envelopes). Per 08-RESEARCH.md Pitfall 5: if any tool's stub in `_DISPATCH_ARGS` produces zero rows that contain any heavy column, the assertion passes trivially — note this in SUMMARY for follow-up; do NOT modify stubs in this plan.
  </action>
  <acceptance_criteria>
    - The existing per-tool fan-out test still exists and is selectable: `grep -c 'def test_every_registered_tool_returns_envelope_with_correct_provenance' tests/test_envelope_snapshot.py` is `>= 1`.
    - New TEST-03 leak assertion present with named-key error message: `grep -c 'TEST-03 leak' tests/test_envelope_snapshot.py` is `>= 2` (one for top-level, one for retrieved_content).
    - Uses HEAVY_COLUMNS partition assertion: `grep -E 'set\\(.*\\)\\s*[&\\-]\\s*config\\.HEAVY_COLUMNS|config\\.HEAVY_COLUMNS\\s*[&\\-]' tests/test_envelope_snapshot.py | wc -l | xargs` is `>= 2`.
    - Existing provenance.source assertion preserved: `grep -c 'data.zeeker.sg' tests/test_envelope_snapshot.py` is `>= 1`.
    - Per VALIDATION.md row 08-03 / TEST-03 (snapshot keys): `uv run pytest tests/test_envelope_snapshot.py -x` exits 0.
    - Per VALIDATION.md row 08-03 / TEST-03 (retrieved_content): `uv run pytest tests/test_envelope_snapshot.py -k retrieved_content -x` exits 0.
    - Whole file passes: `uv run pytest tests/test_envelope_snapshot.py -x` exits 0.
    - Ruff clean: `uv run ruff check tests/test_envelope_snapshot.py` exits 0 AND `uv run ruff format --check tests/test_envelope_snapshot.py` exits 0.
    - No conftest changes: `git diff --name-only tests/conftest.py` is empty.
    - No syrupy import added: `grep -c 'syrupy' tests/test_envelope_snapshot.py` is `0`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/test_envelope_snapshot.py -x && uv run pytest tests/test_envelope_snapshot.py -k retrieved_content -x && uv run ruff check tests/test_envelope_snapshot.py && uv run ruff format --check tests/test_envelope_snapshot.py</automated>
  </verify>
  <done>tests/test_envelope_snapshot.py per-tool fan-out now asserts the explicit row-key partition contract (set ∩ HEAVY_COLUMNS == ∅; retrieved_content keys ⊆ HEAVY_COLUMNS) with diff-readable error messages naming the leaked subset per tool. Existing assertions preserved as a subset of the new assertions. Ruff clean. TEST-03 traceability is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/_corpus/hostile_inputs.py — append 4 new canaries (BOM, RTL, malformed UTF-8 surrogate, FTS5 op string)</name>
  <files>tests/_corpus/hostile_inputs.py</files>
  <read_first>
    - tests/_corpus/hostile_inputs.py lines 17-28 (existing CANARY_STRINGS list — the 5 Phase 6 entries; their order MUST be preserved as indices 0-4 so existing pytest test IDs from tests/test_hostile_inputs_consolidated.py stay stable)
    - tests/_corpus/hostile_inputs.py lines 31-51 (existing _surfaces_contain helper — verifies via repr-based detection that handles all 4 new canaries without modification, per 08-RESEARCH.md "Storage decision" line 1069)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/_corpus/hostile_inputs.py MODIFIED (the exact 4 new entries — locked verbatim per the spawn message: BOM, RTL override, malformed UTF-8 surrogate pair, FTS5 op string)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Phase 8 additions (4 new canaries) lines 1046-1054 (the 4-canary table; SPAWN MESSAGE OVERRIDES with these specific 4 — BOM, RTL, malformed UTF-8 surrogate pair, FTS5 op string. NOTE: the RESEARCH.md table proposed BOM, RTL, ANSI escape, JSON injection — the SPAWN MESSAGE specifies a different fourth set; PATTERNS.md aligns with the SPAWN MESSAGE which is authoritative)
    - The SPAWN MESSAGE for Phase 8 (recommended plan partitioning, 08-03 wave: "add 4 canaries: BOM, RTL override, malformed UTF-8 surrogate, FTS5 op string")
  </read_first>
  <behavior>
    - CANARY_STRINGS grows from 5 to 9 entries by APPENDING (not inserting in middle, not reordering) the 4 new entries at the END.
    - The 4 new entries (in append order):
      - BOM: U+FEFF byte-order mark (the literal character; comment notes it's an invisible char that often round-trips through naive string handling)
      - RTL override: U+202E right-to-left override
      - Malformed UTF-8 surrogate pair: a string like `"\udcc0\udc80"` (paired lone surrogates that are not valid UTF-8 if encoded)
      - FTS5 op string: `"MATCH 'data' AND NEAR("` (a fragment of upstream FTS5 syntax that should never echo)
    - Each new entry has an inline comment naming purpose, mirroring the existing 5 entries' comment style verbatim.
    - Existing 5 entries unchanged at indices 0-4 (preserves test ID stability per 08-PATTERNS.md "Order matters for test ID stability — add at the END to preserve existing test IDs").
    - Existing helpers (`_surfaces_contain`, `_canary_sentinel` if present) and module docstring remain unchanged.
  </behavior>
  <action>
    Edit `tests/_corpus/hostile_inputs.py`. Locate the existing CANARY_STRINGS list at lines 17-28 (verify exact line range). At the END of the list — AFTER the existing `"\udc80",  # lone surrogate — UTF-8 boundary handling` line and BEFORE the closing `]` — APPEND four new list entries, one per line, each with its inline comment in the same `# ...` style as the existing entries. The four entries verbatim per 08-PATTERNS.md and the spawn message: (a) `"﻿",  # BOM (byte-order mark) — invisible char that often round-trips through naive string handling` — use the escape `﻿` (NOT a literal BOM character in the source file, which can confuse editors / git diffs). (b) `"‮",  # RTL override — flips display direction; potential confusion in agent display surface` — use the escape `‮`. (c) `"\udcc0\udc80",  # malformed UTF-8 surrogate pair — UTF-8 boundary canary` — use surrogate escapes (matches the existing `\udc80` style). (d) `"MATCH 'data' AND NEAR(",  # FTS5 operator string — fragment of upstream syntax that must never echo back` — plain string. Do NOT modify the existing 5 entries; do NOT reorder; do NOT touch the module docstring or any helper function below the list. Per 08-PATTERNS.md "Order matters for test ID stability (pytest names cases by parameter index); add at the END to preserve existing test IDs." — verify after the edit that the 5 existing entries are still at indices 0-4 by inspecting the file (e.g., `python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; print(CANARY_STRINGS[:5])"` — but do not commit a sub-shell test, just visually verify). The file MUST be ruff-clean. NO new fixtures in tests/conftest.py. NO changes to `_surfaces_contain` — per 08-RESEARCH.md line 1069 the existing helper handles all 4 new canaries via repr-based detection without modification.
  </action>
  <acceptance_criteria>
    - CANARY_STRINGS has exactly 9 entries: `python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; assert len(CANARY_STRINGS) == 9, len(CANARY_STRINGS)"` exits 0.
    - First 5 entries unchanged (test ID stability): `python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; assert CANARY_STRINGS[0] == '</system>' and CANARY_STRINGS[3] == 'ZEEKER_CANARY_42' and CANARY_STRINGS[4] == '\\udc80'"` exits 0.
    - BOM canary present: `python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; assert '\\ufeff' in CANARY_STRINGS"` exits 0.
    - RTL canary present: `python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; assert '\\u202e' in CANARY_STRINGS"` exits 0.
    - FTS5 op canary present: `python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; assert \"MATCH 'data' AND NEAR(\" in CANARY_STRINGS"` exits 0.
    - Malformed UTF-8 surrogate canary present: `python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; assert '\\udcc0\\udc80' in CANARY_STRINGS"` exits 0.
    - Helper unchanged: `grep -c 'def _surfaces_contain' tests/_corpus/hostile_inputs.py` is `>= 1` AND no other diff to that function (compare via `git diff -U0 tests/_corpus/hostile_inputs.py` — only additions in CANARY_STRINGS region).
    - Module imports unchanged: `git diff -U0 tests/_corpus/hostile_inputs.py` shows added lines only inside the CANARY_STRINGS block.
    - Ruff clean: `uv run ruff check tests/_corpus/hostile_inputs.py` exits 0 AND `uv run ruff format --check tests/_corpus/hostile_inputs.py` exits 0.
    - No conftest changes: `git diff --name-only tests/conftest.py` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; assert len(CANARY_STRINGS) == 9 and CANARY_STRINGS[0] == '</system>' and '\\ufeff' in CANARY_STRINGS and '\\u202e' in CANARY_STRINGS and 'MATCH' in ''.join(s for s in CANARY_STRINGS if isinstance(s, str)); print('ok 9 canaries')" && uv run ruff check tests/_corpus/hostile_inputs.py && uv run ruff format --check tests/_corpus/hostile_inputs.py</automated>
  </verify>
  <done>CANARY_STRINGS contains exactly 9 entries with the 5 existing Phase 6 canaries unchanged at indices 0-4 and the 4 new canaries appended at indices 5-8. Helpers untouched. Ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run tests/test_hostile_inputs_consolidated.py at 9×3 = 27 cases — document any new per-canary exception inline</name>
  <files>tests/test_hostile_inputs_consolidated.py</files>
  <read_first>
    - tests/test_hostile_inputs_consolidated.py lines 138-263 (the parametrized test body — `@pytest.mark.parametrize("canary", CANARY_STRINGS)` AUTO-EXPANDS as CANARY_STRINGS grows; no decorator change required)
    - tests/test_hostile_inputs_consolidated.py lines 233-262 (the surface-fan-out + per-canary documented-exception block — the lone-surrogate exception at lines 258-259 is the existing analog for any new canary needing a carry-forward exception)
    - tests/_corpus/hostile_inputs.py (the now-9-entry CANARY_STRINGS produced by Task 2)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_hostile_inputs_consolidated.py MODIFIED (the 9 × 3 = 27-case auto-expansion + per-canary inline exception documentation pattern)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Hostile-Input Corpus (TEST-06) lines 1031-1071 (path mapping table; the 4 new canaries' expected detection surfaces)
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task row for `08-03 / TEST-06 (hostile inputs)` (canonical command: `pytest tests/test_hostile_inputs_consolidated.py -x`)
  </read_first>
  <behavior>
    - The matrix grows from 5×3 = 15 cases (Phase 6) to 9×3 = 27 cases (Phase 8) via pytest auto-expansion — no decorator change required.
    - All 27 cases pass: zero canary leaks across error / log / response surfaces.
    - If a new canary needs a carry-forward exception (analogous to the lone-surrogate exception at existing lines 258-259), document the exception inline AT the existing exception site with a comment naming the canary and the surfaces excluded.
    - If a new canary causes a genuine production leak (test fails), surface the failure verbatim in the SUMMARY rather than silently masking with an exception — the planner has not authorized scope-narrowing exceptions; only carry-forward exceptions matching the lone-surrogate precedent are allowed.
  </behavior>
  <action>
    Step (A) — Run the full matrix: `uv run pytest tests/test_hostile_inputs_consolidated.py -x -v` and capture the output. Verify pytest collects 27 cases (9 canaries × 3 tools — the canary parametrize axis × the tool parametrize axis at lines 138-149). Step (B) — If all 27 pass GREEN: NO edit needed; proceed to Step E. Step (C) — If any of the 4 new canary cases fail with a genuine production leak (e.g., the BOM character actually appears in an error body), surface the failure in SUMMARY and stop — do NOT mask with an exception. The planner has not authorized scope-narrowing; a real leak is a real bug that must be fixed in production code (which is out of scope for Phase 8 which is a coverage phase). Step (D) — If any of the 4 new canary cases fails for a CARRY-FORWARD reason analogous to the lone-surrogate exception (e.g., a Python repr() of the BOM character contains the literal `﻿` substring in a stderr capture even though the actual response body never echoes it), document the exception inline at the existing exception site (around line 258-259) with a comment naming the canary verbatim. The exception pattern: `if canary == "﻿": leaks = [s for s in leaks if not s.startswith(("error", "stderr_repr"))]` — exactly mirroring the existing lone-surrogate carry-forward shape. Per 08-PATTERNS.md "Document these in inline comments AT THE EXCEPTION SITE, not in a separate file." Step (E) — Verify the canonical VALIDATION.md command: `uv run pytest tests/test_hostile_inputs_consolidated.py -x` exits 0. Step (F) — Verify pytest collected exactly 27 cases (or the appropriate count if some are skipped via the carry-forward exception): `uv run pytest tests/test_hostile_inputs_consolidated.py --collect-only -q 2>&1 | grep -c 'test_'` is `>= 27`. Step (G) — Run ruff: `uv run ruff check tests/test_hostile_inputs_consolidated.py` AND `uv run ruff format --check tests/test_hostile_inputs_consolidated.py`. NO new fixtures. NO matrix-decorator changes (auto-expansion handles it). NO new test functions.
  </action>
  <acceptance_criteria>
    - Matrix collects 9 × 3 = 27 cases (or close to it if some carry-forward exceptions reduce the count): `uv run pytest tests/test_hostile_inputs_consolidated.py --collect-only -q 2>&1 | grep -E '\\.py::' | wc -l | xargs` is `>= 27`.
    - Per VALIDATION.md row 08-03 / TEST-06 (hostile inputs): `uv run pytest tests/test_hostile_inputs_consolidated.py -x` exits 0.
    - The matrix decorator is unchanged (no new parametrize axes added): `git diff -U0 tests/test_hostile_inputs_consolidated.py | grep -c '^[+-].*@pytest.mark.parametrize'` is `0` (zero net changes to parametrize lines).
    - If any per-canary exception was added (Step D): it lives inline at the existing exception site with a comment naming the canary verbatim — verify by inspecting the diff around line 258. If no exception was needed, the diff to this file is empty.
    - Full suite remains GREEN: `uv run pytest -x -q` exits 0.
    - Ruff clean: `uv run ruff check tests/test_hostile_inputs_consolidated.py` exits 0 AND `uv run ruff format --check tests/test_hostile_inputs_consolidated.py` exits 0.
    - No conftest changes: `git diff --name-only tests/conftest.py` is empty.
    - No new test functions: `grep -c '^async def test_\\|^def test_' tests/test_hostile_inputs_consolidated.py` is the same count as before this task (track via diff).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/test_hostile_inputs_consolidated.py --collect-only -q 2>&1 | grep -cE '\\.py::' && uv run pytest tests/test_hostile_inputs_consolidated.py -x && uv run ruff check tests/test_hostile_inputs_consolidated.py && uv run ruff format --check tests/test_hostile_inputs_consolidated.py</automated>
  </verify>
  <done>tests/test_hostile_inputs_consolidated.py runs at 9 × 3 = 27 cases via auto-expansion from the 9-entry CANARY_STRINGS; all cases GREEN; per-canary exceptions (if any) are inline at the existing exception site with named comments; matrix decorator unchanged; ruff clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Append TEST-04 traceability marker to tests/tools/test_retrieval_fragment_join.py docstring</name>
  <files>tests/tools/test_retrieval_fragment_join.py</files>
  <read_first>
    - tests/tools/test_retrieval_fragment_join.py lines 637-720 (the existing test_1500_fragment_walk_synthetic — Phase 5 origin; verify the docstring header lives at lines 643-648 or thereabouts via the file as it exists now)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/tools/test_retrieval_fragment_join.py MODIFIED (the EXACT one-paragraph append — locked verbatim)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § 1,500-Fragment Regression (TEST-04) lines 1075-1086 (action: docstring marker + REQUIREMENTS.md traceability update; the REQUIREMENTS.md update is owned by 08-06 not this plan)
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task row for `08-03 / TEST-04 (1500-frag)` (canonical command: `pytest tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic -x`)
  </read_first>
  <behavior>
    - The existing test_1500_fragment_walk_synthetic docstring gains one new paragraph at the END naming TEST-04 ownership: "TEST-04 owner: Phase 8 (regression test originated in Phase 5 D5-06)."
    - NO code changes — only the docstring is edited.
    - The test continues to pass (this is purely a docstring edit).
  </behavior>
  <action>
    Edit `tests/tools/test_retrieval_fragment_join.py`. Locate the existing `test_1500_fragment_walk_synthetic` function at line 637 (verify the exact docstring start/end via the file as it exists now). Inside the function's docstring (a multi-line `"""..."""` literal at the top of the function body), at the END of the docstring (just before the closing `"""`), APPEND a blank line and one new paragraph: `TEST-04 owner: Phase 8 (regression test originated in Phase 5 D5-06).` The paragraph is a single line of plain text. Per 08-PATTERNS.md: "No code changes. This is the smallest possible edit — adds the TEST-04 traceability per RESEARCH.md lines 1081-1086." Do NOT modify the test function body, fixture wiring, parametrize decorators, or any other test in the file. Do NOT modify REQUIREMENTS.md in this plan — the traceability table update is owned by Plan 08-06's README/traceability sweep. After the edit, run the canonical VALIDATION.md command: `uv run pytest tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic -x` exits 0. Run ruff: `uv run ruff check tests/tools/test_retrieval_fragment_join.py` AND `uv run ruff format --check tests/tools/test_retrieval_fragment_join.py`.
  </action>
  <acceptance_criteria>
    - TEST-04 traceability marker present in the function docstring: `grep -A 50 'def test_1500_fragment_walk_synthetic' tests/tools/test_retrieval_fragment_join.py | grep -c 'TEST-04 owner'` is `>= 1`.
    - Per VALIDATION.md row 08-03 / TEST-04 (1500-frag): `uv run pytest tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic -x` exits 0.
    - No code changes — diff is docstring-only: `git diff tests/tools/test_retrieval_fragment_join.py` shows changes only inside a `"""..."""` block, no edits to function-body lines, no new imports, no decorator changes.
    - Full suite remains GREEN: `uv run pytest -x -q` exits 0.
    - Ruff clean: `uv run ruff check tests/tools/test_retrieval_fragment_join.py` exits 0 AND `uv run ruff format --check tests/tools/test_retrieval_fragment_join.py` exits 0.
    - REQUIREMENTS.md NOT touched in this plan (owned by 08-06): `git diff --name-only .planning/REQUIREMENTS.md` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic -x && grep -A 50 'def test_1500_fragment_walk_synthetic' tests/tools/test_retrieval_fragment_join.py | grep 'TEST-04 owner' && uv run ruff check tests/tools/test_retrieval_fragment_join.py</automated>
  </verify>
  <done>tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic docstring carries the TEST-04 traceability marker. No code changes. Test still passes. The REQUIREMENTS.md traceability table update is forward-pointed to Plan 08-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Successful tool envelope ↔ HEAVY_COLUMNS partition contract | If a future change to `tools/retrieval.py` row-reshape logic accidentally inlines a heavy column at row top level, the response leaks heavy text outside `retrieved_content` — breaking the "labeling, not filtering" injection-resistance contract |
| Hostile-input corpus (CANARY_STRINGS) ↔ filter-value-echo paths across tools / surfaces | Any new code path that emits a user-supplied value into an error body, log line, or response metadata field re-opens INJ-05 — the 9 × 3 matrix is the regression gate |
| Test corpus growth ↔ existing test-ID stability | Reordering CANARY_STRINGS entries renames pytest test IDs (parametrize index → name), breaking historical CI logs; appending preserves IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DATA-LEAK | Information Disclosure | tools/retrieval.py + tools/fetch.py + tools/search.py row-reshape paths | mitigate | Task 1's per-tool fan-out adds explicit `set(row.keys()) ∩ HEAVY_COLUMNS == ∅` partition assertion with diff-readable failure message naming the leaked subset. Failure surfaces the exact tool + leaked keys for one-line triage. |
| T-INJECT | Information Disclosure / Injection | All filter-value-echo emission paths (error bodies, structured log lines, response metadata) | mitigate | Tasks 2 + 3 grow the canary corpus to 9 entries (BOM, RTL, malformed UTF-8 surrogate, FTS5 op string added) and exercise it via the existing 9 × 3 = 27-case parametrized matrix across query_table / search / fetch. Surface fan-out via `_surfaces_contain` covers stdout / stderr / log / error simultaneously. |
| T-TEST-ID-DRIFT | Tampering (test-ID renaming silently invalidates historical CI logs) | tests/_corpus/hostile_inputs.py CANARY_STRINGS ordering | mitigate | Task 2 strictly APPENDS the 4 new canaries at indices 5-8; existing 5 entries stay verbatim at indices 0-4. Acceptance criteria assert the first-5-entries invariant via Python introspection. |
| T-TEST-04-ORPHAN | Tampering (requirement orphaned from test) | TEST-04 traceability — regression test exists from Phase 5 but no Phase 8 owner marker | mitigate | Task 4's docstring marker explicitly names Phase 8 ownership. The REQUIREMENTS.md traceability-table update is forward-pointed to Plan 08-06's traceability sweep. |
| T-SCOPE-NARROWING | Tampering (test exceptions silently mask production leaks) | tests/test_hostile_inputs_consolidated.py per-canary carry-forward exceptions | mitigate | Task 3 explicitly forbids scope-narrowing exceptions: only carry-forward exceptions analogous to the existing lone-surrogate precedent are allowed, and they must be documented inline at the exception site with a comment naming the canary. Genuine production leaks must be surfaced in SUMMARY rather than masked. |
</threat_model>

<verification>
- VALIDATION.md per-task table rows covered by this plan (all must flip to ✅ green):
  - `08-03 / TEST-03 (snapshot keys)` — `pytest tests/test_envelope_snapshot.py -x`
  - `08-03 / TEST-03 (retrieved_content)` — `pytest tests/test_envelope_snapshot.py -k retrieved_content -x`
  - `08-03 / TEST-06 (hostile inputs)` — `pytest tests/test_hostile_inputs_consolidated.py -x` (now collects 9×3 = 27 cases)
  - `08-03 / TEST-04 (1500-frag)` — `pytest tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic -x`
- CANARY_STRINGS invariants: `python -c "from tests._corpus.hostile_inputs import CANARY_STRINGS; assert len(CANARY_STRINGS) == 9 and CANARY_STRINGS[0] == '</system>' and CANARY_STRINGS[4] == '\\udc80'"` exits 0.
- Full unit suite GREEN: `uv run pytest -x -q` exits 0.
- Ruff clean: `uv run ruff check src tests` exits 0 AND `uv run ruff format --check src tests` exits 0.
- File-touch discipline: `git diff --name-only` shows exactly four files from this plan — `tests/test_envelope_snapshot.py`, `tests/_corpus/hostile_inputs.py`, `tests/test_hostile_inputs_consolidated.py` (possibly empty diff if no exception was needed), `tests/tools/test_retrieval_fragment_join.py`. `tests/conftest.py` and `.planning/REQUIREMENTS.md` are NOT touched.
</verification>

<success_criteria>
- TEST-03 closes: per-tool envelope snapshot fan-out asserts the explicit row-key partition (set ∩ HEAVY_COLUMNS == ∅; retrieved_content keys ⊆ HEAVY_COLUMNS) with diff-readable error messages naming the leaked subset per tool.
- TEST-06 closes: CANARY_STRINGS grows to 9 entries (4 new canaries appended preserving existing test ID stability), and the 9 × 3 = 27-case parametrized matrix runs GREEN across query_table / search / fetch with zero leaks across error / log / response surfaces.
- TEST-04 traceability closes: tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic carries an explicit Phase 8 ownership marker in its docstring.
- D2-10 single-source-of-truth invariant preserved (no direct `config.HIDDEN_COLUMNS` reads anywhere — Plan 08-02 owns hidden-column tests; this plan does not touch hidden-column lookup).
- No new fixtures in `tests/conftest.py` (single-plan-touch enforced).
- No production code touched (Phase 8 is a coverage phase; T-DATA-LEAK / T-INJECT mitigations are TEST additions, not production-code changes).
- Full pytest suite GREEN; ruff clean across `src` and `tests`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-tests-24h-soak/08-03-SUMMARY.md` documenting:
- Number of cases collected by `pytest tests/test_hostile_inputs_consolidated.py --collect-only -q` (expected 27 = 9 × 3).
- The first-5-entries invariant verbatim verification output (CANARY_STRINGS[0..4] match Phase 6).
- Whether any per-canary carry-forward exception was added in Task 3 (and which canary if so).
- Whether any tool's `_DISPATCH_ARGS` stub in `tests/test_envelope_snapshot.py` was identified as omitting heavy columns (per 08-RESEARCH.md Pitfall 5 — note for follow-up; do NOT fix in this plan).
- Confirmation that tests/conftest.py and .planning/REQUIREMENTS.md were NOT modified.
- A 1-line forward pointer: "TEST-03/04/06 closed; 08-04/05/06 may proceed (08-04 / 08-05 in parallel within Wave 3; 08-06 in Wave 4)."
</output>
</content>
</invoke>