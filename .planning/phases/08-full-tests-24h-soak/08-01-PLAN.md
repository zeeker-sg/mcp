---
phase: 08
plan: 01
plan_id: "08-01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/mcp_zeeker/app.py
  - tests/test_dependency_footprint.py
  - tests/test_app_lifespan_contract.py
autonomous: true
requirements:
  - NFR-04
must_haves:
  truths:
    - "tests/test_dependency_footprint.py is importable and parses pyproject.toml via stdlib tomllib (no new deps), asserting [project.dependencies] == {fastmcp, pydantic, httpx, starlette, uvicorn, structlog} and [dependency-groups.dev] == {pytest, pytest-asyncio, pytest-httpx, ruff}."
    - "tests/test_app_lifespan_contract.py drives the lifespan async context manager with a synthetic non-FunctionTool registered via monkeypatch on mcp.list_tools and observes RuntimeError (matching 'tool contract drift') — never AttributeError — closing CR-02 from 07-VERIFICATION.md."
    - "src/mcp_zeeker/app.py line 59 uses getattr(tool, 'return_type', None) is not Envelope so a Tool subclass without a return_type attribute surfaces as the contract-drift RuntimeError, not AttributeError."
    - "Phase 8 Wave 0 establishes the dep-footprint and lifespan-contract regression tests that subsequent waves rely on; full pytest suite remains GREEN with the new files added."
  artifacts:
    - path: tests/test_dependency_footprint.py
      provides: "NFR-04 lock — set-equality assertions for runtime + dev dep tuples parsed from pyproject.toml via tomllib"
      contains: "test_runtime_deps_match_locked_set"
    - path: tests/test_app_lifespan_contract.py
      provides: "CR-02 regression — non-FunctionTool stand-in raises RuntimeError, not AttributeError"
      contains: "test_non_function_tool_raises_runtime_error_not_attribute_error"
    - path: src/mcp_zeeker/app.py
      provides: "CR-02 fix at line 59 — getattr() defensive read replaces direct attribute access"
      contains: "getattr(tool, \"return_type\", None)"
  key_links:
    - from: tests/test_dependency_footprint.py
      to: pyproject.toml
      via: "tomllib.loads(Path(__file__).resolve().parent.parent / 'pyproject.toml').read_text() — set-equality vs frozenset literal"
      pattern: "tomllib\\.loads"
    - from: tests/test_app_lifespan_contract.py
      to: src/mcp_zeeker/app.py::lifespan
      via: "monkeypatch.setattr(mcp, 'list_tools', stub_returning_non_function_tool); async with lifespan(Starlette()): ... — pytest.raises(RuntimeError, match='tool contract drift')"
      pattern: "async with lifespan"
    - from: src/mcp_zeeker/app.py::lifespan
      to: getattr builtin
      via: "if getattr(tool, 'return_type', None) is not Envelope: raise RuntimeError(...) — converts latent AttributeError into the intended contract-drift RuntimeError"
      pattern: "getattr\\(tool, .return_type."
---

<objective>
Land Phase 8 Wave 0: the two NEW unit-test files that downstream waves depend on (`tests/test_dependency_footprint.py` for NFR-04 and `tests/test_app_lifespan_contract.py` for CR-02), AND the one-line CR-02 production fix at `src/mcp_zeeker/app.py:59` that converts the latent `AttributeError` into the intended `RuntimeError("tool contract drift: ...")`.

Purpose: Phase 8 is a coverage phase. Wave 0 establishes the two regression gates that the rest of the phase (and v1) cannot ship without. NFR-04 lock prevents silent dep drift. The CR-02 fix is the carry-forward from 07-VERIFICATION.md "Deferred Items" row 1 that the Phase 7 success-criteria contract did not own; Phase 8's "complete test suite covers every contract surface" goal explicitly includes hardening the lifespan contract against future `TransformedTool` / non-`FunctionTool` registrations.

Output: One production-code one-liner (`src/mcp_zeeker/app.py:59`), two NEW test files (`tests/test_dependency_footprint.py`, `tests/test_app_lifespan_contract.py`).
</objective>

## Phase Goal

**As a** developer maintaining mcp.zeeker.sg, **I want** the test suite to enforce NFR-04's locked dependency footprint and to verify the FastMCP lifespan tolerates non-FunctionTool registrations gracefully (raising a contract-drift `RuntimeError` rather than a stack-trace-noisy `AttributeError`), **so that** the connector cannot silently grow its runtime dependency set in CI and a future Phase 9 `TransformedTool` registration cannot break startup with an unhelpful error.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-full-tests-24h-soak/08-RESEARCH.md
@.planning/phases/08-full-tests-24h-soak/08-PATTERNS.md
@.planning/phases/08-full-tests-24h-soak/08-VALIDATION.md
@.planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VERIFICATION.md
@src/mcp_zeeker/app.py
@src/mcp_zeeker/server.py
@src/mcp_zeeker/core/envelope.py
@src/mcp_zeeker/config.py
@tests/test_config_lookup_single_source.py
@tests/test_envelope_snapshot.py
@tests/test_retrieved_at_middleware.py
@tests/test_error_catalog.py
@pyproject.toml

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->
<!-- Use these directly — no codebase exploration required. -->

From src/mcp_zeeker/app.py (current lifespan, broken at line 59):
- `lifespan(app: Starlette)` is an `@contextlib.asynccontextmanager` async generator
- Inside it calls `tools = await mcp.list_tools()` then iterates and reads `tool.return_type`
- The intended failure mode on a non-Envelope return_type is `RuntimeError("tool contract drift: ...")`
- The latent bug: `tool.return_type` on a FastMCP `Tool` base class (rather than `FunctionTool` subclass) raises `AttributeError` BEFORE the intended `RuntimeError` can fire
- The lifespan must enter via `async with lifespan(app):` — direct call yields a generator object, not the context

From src/mcp_zeeker/server.py:
- `mcp` is the module-level FastMCP instance
- `mcp.list_tools()` is async and returns `list[Tool]` (FastMCP Tool base; concrete instances today are FunctionTool)

From src/mcp_zeeker/core/envelope.py:
- `Envelope` is the canonical return type all 6 production tools must annotate

From src/mcp_zeeker/config.py:
- `TOOL_TRAILER: str` — the locked safety-trailer suffix every tool description must end with

From pyproject.toml [project] / [dependency-groups]:
- runtime: fastmcp, pydantic, httpx, starlette, uvicorn, structlog (6 entries — order in TOML is irrelevant; use frozenset comparison)
- dev: pytest, pytest-asyncio, pytest-httpx, ruff (4 entries)
- Specifier shapes vary: `name~=N.M`, `name>=X,<Y` (PEP 508). Strip the specifier on the FIRST non-name char (any of `~ = < > ! ; [ ` ` `).

From tests/test_config_lookup_single_source.py (analog — whole-file static-source assertion shape):
- Pattern: `Path(__file__).resolve().parent.parent` returns project root
- Assertion message convention: `f"... missing={missing!r} extra={extra!r}"` — diff-readable

From tests/test_envelope_snapshot.py lines 47-74 + tests/test_error_catalog.py lines 134-159 (analog — monkeypatch + SimpleNamespace for middleware/lifespan unit tests):
- `types.SimpleNamespace(name="x", description="...")` synthesizes a Tool stand-in
- `monkeypatch.setattr(mcp, "list_tools", _fake_list_tools)` swaps out the registry for one test
- The fake_list_tools returns `[fake_tool]` and is `async def`

From tests/conftest.py:
- Phase 8 MUST NOT add fixtures (08-RESEARCH.md "Wave 0 Requirements" line 88: "single-plan-touch already enforced; Phase 8 reuses existing fixtures only")
- Both new test files use only stdlib + pytest + monkeypatch
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_dependency_footprint.py — NFR-04 lock via tomllib</name>
  <files>tests/test_dependency_footprint.py</files>
  <read_first>
    - pyproject.toml lines 1-22 (the literal runtime + dev dep tuples this test locks)
    - tests/test_config_lookup_single_source.py (full file — the closest analog: whole-file static-source assertion + diff-readable error messages + Path(__file__).resolve().parent.parent root resolution)
    - tests/test_error_catalog.py lines 50-65 (set-equality assertion shape; the `f"set mismatch: {set(a) ^ set(b)}"` style is the convention to mirror)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_dependency_footprint.py (concrete pattern locked for this file — extracted dep-name parsing + assertion shapes)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Dependency-footprint enforcement (lines ~562-635 — full reference test body)
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task table rows for 08-01 / NFR-04 (the `pytest tests/test_dependency_footprint.py::test_runtime_deps_match_locked_set -x` and `::test_dev_deps_match_locked_set -x` commands the test must support)
  </read_first>
  <behavior>
    - test_runtime_deps_match_locked_set: parses pyproject.toml via tomllib, extracts [project.dependencies], strips PEP-508 specifiers to bare names, asserts the resulting frozenset equals exactly {fastmcp, pydantic, httpx, starlette, uvicorn, structlog}. Failure message names the diff via "missing=" and "extra=".
    - test_dev_deps_match_locked_set: same shape against [dependency-groups.dev]; asserts equal to {pytest, pytest-asyncio, pytest-httpx, ruff}.
    - test_pinning_discipline_runtime: every entry in [project.dependencies] contains at least one version operator from {`~=`, `>=`, `==`} so the lock can never silently widen.
    - All three tests must be plain (sync) `def test_*` functions — no pytest-asyncio decoration. Module imports stdlib only (tomllib, pathlib, re).
  </behavior>
  <action>
    Create the new file `tests/test_dependency_footprint.py`. Per 08-PATTERNS.md and 08-RESEARCH.md, the file MUST: (a) start with `from __future__ import annotations`; (b) import stdlib only — `tomllib`, `pathlib.Path`, `re` — and import nothing from `mcp_zeeker` (the test reads pyproject.toml directly, never the installed dist metadata, to avoid drift between source and installed wheel); (c) define module-level `RUNTIME_DEPS_LOCKED = frozenset({"fastmcp","pydantic","httpx","starlette","uvicorn","structlog"})` and `DEV_DEPS_LOCKED = frozenset({"pytest","pytest-asyncio","pytest-httpx","ruff"})` with comments naming NFR-04 and pointing back to pyproject.toml lines 6-21; (d) define a private helper `_project_root() -> Path` returning `Path(__file__).resolve().parent.parent` per 08-PATTERNS.md "Trustable Project-Root Resolution" pattern (the test file lives at fixed depth `tests/` so `.parent.parent` returns the directory containing `pyproject.toml`); (e) define a private helper `_parse_dep_name(spec: str) -> str` that strips at the first occurrence of any of `~ = < > ! ; [` or whitespace — implement using a small explicit loop (08-RESEARCH.md "Dependency-footprint enforcement" line ~601) OR `re.split(r"[~=<>!;\\[\\s]", spec, 1)[0]` — either is acceptable. Then implement THREE plain (sync) test functions named exactly `test_runtime_deps_match_locked_set`, `test_dev_deps_match_locked_set`, `test_pinning_discipline_runtime`. Each test parses pyproject.toml via `tomllib.loads(_project_root() / "pyproject.toml").read_text()`, navigates to the right section (`payload["project"]["dependencies"]` for runtime, `payload["dependency-groups"]["dev"]` for dev — note the hyphenated key requires bracket lookup, NOT dotted attribute access), builds a frozenset of stripped names, and compares. Failure messages MUST be diff-readable per the 08-PATTERNS.md "Diff-Readable Assertion Messages" pattern: `f"runtime deps drifted: missing={RUNTIME_DEPS_LOCKED - declared!r} extra={declared - RUNTIME_DEPS_LOCKED!r}"`. The third test (`test_pinning_discipline_runtime`) iterates over the raw `dependencies` list and asserts each entry contains at least one of the three operator substrings (`"~="`, `">="`, `"=="`); failure message names the offending entry verbatim. NO pytest-asyncio markers; NO `mcp_zeeker` imports anywhere in the file (it would couple the dep audit to the source being audited). Per 08-RESEARCH.md "Pitfall 8" — the test reads pyproject.toml ONLY, never uv.lock (which would surface transitive deps). NO new fixtures in tests/conftest.py per 08-RESEARCH.md "Wave 0 Requirements" line 88. The file MUST pass `uv run ruff check` and `uv run ruff format --check`.
  </action>
  <acceptance_criteria>
    - File exists: `test -f tests/test_dependency_footprint.py` exits 0.
    - Three test functions present: `grep -c '^def test_runtime_deps_match_locked_set' tests/test_dependency_footprint.py` is `1`; `grep -c '^def test_dev_deps_match_locked_set' tests/test_dependency_footprint.py` is `1`; `grep -c '^def test_pinning_discipline_runtime' tests/test_dependency_footprint.py` is `1`.
    - Stdlib-only — no mcp_zeeker import: `grep -v '^#' tests/test_dependency_footprint.py | grep -c 'import mcp_zeeker\|from mcp_zeeker'` is `0`.
    - Uses tomllib stdlib parser: `grep -v '^#' tests/test_dependency_footprint.py | grep -c 'tomllib'` is `>= 1`.
    - Diff-readable assertion message present: `grep -v '^#' tests/test_dependency_footprint.py | grep -E "missing=.+extra=" | wc -l | xargs` is `>= 2` (one per dep-set test).
    - Per VALIDATION.md row for NFR-04: `uv run pytest tests/test_dependency_footprint.py::test_runtime_deps_match_locked_set -x` exits 0.
    - Per VALIDATION.md row for NFR-04: `uv run pytest tests/test_dependency_footprint.py::test_dev_deps_match_locked_set -x` exits 0.
    - Pinning-discipline test passes: `uv run pytest tests/test_dependency_footprint.py::test_pinning_discipline_runtime -x` exits 0.
    - Whole file passes: `uv run pytest tests/test_dependency_footprint.py -x` exits 0.
    - Ruff clean: `uv run ruff check tests/test_dependency_footprint.py` exits 0 AND `uv run ruff format --check tests/test_dependency_footprint.py` exits 0.
    - No conftest changes: `git diff --name-only tests/conftest.py` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/test_dependency_footprint.py -x && uv run ruff check tests/test_dependency_footprint.py && uv run ruff format --check tests/test_dependency_footprint.py</automated>
  </verify>
  <done>tests/test_dependency_footprint.py exists, three plain (sync) tests pass, file is stdlib-only, ruff-clean. NFR-04 is now locked in CI — any future PR that adds a runtime or dev dep without updating the locked frozensets fails this test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — Create tests/test_app_lifespan_contract.py asserting RuntimeError (not AttributeError) on non-FunctionTool</name>
  <files>tests/test_app_lifespan_contract.py</files>
  <read_first>
    - src/mcp_zeeker/app.py lines 40-91 (full lifespan async context manager — Task 3 will fix line 59; this test must FAIL on the current line 59 form)
    - src/mcp_zeeker/server.py (mcp module-level FastMCP instance — the monkeypatch target is `mcp.list_tools`)
    - src/mcp_zeeker/core/envelope.py (Envelope class — the contract reference; the synthetic tool must NOT have return_type attribute so the contract-drift branch fires)
    - src/mcp_zeeker/config.py (TOOL_TRAILER constant — the synthetic tool's description must end with this so the trailer guard at line 64 doesn't pre-empt the return_type guard at line 59)
    - tests/test_envelope_snapshot.py lines 47-74 (analog — monkeypatch + SimpleNamespace pattern for FastMCP middleware/lifespan unit tests)
    - tests/test_error_catalog.py lines 134-159 (analog — direct-call middleware unit pattern with SimpleNamespace stand-in)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § tests/test_app_lifespan_contract.py (concrete CR-02 carryover pattern — full reference test body)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § CR-02 lifespan regression (lines ~501-560)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VERIFICATION.md lines 149, 165-167 (CR-02 deferral context — the bug is latent; today all 6 registered tools are FunctionTool instances, so the test MUST install a stand-in that lacks return_type)
  </read_first>
  <behavior>
    - test_non_function_tool_raises_runtime_error_not_attribute_error: monkeypatches `mcp.list_tools` to return a list containing one synthetic Tool stand-in (a `types.SimpleNamespace` with `name`, `description=f"...{config.TOOL_TRAILER}"`, and DELIBERATELY no `return_type` attribute). Drives the lifespan via `async with lifespan(Starlette()):` and asserts a `RuntimeError` matching `"tool contract drift"` is raised on enter. The test MUST FAIL on the current `app.py:59` (which raises `AttributeError`) and PASS after Task 3's one-line fix.
    - The test must NOT raise AttributeError — pytest.raises must specifically match RuntimeError.
    - On the current broken code, the assertion message must surface the AttributeError signature (so the executor sees the RED failure clearly).
  </behavior>
  <action>
    Create the new file `tests/test_app_lifespan_contract.py`. Per 08-PATTERNS.md and 08-RESEARCH.md, the file MUST: (a) start with `from __future__ import annotations`; (b) import stdlib `types`, `pytest`, plus `from starlette.applications import Starlette`, `from mcp_zeeker import config`, `from mcp_zeeker.app import lifespan`, `from mcp_zeeker.server import mcp` — these are all already-pinned modules, no new deps; (c) define ONE async test function named exactly `test_non_function_tool_raises_runtime_error_not_attribute_error` decorated with no extra markers (pytest-asyncio auto-mode picks it up). Inside the test body: (1) build a synthetic non-FunctionTool stand-in via `types.SimpleNamespace(name="fake_non_function_tool", description=f"some description\n\n{config.TOOL_TRAILER}")` — DELIBERATELY do NOT pass a `return_type` keyword; this is the entire point of the regression. The trailer is included so the description-trailer guard at app.py:64 doesn't fire first. (2) Define an async stub `_fake_list_tools()` that `return [fake_tool]`. (3) `monkeypatch.setattr(mcp, "list_tools", _fake_list_tools)` — the monkeypatch fixture is part of pytest standard library. (4) Build a minimal Starlette app via `app = Starlette()`. (5) Wrap the lifespan entry in `pytest.raises(RuntimeError, match="tool contract drift")` and `async with lifespan(app): pass`. The `match=` clause is critical — it verifies the error is the INTENDED contract-drift `RuntimeError`, not a different RuntimeError raised elsewhere (e.g., from `build_http_client` if the env is misconfigured). NO new fixtures in tests/conftest.py per 08-RESEARCH.md "Wave 0 Requirements" line 88. The test docstring must reference CR-02 verbatim and point to 07-VERIFICATION.md as the rationale source. Per 08-PATTERNS.md "Monkeypatch + SimpleNamespace" pattern: avoid the in-memory `mcp_client` fixture — it pulls in too much for this narrow contract test. The test MUST FAIL on current `app.py:59` (which reads `tool.return_type` directly, raising `AttributeError` instead of the intended `RuntimeError`); Task 3's fix makes it PASS.
  </action>
  <acceptance_criteria>
    - File exists: `test -f tests/test_app_lifespan_contract.py` exits 0.
    - Test function present with exact name: `grep -c 'def test_non_function_tool_raises_runtime_error_not_attribute_error' tests/test_app_lifespan_contract.py` is `>= 1`.
    - Uses SimpleNamespace stand-in (no return_type attr): `grep -c 'SimpleNamespace' tests/test_app_lifespan_contract.py` is `>= 1`.
    - Uses pytest.raises(RuntimeError) with match clause: `grep -E 'pytest\.raises\(RuntimeError.*match.*tool contract drift' tests/test_app_lifespan_contract.py | wc -l | xargs` is `>= 1`.
    - Uses async with lifespan: `grep -c 'async with lifespan' tests/test_app_lifespan_contract.py` is `>= 1`.
    - Monkeypatches mcp.list_tools: `grep -c 'monkeypatch.setattr(mcp' tests/test_app_lifespan_contract.py` is `>= 1`.
    - On the CURRENT broken code (Task 3 not yet applied), the test MUST FAIL: `uv run pytest tests/test_app_lifespan_contract.py::test_non_function_tool_raises_runtime_error_not_attribute_error -x` exits non-zero AND the failure surfaces an AttributeError (verify by capturing output: `uv run pytest tests/test_app_lifespan_contract.py -x 2>&1 | grep -E 'AttributeError|FAILED' | head -3` shows at least one AttributeError or FAILED line).
    - Stdlib + already-pinned imports only — no new deps: `grep -v '^#' tests/test_app_lifespan_contract.py | grep -E '^import |^from ' | grep -vE 'types|pytest|starlette|mcp_zeeker|__future__'` returns nothing.
    - Ruff clean: `uv run ruff check tests/test_app_lifespan_contract.py` exits 0 AND `uv run ruff format --check tests/test_app_lifespan_contract.py` exits 0.
    - No conftest changes: `git diff --name-only tests/conftest.py` is empty.
    - Production code untouched in this task: `git diff --name-only src/` is empty after this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/test_app_lifespan_contract.py -x 2>&1 | tail -10 && uv run ruff check tests/test_app_lifespan_contract.py && uv run ruff format --check tests/test_app_lifespan_contract.py && test -z "$(git diff --name-only src/)"</automated>
  </verify>
  <done>tests/test_app_lifespan_contract.py exists, the test FAILS RED on current src/mcp_zeeker/app.py:59 with an AttributeError surfacing in the failure output, ruff is clean, production code is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — Fix src/mcp_zeeker/app.py:59 with getattr() and run full suite</name>
  <files>src/mcp_zeeker/app.py</files>
  <read_first>
    - src/mcp_zeeker/app.py lines 50-70 (the envelope-contract guard block; the change is exactly one line at line 59 — do NOT touch surrounding lines including the trailer guard at line 64 or the ImportError tolerance at line 68-70)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § src/mcp_zeeker/app.py MODIFIED (the surgical change spec)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § CR-02 fix is one line + one test (lines ~553-560 — the verbatim before/after)
    - .planning/phases/07-rate-limit-structured-errors-healthz-logs/07-VERIFICATION.md "Deferred Items" row 1 (CR-02 owner: Phase 8)
    - The RED test from Task 2 (tests/test_app_lifespan_contract.py) — Task 3's success criterion is making it GREEN
  </read_first>
  <behavior>
    - After this task, src/mcp_zeeker/app.py line 59 reads `if getattr(tool, "return_type", None) is not Envelope:` instead of `if tool.return_type is not Envelope:`.
    - On a non-FunctionTool stand-in (no return_type attribute), the lifespan now raises `RuntimeError("tool contract drift: ... return_type is not Envelope")` — never AttributeError.
    - All 6 production tools (which ARE FunctionTool instances) continue to register and pass the contract guard at startup unchanged.
    - The Task 2 RED test now passes GREEN.
    - The full suite (pytest -x) remains GREEN with the new files added.
  </behavior>
  <action>
    Edit `src/mcp_zeeker/app.py` line 59. Change EXACTLY this one line: `if tool.return_type is not Envelope:` becomes `if getattr(tool, "return_type", None) is not Envelope:`. Do NOT modify any other line. Do NOT modify the surrounding RuntimeError message format on lines 60-62 — the contract-drift message text is the regex `match="tool contract drift"` the Task 2 test asserts on. Do NOT modify the trailer guard at line 64. Do NOT modify the ImportError tolerance at lines 68-70. Do NOT touch the surrounding `try` / `except ImportError` block; the `getattr` change is INSIDE the existing `try` block. Per 08-PATTERNS.md: "surgical change. Do NOT touch the surrounding ImportError tolerance (lines 68-70)." After the edit, run the full pytest suite to verify (a) the Task 2 test now passes, (b) all 6 production tools still pass the contract guard at startup (the existing `tests/test_envelope_contract.py` and `tests/test_envelope_snapshot.py` exercise this path with real FunctionTool instances), (c) no other tests regress. Also run ruff format and ruff check on the modified file. Per 08-VALIDATION.md per-task table row for `(CR-02 carryover)`: the canonical verification command is `pytest tests/test_app_lifespan_contract.py -x` — this command MUST exit 0 after this task.
  </action>
  <acceptance_criteria>
    - Exactly one line changed in src/mcp_zeeker/app.py: `git diff --stat src/mcp_zeeker/app.py | grep -E '\\| 2 \\+' | wc -l | xargs` is `>= 1` (one line added, one line removed).
    - The new line uses getattr: `grep -n 'getattr(tool, "return_type", None) is not Envelope' src/mcp_zeeker/app.py | wc -l | xargs` is `1`.
    - The old direct-attribute line is gone: `grep -nE 'if tool\.return_type is not Envelope' src/mcp_zeeker/app.py | wc -l | xargs` is `0`.
    - The surrounding trailer guard is untouched: `grep -c 'TOOL_TRAILER' src/mcp_zeeker/app.py` is `>= 1`.
    - The surrounding ImportError tolerance is untouched: `grep -c 'except ImportError' src/mcp_zeeker/app.py` is `>= 1`.
    - Per VALIDATION.md row for (CR-02 carryover): `uv run pytest tests/test_app_lifespan_contract.py -x` exits 0.
    - Per VALIDATION.md sampling-rate trigger ("After every task commit"): `uv run pytest tests/test_dependency_footprint.py tests/test_app_lifespan_contract.py -x` exits 0.
    - Existing envelope-contract tests still pass: `uv run pytest tests/test_envelope_contract.py tests/test_envelope_snapshot.py tests/test_app.py -x` exits 0.
    - Full suite remains GREEN: `uv run pytest -x` exits 0.
    - Ruff clean on the changed file: `uv run ruff check src/mcp_zeeker/app.py` exits 0 AND `uv run ruff format --check src/mcp_zeeker/app.py` exits 0.
    - Ruff clean on whole repo: `uv run ruff check src tests` exits 0 AND `uv run ruff format --check src tests` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && uv run pytest tests/test_app_lifespan_contract.py tests/test_dependency_footprint.py tests/test_envelope_contract.py tests/test_envelope_snapshot.py tests/test_app.py -x && uv run pytest -x && uv run ruff check src tests && uv run ruff format --check src tests</automated>
  </verify>
  <done>src/mcp_zeeker/app.py:59 uses getattr() defensively; the Task 2 RED test now passes GREEN; all 6 production FunctionTool registrations continue to pass the contract guard; full pytest suite GREEN; ruff clean across src and tests. CR-02 from 07-VERIFICATION.md is closed end-to-end (production fix + regression test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml ↔ deployed wheel ↔ uv.lock | A future PR could silently add a runtime dep (e.g., during a stack upgrade); without a CI gate, NFR-04's "exactly six runtime deps" invariant degrades silently |
| FastMCP Tool base class ↔ FunctionTool subclass ↔ TransformedTool / future subclasses | The lifespan reads `return_type` on `Tool` base; only `FunctionTool` declares it. A future Phase 9 `TransformedTool` registration would AttributeError at startup with no clear contract-drift signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DEP-DRIFT | Tampering / Information Disclosure | pyproject.toml [project.dependencies] (NFR-04 invariant) | mitigate | Task 1's `tests/test_dependency_footprint.py` asserts strict set-equality with the locked frozenset literal; runs on every PR via the standard pytest invocation. Diff-readable assertion message names exactly which dep was added or removed. Reads pyproject.toml ONLY (per 08-RESEARCH.md Pitfall 8 — uv.lock would surface transitive deps and produce false positives). |
| T-LIFESPAN-OPACITY | Denial of Service (startup failure with unhelpful error) | src/mcp_zeeker/app.py lifespan envelope-contract guard at line 59 | mitigate | Task 3 changes `tool.return_type` → `getattr(tool, "return_type", None)`. On a non-FunctionTool stand-in, the lifespan now raises the intended `RuntimeError("tool contract drift: ...")` instead of `AttributeError` — operators see a clear contract-drift message in the startup log, not a Python attribute trace. Task 2's regression test locks the behavior. |
| T-CR-02-LATENT | Tampering (latent bug surfacing only on Phase 9 expansion) | FastMCP Tool subclass hierarchy — TransformedTool, custom subclasses | accept | Today all 6 production tools are FunctionTool instances and the lifespan path works. The Task 3 fix is defensive against future subclass registrations; the Task 2 test prevents regression. No further mitigation needed; the fix is one line and is now permanently regression-tested. |
</threat_model>

<verification>
- VALIDATION.md per-task table rows covered by this plan: rows for `08-01 / NFR-04 / runtime deps`, `08-01 / NFR-04 / dev deps`, `08-01 / (CR-02 carryover) / lifespan RuntimeError` — all three rows must flip to ✅ green.
- Per the VALIDATION.md "Sampling Rate" gate ("After every task commit"): `uv run pytest tests/test_dependency_footprint.py tests/test_app_lifespan_contract.py -x` exits 0.
- Full unit suite: `uv run pytest -x -q` exits 0 (no regressions on existing 360+ tests).
- Lint clean: `uv run ruff check src tests` exits 0 AND `uv run ruff format --check src tests` exits 0.
- File-touch discipline: `git diff --name-only` shows exactly three paths from this plan — `src/mcp_zeeker/app.py`, `tests/test_dependency_footprint.py`, `tests/test_app_lifespan_contract.py`. No conftest.py changes (08-RESEARCH.md "Wave 0 Requirements" line 88 enforced).
- CR-02 closure smoke from 07-VERIFICATION.md "Deferred Items": `python -c "import types; from starlette.applications import Starlette; import asyncio; from mcp_zeeker import config; from mcp_zeeker.app import lifespan; from mcp_zeeker.server import mcp; fake = types.SimpleNamespace(name='fake', description='x' + config.TOOL_TRAILER); async def stub(): return [fake]; mcp.list_tools = stub; \nasync def go():\n    try:\n        async with lifespan(Starlette()): pass\n    except RuntimeError as e: print('ok:', e)\n    except AttributeError as e: print('BAD:', e)\nasyncio.run(go())"` prints a line starting with `ok: ` (not `BAD: `).
</verification>

<success_criteria>
- NFR-04 is locked in CI: `tests/test_dependency_footprint.py` asserts the exact 6-runtime + 4-dev frozensets and fails any future drift with a diff-readable message.
- CR-02 from 07-VERIFICATION.md is closed end-to-end: the lifespan accesses `tool.return_type` defensively via `getattr(...)`, and a regression test (`tests/test_app_lifespan_contract.py::test_non_function_tool_raises_runtime_error_not_attribute_error`) ensures any future revert to direct-attribute access surfaces immediately.
- All 6 production FunctionTool instances continue to pass the lifespan contract guard at startup unchanged (verified by existing `tests/test_envelope_contract.py` + `tests/test_envelope_snapshot.py` staying GREEN).
- Phase 8 Wave 0 establishes the foundation that downstream waves (08-02..08-06) build on: dep footprint can never silently grow, lifespan contract is regression-protected.
- Full pytest suite is GREEN, ruff is clean across `src` and `tests`, and `tests/conftest.py` is untouched (single-plan-touch rule per 08-RESEARCH.md "Wave 0 Requirements" line 88).
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-tests-24h-soak/08-01-SUMMARY.md` documenting:
- The exact one-line change in `src/mcp_zeeker/app.py:59` (paste the before / after).
- The two NEW test file paths and the test function names within each.
- Confirmation that `tests/conftest.py` was not modified.
- The NFR-04 locked frozensets verbatim (so future audits can grep for them).
- Confirmation that the canonical CR-02 closure smoke (described in `<verification>`) prints `ok:` and not `BAD:`.
- A 1-line forward pointer: "Wave 0 complete; 08-02 / 08-03 / 08-04 / 08-05 may now run in parallel within their respective waves."
</output>
</content>
</invoke>