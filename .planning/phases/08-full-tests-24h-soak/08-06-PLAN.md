---
phase: 08
plan: 06
plan_id: "08-06"
type: execute
wave: 4
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
  - "08-04"
  - "08-05"
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - NFR-05
must_haves:
  truths:
    - "README.md `### Anthropic IP allowlist` section is rewritten from the Phase 1 forward-looking placeholder (4 lines) into an operator-actionable section (~10 lines): Anthropic does not publish a stable machine-readable list of MCP-egress IPs as of 2026-05; operators consult Anthropic's operator-facing documentation, apply the allowlist at the host Caddy / firewall layer (NOT in the MCP container — Caddy already owns ingress per Caddyfile.prod), and re-verify at Phase 9 + quarterly. `grep -q 'Anthropic IP' README.md` succeeds (closing the canonical NFR-05 verification command from VALIDATION.md)."
    - "README.md continues to document the single-worker constraint (RATE-06 / NFR-05 second clause) — the existing section at README.md:71-90 is untouched OR pointer-extended; the existing `--workers 1` discipline section is the canonical reference and must remain searchable via `grep -q 'workers 1' README.md`."
    - ".planning/REQUIREMENTS.md traceability table reflects Phase 8 closures: TEST-01..06 + NFR-01..05 rows update Status column from `Pending` to `Satisfied (Phase 8 plan-NN)` per row, with the appropriate plan ID per requirement (per the union of plans 08-01..06 requirements frontmatter). The TEST-04 row specifically references Phase 5 origin + Phase 8 ownership marker landed in 08-03's docstring edit."
  artifacts:
    - path: README.md
      provides: "Operator-actionable Anthropic IP allowlist section replacing the Phase 1 forward-looking placeholder; preserves existing single-worker discipline section"
      contains: "Anthropic IP allowlist"
    - path: .planning/REQUIREMENTS.md
      provides: "Traceability table with Phase 8 status updates: TEST-01..06 + NFR-01..05 marked Satisfied with plan ID references"
      contains: "TEST-01"
  key_links:
    - from: README.md "### Anthropic IP allowlist" section
      to: Caddyfile.prod
      via: "Caddy owns ingress per existing operator-config; allowlist applied at the host Caddy layer (NOT in the MCP container) per the operator-actionable instructions"
      pattern: "Caddy"
    - from: .planning/REQUIREMENTS.md TEST-04 row
      to: tests/tools/test_retrieval_fragment_join.py::test_1500_fragment_walk_synthetic
      via: "Plan 08-03 Task 4 docstring marker established Phase 8 ownership; this plan updates the traceability table to reference the test file"
      pattern: "TEST-04"
---

<objective>
Land Phase 8 Wave 4 (final): the operator-documentation delta that closes NFR-05 and the requirements-traceability sweep that flips TEST-01..06 + NFR-01..05 from `Pending` to `Satisfied (Phase 8 plan-NN)` in `.planning/REQUIREMENTS.md`. Two MODIFIED files, no new code, no new tests.

Purpose: NFR-05 is the operator-facing surface of Phase 8 — once shipped, anyone deploying mcp.zeeker.sg knows (a) they MUST allowlist Anthropic's MCP-egress IPs at the Caddy / firewall layer, (b) Anthropic does not publish a stable machine-readable list as of the v1 ship date, and (c) the single-worker uvicorn discipline is non-negotiable. Without this delta, NFR-05 is unverifiable (the canonical command in 08-VALIDATION.md is literally `grep -q 'Anthropic IP' README.md` with the operator-actionable wording). The traceability sweep is the audit trail — REQUIREMENTS.md is the canonical source-of-truth for what shipped vs what is pending; without the sweep, Phase 9 (registry submission) cannot honestly claim TEST-* / NFR-* coverage.

Output: Two MODIFIED files — `README.md` (one section rewritten + section-pointer if needed) and `.planning/REQUIREMENTS.md` (traceability table rows updated for the 11 Phase-8 requirement IDs). No source code changes. No test changes. No new dependencies. No new artifacts.
</objective>

## Phase Goal

**As a** Phase 9 (registry submission) reviewer or future operator deploying mcp.zeeker.sg, **I want** the README to tell me concretely what to do about Anthropic's MCP-egress IP allowlist (where to apply it, who owns the source-of-truth list, and the cadence to re-verify) AND the REQUIREMENTS.md traceability table to honestly reflect which Phase 8 contracts have been verified end-to-end, **so that** I can ship the v1 deploy without reinventing the operator-config story and submit the Phase 9 PR with a clean traceability matrix.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-full-tests-24h-soak/08-RESEARCH.md
@.planning/phases/08-full-tests-24h-soak/08-PATTERNS.md
@.planning/phases/08-full-tests-24h-soak/08-VALIDATION.md
@.planning/phases/08-full-tests-24h-soak/08-01-PLAN.md
@.planning/phases/08-full-tests-24h-soak/08-02-PLAN.md
@.planning/phases/08-full-tests-24h-soak/08-03-PLAN.md
@.planning/phases/08-full-tests-24h-soak/08-04-PLAN.md
@.planning/phases/08-full-tests-24h-soak/08-05-PLAN.md
@README.md
@Caddyfile.prod

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From README.md lines 105-110 (existing forward-looking placeholder — REPLACE this in Task 1):
```
### Anthropic IP allowlist (forward-looking)

The deployed instance must accept connections from Anthropic's published egress IP ranges to
be reachable via Claude Desktop and Claude Code. Phase 1 ships without an explicit IP
allowlist; Phase 9 (registry submission) will add the operational note and any Caddy-level
`trusted_proxies` configuration needed.
```

From .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § README.md MODIFIED (locked replacement text — operator-actionable, ~10 lines, mirrors existing `## Caddy header requirements` bullet-list style at README.md:57-67):
The replacement text per 08-PATTERNS.md "Phase 8 modification":
```
### Anthropic IP allowlist

The deployed instance must accept inbound connections from Anthropic's MCP egress IP ranges
to be reachable via Claude Desktop and Claude Code. Anthropic does not (as of 2026-05) publish
a stable, machine-readable list of MCP-egress IPs; operators should:

1. Consult Anthropic's operator-facing documentation or registry-onboarding contact for the
   current allowlist.
2. Apply the allowlist at the host Caddy layer (or upstream firewall), NOT in the MCP
   container — Caddy already owns ingress per `Caddyfile.prod`.
3. Re-verify the allowlist at Phase 9 (registry submission) and quarterly thereafter; the
   IPs change without notice.

Operators who allowlist by domain rather than IP can use Anthropic's published egress hostnames
where available; this trades a lookup hop for resilience to IP churn.
```

From README.md lines 71-90 (existing single-worker constraint section — DO NOT MODIFY in this plan; per 08-PATTERNS.md the existing `--workers 1` discipline section is canonical):
- The section already documents the RATE-06 single-worker constraint
- NFR-05 second clause ("deployment README documents ... the single-worker Uvicorn constraint") is satisfied by the existing section — Phase 8 only needs to verify it remains searchable via `grep -q 'workers 1' README.md`

From Caddyfile.prod (existing operator-config):
- Caddy owns ingress (TLS termination, header normalization, etc.) per the existing config
- The replacement README text references this verbatim via "Caddy already owns ingress per `Caddyfile.prod`"

From .planning/REQUIREMENTS.md lines 271-281 (existing traceability table rows for Phase 8 — currently `Pending`):
- TEST-01..06 (6 rows) marked `Pending` → flip to `Satisfied (Phase 8)` with the appropriate plan ID per row:
  - TEST-01 → "Satisfied (08-02)"
  - TEST-02 → "Satisfied (08-04)"
  - TEST-03 → "Satisfied (08-03)"
  - TEST-04 → "Satisfied (08-03 docstring marker; test originated in Phase 5)"
  - TEST-05 → "Satisfied (08-05 smoke gate; full 24h via workflow_dispatch)"
  - TEST-06 → "Satisfied (08-03)"
- NFR-01..05 (5 rows) marked `Pending` → flip to `Satisfied (Phase 8)` with the appropriate plan ID per row:
  - NFR-01 → "Satisfied (08-05 report-gate)"
  - NFR-02 → "Satisfied (08-05 driver --concurrency 50)"
  - NFR-03 → "Satisfied (08-05 report-gate)"
  - NFR-04 → "Satisfied (08-01 dep-footprint test)"
  - NFR-05 → "Satisfied (08-06 README delta)"

From .planning/REQUIREMENTS.md lines 264-268 (existing OBS rows for Phase 7 carryover — DO NOT MODIFY in this plan):
- OBS-04 was updated by Plan 07-07 to "Satisfied (07-07 gap closure — CR-01)"
- OBS-02 marked "Deferred to v2 (D7-05)"
- This plan does NOT touch OBS rows — those are Phase 7's domain

From .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task table:
- Row `08-06 / NFR-05 (allowlist doc)` — canonical command: `grep -q 'Anthropic IP' README.md` (must exit 0 after this plan)
- Row `08-06 / NFR-05 (single-worker doc)` — already satisfied at README.md:71-75; this plan verifies it remains so via `grep -q 'workers 1' README.md`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace README.md `### Anthropic IP allowlist (forward-looking)` section with operator-actionable wording</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 105-110 (the existing placeholder section — REPLACE this; NOT append, NOT prepend, NOT add a sibling section)
    - README.md lines 57-67 (the existing `## Caddy header requirements` section — its bullet-list operator-action style is the model the replacement mirrors)
    - README.md lines 71-90 (the existing single-worker constraint section — DO NOT MODIFY; this plan verifies it remains intact)
    - .planning/phases/08-full-tests-24h-soak/08-PATTERNS.md § README.md MODIFIED (the LOCKED replacement text — copy verbatim, including the heading "### Anthropic IP allowlist" without the "(forward-looking)" suffix)
    - .planning/phases/08-full-tests-24h-soak/08-RESEARCH.md § Operator Documentation (NFR-05) lines 1397-1422 (the rationale for the wording — Anthropic does not publish a stable list as of 2026-05; operator chooses between sharing per-IP bucket vs maintaining a wildcard CIDR allowlist)
    - .planning/phases/08-full-tests-24h-soak/08-VALIDATION.md per-task row `08-06 / NFR-05 (allowlist doc)` (canonical command: `grep -q 'Anthropic IP' README.md` MUST exit 0 after this plan)
    - Caddyfile.prod (the operator-config the replacement text references via backtick-quoted `Caddyfile.prod`)
  </read_first>
  <behavior>
    - The existing section at README.md:105-110 (4 lines starting with `### Anthropic IP allowlist (forward-looking)`) is REPLACED with the operator-actionable section per 08-PATTERNS.md (~10-12 lines, heading "### Anthropic IP allowlist" without the "(forward-looking)" suffix).
    - The replacement mirrors the bullet-list operator-action style of the existing `## Caddy header requirements` section at README.md:57-67.
    - The existing single-worker constraint section at README.md:71-90 is UNCHANGED.
    - All other sections of README.md are UNCHANGED.
    - `grep -q 'Anthropic IP' README.md` exits 0 (the canonical NFR-05 verification command).
    - `grep -q 'workers 1' README.md` exits 0 (the second-clause NFR-05 verification).
  </behavior>
  <action>
    Edit `README.md`. Locate the existing `### Anthropic IP allowlist (forward-looking)` section at lines 105-110 (verify the exact line range via the file as it exists now — line numbers may have drifted). Use the Edit tool to REPLACE the entire 4-line placeholder section (the heading line + 4 lines of paragraph text) with the locked replacement text from 08-PATTERNS.md § README.md MODIFIED. The replacement begins with the heading `### Anthropic IP allowlist` (note: NO `(forward-looking)` suffix anymore — the section is now actionable, not forward-looking) and contains:
       - 1 paragraph framing: "The deployed instance must accept inbound connections from Anthropic's MCP egress IP ranges to be reachable via Claude Desktop and Claude Code. Anthropic does not (as of 2026-05) publish a stable, machine-readable list of MCP-egress IPs; operators should:"
       - 3 numbered bullets:
         1. Consult Anthropic's operator-facing documentation or registry-onboarding contact for the current allowlist.
         2. Apply the allowlist at the host Caddy layer (or upstream firewall), NOT in the MCP container — Caddy already owns ingress per `Caddyfile.prod`.
         3. Re-verify the allowlist at Phase 9 (registry submission) and quarterly thereafter; the IPs change without notice.
       - 1 trailing paragraph: "Operators who allowlist by domain rather than IP can use Anthropic's published egress hostnames where available; this trades a lookup hop for resilience to IP churn."
    Per 08-PATTERNS.md "Style match: Mirror the existing `## Caddy header requirements` section's bullet-list operator-action pattern (`README.md` lines 57-67)." DO NOT modify ANY other section of README.md. DO NOT touch the single-worker constraint section at README.md:71-90 — it is the canonical NFR-05 second-clause documentation and Phase 8 only verifies it remains searchable. DO NOT modify the `## Environment` table (line 112+) or `## Testing` section (line 122+) or any other downstream content. After the edit, verify both NFR-05 grep commands: `grep -q 'Anthropic IP' README.md` AND `grep -q 'workers 1' README.md` BOTH exit 0. Verify the section is no longer marked "(forward-looking)": `grep -c 'forward-looking' README.md` is `0` (or matches its prior count if the phrase appears elsewhere — which it does NOT in the current README based on the read context). NO source code changes; NO test changes; NO new dependencies.
  </action>
  <acceptance_criteria>
    - README.md still exists and is non-empty: `test -s README.md` exits 0.
    - Per VALIDATION.md row 08-06 / NFR-05 (allowlist doc): `grep -q 'Anthropic IP' README.md` exits 0.
    - Per VALIDATION.md row 08-06 / NFR-05 (single-worker doc): `grep -q 'workers 1' README.md` exits 0 (the existing section is preserved).
    - Operator-actionable wording present (one of the locked bullets): `grep -c 'host Caddy layer' README.md` is `>= 1`.
    - The "(forward-looking)" suffix is GONE from the heading: `grep -c 'Anthropic IP allowlist (forward-looking)' README.md` is `0`.
    - The new section references Caddyfile.prod by name: `grep -c 'Caddyfile.prod' README.md` is `>= 1` (this is the existing reference style; verify it remains).
    - The single-worker section is INTACT: `grep -c 'workers 1' README.md` is `>= 1` (preserves NFR-05 second clause).
    - No other sections changed: the diff is bounded to the previous lines 105-110 region — `git diff README.md | wc -l | xargs test 50 -ge` (small bounded diff; ~30 added + 4 removed = ~34 lines total in unified-diff format).
    - Full unit suite remains GREEN (the README change does not affect pytest): `uv run pytest -x -q` exits 0.
    - NFR-04 still locked: `uv run pytest tests/test_dependency_footprint.py -x` exits 0.
    - No source / test / scripts changes: `git diff --name-only -- src tests scripts` is empty for this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && grep -q 'Anthropic IP' README.md && grep -q 'workers 1' README.md && grep -c 'host Caddy layer' README.md && grep -c 'Caddyfile.prod' README.md && (grep -c 'Anthropic IP allowlist (forward-looking)' README.md; true) | head -1 | xargs test 0 -ge && uv run pytest -x -q && uv run pytest tests/test_dependency_footprint.py -x</automated>
  </verify>
  <done>README.md "### Anthropic IP allowlist" section is operator-actionable (numbered-bullet allowlist instructions, Caddyfile.prod reference, quarterly re-verify cadence); the existing single-worker constraint section is preserved; NFR-05 grep gates pass. Diff is bounded to the prior placeholder region; full suite GREEN; NFR-04 still locked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update .planning/REQUIREMENTS.md traceability table — flip TEST-01..06 + NFR-01..05 from Pending to Satisfied (Phase 8 plan-NN)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 271-281 (the 11 traceability rows this task updates — TEST-01..06 + NFR-01..05; verify exact line numbers as they exist now)
    - .planning/REQUIREMENTS.md lines 264-268 (the OBS rows — Phase 7 territory; this task DOES NOT touch them)
    - .planning/REQUIREMENTS.md lines 196-218 (the traceability table format header and earlier rows — confirm the column structure: `| Requirement | Phase | Status |`)
    - .planning/phases/08-full-tests-24h-soak/08-01-PLAN.md frontmatter `requirements` field (NFR-04)
    - .planning/phases/08-full-tests-24h-soak/08-02-PLAN.md frontmatter `requirements` field (TEST-01)
    - .planning/phases/08-full-tests-24h-soak/08-03-PLAN.md frontmatter `requirements` field (TEST-03, TEST-04, TEST-06)
    - .planning/phases/08-full-tests-24h-soak/08-04-PLAN.md frontmatter `requirements` field (TEST-02)
    - .planning/phases/08-full-tests-24h-soak/08-05-PLAN.md frontmatter `requirements` field (TEST-05, NFR-01, NFR-02, NFR-03)
    - .planning/REQUIREMENTS.md OBS-04 row at line 267 (Phase 7 / 07-07 already updated this; the format `Satisfied (07-07 gap closure — CR-01)` is the model to mirror)
  </read_first>
  <behavior>
    - 11 traceability rows updated:
      - TEST-01 → `Satisfied (08-02)`
      - TEST-02 → `Satisfied (08-04)`
      - TEST-03 → `Satisfied (08-03)`
      - TEST-04 → `Satisfied (08-03 docstring marker; test originated in Phase 5)`
      - TEST-05 → `Satisfied (08-05 smoke gate; full 24h via workflow_dispatch)`
      - TEST-06 → `Satisfied (08-03)`
      - NFR-01 → `Satisfied (08-05 report-gate)`
      - NFR-02 → `Satisfied (08-05 driver --concurrency 50)`
      - NFR-03 → `Satisfied (08-05 report-gate)`
      - NFR-04 → `Satisfied (08-01 dep-footprint test)`
      - NFR-05 → `Satisfied (08-06 README delta)`
    - The 6 SUB-* rows (line 282-288) remain `Pending` (Phase 9 territory).
    - The OBS rows remain unchanged (Phase 7 territory; OBS-04 already says `Satisfied (07-07 gap closure — CR-01)`).
    - All other rows unchanged.
    - The Coverage and Per-phase counts sections at lines 290-304 are unchanged (they count mappings, not status).
  </behavior>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Locate the traceability table (the section starting `## Traceability` at line 194; the table format is `| Requirement | Phase | Status |`). Verify the exact line numbers of the 11 Phase-8 rows (TEST-01..06 + NFR-01..05) — they currently appear at lines ~271-281 per the read context but may have drifted. Use the Edit tool to update each of the 11 rows' Status column from `Pending` to the per-row mapping listed in the `<behavior>` block above. The exact mapping is sourced from the union of the `requirements` frontmatter fields across plans 08-01..05 (which the executor MUST verify by reading each plan's frontmatter):
       - 08-01 owns NFR-04
       - 08-02 owns TEST-01
       - 08-03 owns TEST-03, TEST-04, TEST-06
       - 08-04 owns TEST-02
       - 08-05 owns TEST-05, NFR-01, NFR-02, NFR-03
       - 08-06 owns NFR-05 (this plan)
    For each of the 11 rows, the new Status column value is the format `Satisfied (08-NN)` with optional clarifying suffix:
       - TEST-04: `Satisfied (08-03 docstring marker; test originated in Phase 5)` — per 08-PATTERNS.md the regression test was written in Phase 5 (FRAG-04); Phase 8 added the TEST-04 ownership marker via Plan 08-03 Task 4 docstring edit
       - TEST-05: `Satisfied (08-05 smoke gate; full 24h via workflow_dispatch)` — the smoke ran as part of 08-05 Task 4; the full 24h is one workflow_dispatch trigger away per soak.yml
       - NFR-02: `Satisfied (08-05 driver --concurrency 50)` — the requirement is implicit in the driver's --concurrency 50 default per 08-VALIDATION.md per-task row `08-05 / NFR-02 (concurrency)`
       - All other rows use the simple `Satisfied (08-NN)` format
    Edit each row individually via the Edit tool (one Edit call per row OR a single Edit call per the table-block if the rows are contiguous and the executor can construct an unambiguous old-string match). Per the GSD-style precedent set by the OBS-04 row update in Plan 07-07: the format follows `Satisfied (07-07 gap closure — CR-01)` style. DO NOT touch the OBS rows (lines 264-268 — Phase 7 / 07-07 closed those). DO NOT touch the SUB rows (lines 282-288 — Phase 9 territory). DO NOT touch the Coverage / Per-phase counts (lines 290-304 — those count mappings, not status). DO NOT touch the table header. DO NOT add a row. DO NOT remove a row. NO source / test changes. NO scripts changes. After all 11 updates, verify via grep: `grep -E '^\\| TEST-0[1-6] .*\\| Satisfied \\(08' .planning/REQUIREMENTS.md | wc -l | xargs` is `6` AND `grep -E '^\\| NFR-0[1-5] .*\\| Satisfied \\(08' .planning/REQUIREMENTS.md | wc -l | xargs` is `5` (11 total Phase-8 Satisfied rows). Confirm zero Phase-8 rows still say `Pending`: `grep -E '^\\| TEST-0[1-6] .*\\| Pending' .planning/REQUIREMENTS.md | wc -l | xargs` is `0` AND `grep -E '^\\| NFR-0[1-5] .*\\| Pending' .planning/REQUIREMENTS.md | wc -l | xargs` is `0`.
  </action>
  <acceptance_criteria>
    - All 6 TEST-* rows flipped to Satisfied (08-NN): `grep -cE '^\\| TEST-0[1-6] .*\\| Satisfied \\(08' .planning/REQUIREMENTS.md` is `6`.
    - All 5 NFR-* rows flipped to Satisfied (08-NN): `grep -cE '^\\| NFR-0[1-5] .*\\| Satisfied \\(08' .planning/REQUIREMENTS.md` is `5`.
    - Zero Phase-8 rows still Pending: `grep -cE '^\\| TEST-0[1-6] .*\\| Pending' .planning/REQUIREMENTS.md` is `0` AND `grep -cE '^\\| NFR-0[1-5] .*\\| Pending' .planning/REQUIREMENTS.md` is `0`.
    - TEST-04 has the Phase 5 origin clarifier: `grep -c 'TEST-04.*originated in Phase 5' .planning/REQUIREMENTS.md` is `>= 1`.
    - TEST-05 has the workflow_dispatch clarifier: `grep -c 'TEST-05.*workflow_dispatch' .planning/REQUIREMENTS.md` is `>= 1`.
    - OBS-04 row UNCHANGED (still says Satisfied 07-07): `grep -E '^\\| OBS-04 .*\\| Satisfied \\(07-07' .planning/REQUIREMENTS.md | wc -l | xargs` is `>= 1`.
    - OBS-02 row UNCHANGED (still says Deferred to v2): `grep -E '^\\| OBS-02 .*\\| Deferred to v2' .planning/REQUIREMENTS.md | wc -l | xargs` is `>= 1`.
    - SUB-* rows UNCHANGED (still Pending): `grep -cE '^\\| SUB-0[1-7] .*\\| Pending' .planning/REQUIREMENTS.md` is `7`.
    - Coverage / Per-phase counts UNCHANGED: `grep -c '^- v1 requirements: 75 total' .planning/REQUIREMENTS.md` is `>= 1` AND `grep -c '^- Phase 8 .*: 11' .planning/REQUIREMENTS.md` is `>= 1`.
    - No source / test / scripts changes: `git diff --name-only -- src tests scripts` is empty for this task.
    - Full unit suite remains GREEN: `uv run pytest -x -q` exits 0.
    - NFR-04 still locked: `uv run pytest tests/test_dependency_footprint.py -x` exits 0.
    - The diff to .planning/REQUIREMENTS.md is bounded (only the 11 Phase-8 rows changed): `git diff --stat .planning/REQUIREMENTS.md | tail -1 | grep -oE '[0-9]+ insertion' | head -1` reports a small line count (typically ≤ 25 changed lines for 11 row updates).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-mcp && test "$(grep -cE '^\\| TEST-0[1-6] .*\\| Satisfied \\(08' .planning/REQUIREMENTS.md)" -eq 6 && test "$(grep -cE '^\\| NFR-0[1-5] .*\\| Satisfied \\(08' .planning/REQUIREMENTS.md)" -eq 5 && test "$(grep -cE '^\\| TEST-0[1-6] .*\\| Pending' .planning/REQUIREMENTS.md)" -eq 0 && test "$(grep -cE '^\\| NFR-0[1-5] .*\\| Pending' .planning/REQUIREMENTS.md)" -eq 0 && grep -E '^\\| OBS-04 .*\\| Satisfied \\(07-07' .planning/REQUIREMENTS.md && uv run pytest -x -q && uv run pytest tests/test_dependency_footprint.py -x</automated>
  </verify>
  <done>11 Phase-8 traceability rows (TEST-01..06 + NFR-01..05) flipped from Pending to Satisfied (Phase 8 plan-NN) with appropriate per-row clarifiers (TEST-04 Phase 5 origin; TEST-05 workflow_dispatch); OBS rows + SUB rows untouched; Coverage / Per-phase counts unchanged; full suite GREEN; NFR-04 still locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md operator instructions ↔ deployed reality | A README that says "the operator should ..." but is silent on the WHERE / HOW / WHEN leaves new operators inventing their own allowlist story; the structured 3-bullet replacement closes that gap |
| .planning/REQUIREMENTS.md traceability ↔ Phase 9 submission claims | A registry submission PR claiming "TEST-* / NFR-* coverage" needs the traceability table to honestly reflect status; Pending rows would invalidate the claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-OPERATOR-DRIFT | Information Disclosure (operator misconfiguration) | README.md "Anthropic IP allowlist" section | mitigate | Task 1 replaces the Phase 1 forward-looking placeholder with operator-actionable instructions: WHERE to apply (host Caddy layer, NOT MCP container), WHO owns the source list (Anthropic operator contact), WHEN to re-verify (Phase 9 + quarterly). The numbered bullets mirror the existing `## Caddy header requirements` style for consistency. |
| T-TRACE-FALSE-CLOSURE | Tampering (silently marking requirements satisfied without evidence) | .planning/REQUIREMENTS.md traceability table | mitigate | Task 2 references the per-row plan IDs explicitly (08-NN format) so any reviewer can backtrack from a `Satisfied` claim to the originating PLAN.md to verify the contract. The TEST-04 row preserves Phase 5 origin (the test predates Phase 8); the TEST-05 row honestly distinguishes smoke gate (run by 08-05 Task 4) vs full 24h (deferred to manual workflow_dispatch). |
| T-OBS-PHASE-CROSSOVER | Tampering (Phase 8 plan accidentally modifies Phase 7 territory) | .planning/REQUIREMENTS.md OBS rows | mitigate | Task 2's acceptance criteria explicitly assert that OBS-04 still says `Satisfied (07-07 gap closure — CR-01)` and OBS-02 still says `Deferred to v2 (D7-05)`. Phase 8 OWNS only TEST-* and NFR-* rows; the SUB-* rows remain Pending for Phase 9 to close. |
| T-NFR-05-UNVERIFIED | Tampering (NFR-05 is a doc requirement; without the README delta, the requirement is unverifiable) | NFR-05 second clause (single-worker doc) | accept | The single-worker section at README.md:71-90 already exists and is preserved. Task 1's acceptance criteria explicitly verify both NFR-05 clauses via grep: `grep -q 'Anthropic IP'` AND `grep -q 'workers 1'`. The first grep targets the new section; the second targets the existing untouched section. |
</threat_model>

<verification>
- VALIDATION.md per-task table rows covered by this plan (all must flip to ✅ green):
  - `08-06 / NFR-05 (allowlist doc)` — `grep -q 'Anthropic IP' README.md` exits 0
  - `08-06 / NFR-05 (single-worker doc)` — `grep -q 'workers 1' README.md` exits 0 (already present at README.md:71-75; verified preserved)
- Phase 8 traceability is fully closed: 11 rows flipped from Pending to Satisfied (08-NN).
- OBS rows preserved (Phase 7 territory): OBS-04 still says `Satisfied (07-07 gap closure — CR-01)`; OBS-02 still says `Deferred to v2 (D7-05)`.
- SUB rows preserved (Phase 9 territory): all 7 SUB-* rows still say `Pending`.
- Full unit suite GREEN: `uv run pytest -x -q` exits 0.
- NFR-04 still locked: `uv run pytest tests/test_dependency_footprint.py -x` exits 0.
- No source / test / scripts changes: `git diff --name-only -- src tests scripts` is empty.
- File-touch discipline: `git diff --name-only` shows exactly two MODIFIED files from this plan — `README.md`, `.planning/REQUIREMENTS.md`. No new files; no source/test/scripts edits.
- Ruff clean: `uv run ruff check src tests scripts` exits 0 AND `uv run ruff format --check src tests scripts` exits 0 (the README + REQUIREMENTS.md edits do not affect Python files; ruff still passes from prior plans).
</verification>

<success_criteria>
- NFR-05 closes: README.md operator-instruction section for Anthropic IP allowlist is actionable (3 numbered bullets pointing at host Caddy layer / Anthropic operator contact / Phase 9 + quarterly re-verify cadence); the single-worker constraint section at README.md:71-90 is preserved; both `grep -q 'Anthropic IP'` and `grep -q 'workers 1'` succeed.
- All 11 Phase 8 requirement IDs (TEST-01..06 + NFR-01..05) are flipped to `Satisfied (Phase 8 plan-NN)` in `.planning/REQUIREMENTS.md` traceability table, with per-row plan ID references that allow Phase 9 reviewers to backtrack to the originating PLAN.md.
- OBS rows preserved as Phase 7 territory (OBS-04 satisfied via 07-07 gap closure; OBS-02 deferred to v2).
- SUB rows preserved as Phase 9 territory (all 7 still Pending).
- No source code, test code, scripts/, or new files changed by this plan — Phase 8's coverage work is complete; this plan is documentation-only.
- Full pytest suite GREEN; NFR-04 still locked; ruff clean.
- Phase 8 is now ready for `/gsd-verify-work` — the entire phase has shipped its 6 plans across 5 waves (Wave 0: 08-01; Wave 1: 08-02; Wave 2: 08-03; Wave 3: 08-04 + 08-05; Wave 4: 08-06).
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-tests-24h-soak/08-06-SUMMARY.md` documenting:
- The replaced README.md section's before / after text (paste verbatim).
- The 11 traceability table updates as a markdown table: requirement ID, before status, after status.
- Confirmation that OBS rows + SUB rows are unchanged.
- Confirmation that no source / test / scripts files were modified.
- Confirmation that NFR-04 dependency-footprint test still passes.
- A 1-line forward pointer: "Phase 8 plan set complete (6 plans across 5 waves). Ready for `/gsd-verify-work 8`. Phase 9 (registry submission) is the next phase per ROADMAP.md."
</output>
</content>
</invoke>