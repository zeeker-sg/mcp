---
phase: 06.1-envelope-hardening-gaps
verified: 2026-05-14T22:39:52Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: null
gaps: []
human_verification: []
---

# Phase 06.1: Envelope Hardening Gap Closure — Verification Report

**Phase Goal:** Close the 4 user-visible-quality findings the manual UAT against
`mcp.zeeker.sg` surfaced on top of Phase 6. After 6.1 lands, every
`list_databases` row carries a non-empty `license`/`license_url`, every
`_citation` is meaningful regardless of the caller's `columns=` choice,
`query_table` against `sg-gov-newsrooms.mlaw_news` heavy columns succeeds, and
the live drift probe matches the actual upstream `/-/metadata.json`.

**Verified:** 2026-05-14T22:39:52Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `MetadataCache.license_for_sync` on a cold cache (`_data is None`) returns `config.LICENSES.get(database, ("", ""))` — NOT `("", "")` (D6.1-01)                                       | ✓ VERIFIED | `src/mcp_zeeker/core/metadata_cache.py:231-234` — `if self._data is None: return config.LICENSES.get(database, ("", ""))`. Regression test `test_license_for_sync_cold_cache_falls_back_to_config` (tests/test_metadata_cache.py:219-243) asserts both the known-DB fallback (`CC-BY-4.0`) AND the genuinely-unknown DB returning `("", "")`. PASSED. |
| 2   | Per-row `_citation` on `query_table` / `fetch` / `search` is meaningfully substituted even when `columns=` excludes citation-template placeholder columns (D6.1-02)                  | ✓ VERIFIED | `placeholder_columns()` in `src/mcp_zeeker/core/citation.py:72-96`. Augmentation in `query_table` (retrieval.py:347-355 compute; line 461 inject in `_col=`; lines 552-557 strip), `fetch` (retrieval.py:779-784 strip), `search._one_table` (search.py:201-210 inject; no strip needed — whitelist 9-key reshape). Regression tests `test_placeholder_columns_extracts_template_names` (pure-function) and `test_citation_populated_under_narrow_columns_projection` (end-to-end mcp_client with stub row, asserts `_citation` contains "Foo v Bar" + top-level keys are exactly `{"_citation", "retrieved_content"}`) PASSED. |
| 3   | `query_table(database='sg-gov-newsrooms', table='mlaw_news', columns=['content_text'], limit=1)` returns a row with `retrieved_content.content_text` populated AND a `_policy` block — NOT `upstream_unavailable` (D6.1-03) | ✓ VERIFIED | Root cause identified: upstream Datasette emits invalid SQL when `_col=` excludes the implicit `published_date` sort column. Fix in `src/mcp_zeeker/tools/retrieval.py:378-383` — `_sort=rowid` injected when caller does not specify a sort. Live regression test `tests/test_heavy_column_upstream.py::test_mlaw_news_heavy_column_returns_content` PASSED under `UPSTREAM_URL=https://data.zeeker.sg ZEEKER_LIVE=1`. |
| 4   | `tests/test_metadata_cache.py::test_live_metadata_parseable` asserts upstream parseability + dual-layer invariant, NOT a literal upstream license value (D6.1-04)                      | ✓ VERIFIED | Test rewritten as `@pytest.mark.parametrize` over all 4 `ALLOWED_DATABASES`; asserts `get_database_license(db)` returns string OR None — never empty string (`tests/test_metadata_cache.py:264-307`). Separate non-live `test_policy_license_unaffected_by_upstream_drift` (lines 316-489) parametrized over 4 drift cases stubs `/-/metadata.json` with drifted upstream values, force-refreshes the cache to verify warm-cache observes the drift, dispatches `query_table`, then asserts `_policy.license` equals operator-locked `CONTENT_POLICIES[(db,table)]["license"]` regardless. Live probe GREEN against `data.zeeker.sg`. |
| 5   | Phase 6 envelope shape is unchanged — no new keys, no new tools, no schema changes                                                                                                   | ✓ VERIFIED | `grep -rn "@mcp.tool" src/mcp_zeeker/` returns 6 registrations (3 discovery + 1 search + 2 retrieval), unchanged. `config.CONTENT_POLICIES` / `CITATION_TEMPLATES` / `LICENSES` content unchanged. No edits to `core/envelope.py`. No changes to `app.py` lifespan, `RetrievedAtMiddleware` registration, or contextvar plumbing. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                          | Expected                                                                                       | Status     | Details                                                                                                                                                                                                          |
| ------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/mcp_zeeker/core/metadata_cache.py`           | `license_for_sync` cold-cache fallback to `config.LICENSES`                                    | ✓ VERIFIED | Line 234: `return config.LICENSES.get(database, ("", ""))` on the `_data is None` branch. Docstring at 209-227 documents corrected D6-04 fallback chain.                                                          |
| `src/mcp_zeeker/core/citation.py`                 | `placeholder_columns(template)` helper exists, exported, filters synthetic `{retrieved_at}`    | ✓ VERIFIED | Lines 72-96; uses `string.Formatter().parse`, discards `"retrieved_at"`. Imported elsewhere as `from mcp_zeeker.core.citation import placeholder_columns`.                                                        |
| `src/mcp_zeeker/tools/retrieval.py`               | Transparent citation-column augmentation in `query_table` + `fetch`; `_sort=rowid` for D6.1-03 | ✓ VERIFIED | `query_table`: compute at 347-355; `& visible` defense-in-depth at 353; `_sort=rowid` at 383; injection in `_col=` at 461; strip at 552-557. `fetch`: strip at 779-784. Both use `placeholder_columns()` import at 57. |
| `src/mcp_zeeker/core/search.py`                   | `_one_table` augments `_col=` with placeholder columns                                         | ✓ VERIFIED | Lines 201-210: `search_added_columns = placeholder_columns(template) - preview_cols` then appended to `_col=` params (sorted). Whitelist 9-key reshape at 249-269 means no strip is needed.                       |
| `tests/test_metadata_cache.py`                    | Cold-cache regression + rewritten live drift probe + dual-layer invariant non-live test        | ✓ VERIFIED | `test_license_for_sync_cold_cache_falls_back_to_config` (renamed from `..._returns_empty`) at 219; `test_live_metadata_parseable` rewritten at 264-307; `test_policy_license_unaffected_by_upstream_drift` at 325. |
| `tests/test_citation_synthesis.py`                | Narrow-columns citation regression + `placeholder_columns` pure-function test                  | ✓ VERIFIED | Lines 307-320 (pure-function); lines 323-446 (end-to-end). Both PASSED.                                                                                                                                          |
| `tests/test_heavy_column_upstream.py`             | Live ZEEKER_LIVE=1-gated mlaw_news heavy-column regression test                                 | ✓ VERIFIED | File exists (109 lines). `@pytest.mark.live` decorator. Manually binds DatasetteClient + MetadataCache + ParentPKCache + tool_started_at. PASSED under live env.                                                  |

### Key Link Verification

| From                                                                            | To                                                              | Via                                                                                            | Status   | Details                                                              |
| ------------------------------------------------------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------- |
| `src/mcp_zeeker/core/metadata_cache.py::license_for_sync`                        | `src/mcp_zeeker/config.py::LICENSES`                            | `config.LICENSES.get(database, ("", ""))` on cold-cache branch                                  | ✓ WIRED  | Line 234. Imported via `from mcp_zeeker import config` at line 229. |
| `src/mcp_zeeker/tools/retrieval.py::query_table`                                 | `src/mcp_zeeker/core/citation.py::placeholder_columns`           | compute `added_columns` BEFORE upstream call; strip AFTER `synthesize_citation`                 | ✓ WIRED  | Import at line 57. Compute at 347-355. Strip at 552-557.            |
| `src/mcp_zeeker/core/search.py::_one_table`                                      | `src/mcp_zeeker/core/citation.py::placeholder_columns`           | augment `_col` param list with placeholder columns                                              | ✓ WIRED  | Import at line 55. Augment at 201-210.                              |
| `src/mcp_zeeker/tools/retrieval.py::query_table` Step 8                           | upstream Datasette `_sort=rowid` override                       | sidesteps invalid-SQL trap on tables with per-table default-sort directives                     | ✓ WIRED  | Lines 378-383. Live test confirms.                                  |

### Data-Flow Trace (Level 4)

| Artifact                              | Data Variable          | Source                                                                                                                            | Produces Real Data | Status     |
| ------------------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ---------- |
| `metadata_cache.py::license_for_sync`  | `(license, license_url)` | `self._data` (warm) OR `config.LICENSES.get(database, ...)` (cold/empty)                                                          | Yes                | ✓ FLOWING  |
| `retrieval.py::query_table _citation`  | row[_citation]         | `synthesize_citation(database, table, upstream_row, retrieved_at_for_call)` — reads from `upstream_row` BEFORE strip               | Yes                | ✓ FLOWING  |
| `retrieval.py::query_table retrieved_content` | retrieved_content[heavy_col] | upstream Datasette `_col=heavy_col` projection with `_sort=rowid` override                                                  | Yes                | ✓ FLOWING (live test confirms)  |
| `search.py::_one_table _citation`     | normalized[_citation]  | `synthesize_citation(db, table, r, retrieved_at_for_call)` — reads from `r` with augmented `_col=` projection                     | Yes                | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                                                  | Command                                                                                                                          | Result                                                       | Status |
| ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------ |
| Full test suite GREEN                                                                     | `uv run pytest tests/ -x -q`                                                                                                     | 334 passed, 7 skipped (4 live metadata + 1 live heavy + 2 carry-forward) | ✓ PASS |
| Cold-cache regression test passes                                                         | `uv run pytest tests/test_metadata_cache.py::test_license_for_sync_cold_cache_falls_back_to_config -v`                            | PASSED                                                       | ✓ PASS |
| Warm-cache regression test still passes (no regression)                                   | `uv run pytest tests/test_metadata_cache.py::test_license_for_sync_warm_cache_returns_upstream -v`                                | PASSED                                                       | ✓ PASS |
| Citation augmentation end-to-end test passes                                              | `uv run pytest tests/test_citation_synthesis.py::test_citation_populated_under_narrow_columns_projection -v`                      | PASSED                                                       | ✓ PASS |
| Placeholder-extractor pure-function test passes                                           | `uv run pytest tests/test_citation_synthesis.py::test_placeholder_columns_extracts_template_names -v`                             | PASSED                                                       | ✓ PASS |
| Dual-layer invariant test passes (4 drift parametrizations)                                | `uv run pytest tests/test_metadata_cache.py::test_policy_license_unaffected_by_upstream_drift -v`                                  | 4 PASSED                                                     | ✓ PASS |
| Live drift probe GREEN against `data.zeeker.sg`                                            | `UPSTREAM_URL=https://data.zeeker.sg ZEEKER_LIVE=1 uv run pytest -m live tests/test_metadata_cache.py -v`                          | 4 PASSED (all ALLOWED_DATABASES)                             | ✓ PASS |
| Live mlaw_news heavy-column probe GREEN against `data.zeeker.sg`                           | `UPSTREAM_URL=https://data.zeeker.sg ZEEKER_LIVE=1 uv run pytest tests/test_heavy_column_upstream.py -v`                            | PASSED                                                       | ✓ PASS |
| Ruff check clean on all 8 modified files                                                  | `uv run ruff check src/mcp_zeeker/core/citation.py src/mcp_zeeker/core/metadata_cache.py src/mcp_zeeker/core/search.py src/mcp_zeeker/tools/retrieval.py tests/test_citation_synthesis.py tests/test_heavy_column_upstream.py tests/test_metadata_cache.py tests/tools/test_query_table.py` | All checks passed!                                           | ✓ PASS |
| 4 atomic commits exist                                                                    | `git log --oneline 49616f0..HEAD`                                                                                                | c490277 (D6.1-01), 078f0f2 (D6.1-02), 75b99a7 (D6.1-03), dc7d62b (D6.1-04) all present | ✓ PASS |
| Tool count unchanged (no new tools)                                                        | `grep -rn "@mcp.tool" src/mcp_zeeker/`                                                                                           | 6 tool registrations (unchanged)                              | ✓ PASS |
| No debt markers in modified files                                                          | `grep -nE "TODO\|TBD\|FIXME\|XXX\|HACK\|PLACEHOLDER\|phase_06_1_investigation_only" <8 modified files>`                              | 0 matches                                                    | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| n/a — no conventional `scripts/*/tests/probe-*.sh` exist in repo | — | — | SKIPPED (no probes declared) |

### Requirements Coverage

| Requirement | Source Plan          | Description                                                                                                                                                                                  | Status      | Evidence                                                                                                              |
| ----------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| ENV-02      | 06.1-01 (gap closure) | Provenance fields: `source`, `database`, `table`, `retrieved_at`, `license`, `attribution` — gap closure ensures cold-cache `license` is non-empty                                              | ✓ SATISFIED | `MetadataCache.license_for_sync` cold-cache fallback (Truth #1). Live `list_databases` returns non-empty license (live probe). |
| ENV-03      | 06.1-01 (gap closure) | License is per-database (looked up from `config.LICENSES`), not hardcoded `CC-BY-4.0` — gap closure restores the lookup chain on cold cache                                                    | ✓ SATISFIED | Cold-cache branch now returns `config.LICENSES.get(database, ...)`. Dual-layer invariant test pins the divergence between upstream-as-is and operator-locked `_policy.license`. |
| ENV-04      | 06.1-01 (gap closure) | For tables without native `citation` field, provenance includes a synthesized citation string — gap closure restores meaningful substitution when caller narrows `columns=`                    | ✓ SATISFIED | `placeholder_columns()` helper + transparent column augmentation in `query_table`/`fetch`/`_one_table` (Truth #2). Regression test asserts `_citation` substitutes case_name when caller passes only `columns=["content_text"]`. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| _(none in modified files)_ | — | grep for TODO/TBD/FIXME/XXX/HACK/PLACEHOLDER/phase_06_1_investigation_only on all 8 modified files: 0 matches | — | — |

### Human Verification Required

None for the four 6.1 must-haves themselves — all are covered by automated
regression tests (live + non-live).

The Phase 6 F-4 operator-review sign-off block in
`tests/manual/PHASE6-CLIENT-VERIFY.md` remains UNSIGNED — this is the
operator's manual UAT obligation against `mcp.zeeker.sg` and is a SEPARATE
gate that 6.1 does NOT close (per scope boundary). The verifier was
explicitly instructed by the verification request that this gate is
out-of-scope for the 6.1 verification.

### Gaps Summary

None. All 5 must-haves verified.

**Code review findings (WR-01, WR-02, WR-03, IN-01..IN-04) recorded in
`06.1-REVIEW.md` are post-execution defense-in-depth improvements and
documentation nits — they are NOT gaps in goal achievement.** None of them
breaks any of the 4 manual-UAT findings 6.1 is closing:

- **WR-01** (`& visible` parity on search-path augmentation): the `_one_table`
  augmentation lacks the defensive intersection that `query_table` applies.
  Today there is no `CITATION_TEMPLATES` entry that references a hidden column,
  so the user-visible contract is unaffected; it's a future-config-drift
  hardening recommendation.
- **WR-02** (`_sort=rowid` parity on `fetch` and `_one_table`): the fix landed
  in `query_table` where the bug actually surfaced. Both `fetch` (URL-keyed
  `__exact` filter masks the per-table default-sort trap empirically) and
  `_one_table` (FTS rank overrides per-table sort empirically) are not
  triggering the bug today. Recommendation is a future-resilience parity fix.
- **WR-03** (observability when template references a column upstream doesn't
  return): nice-to-have INFO log; the substitution path already degrades
  gracefully via `_SafeDict`.
- **IN-01..IN-04**: docstring/style/release-notes nits.

These are valuable Phase 7 / future-hardening items but do not block goal
achievement on Phase 6.1. The code review report itself classifies them as
WARNING / INFO and reports `0 critical` findings.

## Notes on Verification Process

1. **Live test default UPSTREAM_URL:** The project's `.env.example` documents
   `UPSTREAM_URL=http://datasette:8001` (a Docker hostname). Running live tests
   on a host machine without that hostname resolvable requires
   `UPSTREAM_URL=https://data.zeeker.sg ZEEKER_LIVE=1 uv run pytest -m live ...`
   to reach the real production endpoint. With the env var set, all 5 live
   tests (4 metadata parseability + 1 mlaw_news heavy-column) PASS GREEN, as
   the SUMMARY documents. Without the override, the heavy-column live test
   fails with `Errno 8 nodename nor servname provided, or not known` — an
   environmental DNS-resolution issue, NOT a code defect.

2. **Code review was performed AFTER the four commits landed** and is recorded
   in `06.1-REVIEW.md`. It identified 0 BLOCKER findings — every WARNING /
   INFO item is an improvement opportunity, not a goal-breaking defect.

3. **Phase 6.1 is mode: mvp** per ROADMAP.md frontmatter, but the goal is NOT
   a User Story (`As a [role]...`). The verification request supplied
   roadmap-style Success Criteria as the truths to verify, and goal-backward
   verification operates against those criteria. User Story validation would
   block under MVP mode, but the verification request explicitly enumerated
   the must_haves drawn from the ROADMAP Success Criteria + PLAN frontmatter
   — those form the verification contract for this report.

---

_Verified: 2026-05-14T22:39:52Z_
_Verifier: Claude (gsd-verifier)_
